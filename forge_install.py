# -*- coding: utf-8 -*-
"""
forge_install.py
================
Self-installing forge2 package.

Usage:
  1. Copy this file to your Pythonista Documents folder
  2. Run it once in Pythonista
  3. Delete this file when done
  4. Set up your Shortcut — see forge2/onboarding.md
"""

import os

FILES = {
    'forge2/__init__.py': '# forge2 package marker\n',
    'forge2/anchor_tools.py': '# -*- coding: utf-8 -*-\n"""\nanchor_tools.py\n===============\nSmall anchor-matching helpers for AST/text range replacement.\n"""\n\ndef _normalize(s):\n    return \' \'.join((s or \'\').strip().split())\n\n\ndef find_anchor_line_indexes(lines, anchor, match_mode=\'exact\'):\n    """\n    Return 0-based indexes of matching lines.\n    """\n    out = []\n    needle = anchor or \'\'\n    for idx, line in enumerate(lines):\n        if match_mode == \'fuzzy\':\n            if _normalize(needle) == _normalize(line):\n                out.append(idx)\n        else:\n            if needle in line:\n                out.append(idx)\n    return out\n\n\ndef resolve_single_anchor(lines, anchor, match_mode=\'exact\', occurrence=1, expect=1):\n    matches = find_anchor_line_indexes(lines, anchor, match_mode=match_mode)\n\n    if expect is not None and len(matches) != expect:\n        msg = \'Anchor matched %d times, expected %d\' % (len(matches), expect)\n        if len(matches) > 1:\n            candidates = []\n            for idx in matches:\n                preview = lines[idx].strip()\n                if len(preview) > 60:\n                    preview = preview[:57] + \'...\'\n                candidates.append(\'  line %d: %s\' % (idx + 1, preview))\n            msg += \'\\nCandidates:\\n\' + \'\\n\'.join(candidates)\n        return None, msg\n\n    occ = max(1, int(occurrence or 1))\n    if occ > len(matches):\n        return None, \'Occurrence %d out of range for %d matches\' % (occ, len(matches))\n\n    return matches[occ - 1], None\n',
    'forge2/ast_bridge.py': '# -*- coding: utf-8 -*-\n"""\nast_bridge.py\n=============\nAST helpers for forge2. Now self-contained - no dependency on forge1.\n"""\n\nfrom forge2.ast_finder import (\n    supports_end_lineno,\n    find_method_range,\n    find_class_range,\n    find_function_range,\n    find_global_assign_range,\n    find_class_assign_range,\n)\n\n\ndef ast_capabilities():\n    return {\n        \'supports_end_lineno\': supports_end_lineno(),\n    }\n\n\ndef _normalize_range(rng):\n    if not rng:\n        return None\n    if isinstance(rng, tuple) and len(rng) == 2 and rng[0] == \'AMBIGUOUS\':\n        return None\n    return rng\n\n\ndef resolve_target_range(source_text, class_name, target_name):\n    """\n    Returns:\n      (start_lineno, end_lineno, kind)\n    or\n      (None, None, None)\n    """\n    if class_name and target_name:\n        if target_name.startswith(\'@\'):\n            rng = _normalize_range(find_class_assign_range(source_text, class_name, target_name[1:]))\n            if rng:\n                return rng[0], rng[1], \'class_assign\'\n\n        if target_name == \'*\':\n            rng = _normalize_range(find_class_range(source_text, class_name))\n            if rng:\n                return rng[0], rng[1], \'class\'\n\n        rng = _normalize_range(find_method_range(source_text, class_name, target_name))\n        if rng:\n            return rng[0], rng[1], \'method\'\n\n    if (not class_name) and target_name:\n        if target_name.startswith(\'@\'):\n            rng = _normalize_range(find_global_assign_range(source_text, target_name[1:]))\n            if rng:\n                return rng[0], rng[1], \'global_assign\'\n\n        rng = _normalize_range(find_function_range(source_text, target_name))\n        if rng:\n            return rng[0], rng[1], \'function\'\n\n    return None, None, None\n',
    'forge2/ast_finder.py': '# -*- coding: utf-8 -*-\n"""\nforge2.ast_finder\n=================\nAST-based range locators. Pure stdlib, no internal deps.\nCopied from forge/ast_finder.py - forge2 is now self-contained.\nAll finders return (start_line, end_line) 1-based inclusive,\nNone if not found, or ("AMBIGUOUS", matches) if multiple hits.\n"""\n\nimport ast\n\n\ndef supports_end_lineno():\n    src = "def f():\\n    return 1\\n"\n    t = ast.parse(src)\n    fn = t.body[0]\n    return hasattr(fn, "end_lineno") and fn.end_lineno is not None\n\n\ndef find_method_range(source, class_name, method_name):\n    tree = ast.parse(source)\n    matches = []\n    for node in tree.body:\n        if not (isinstance(node, ast.ClassDef) and node.name == class_name):\n            continue\n        items = [it for it in node.body if getattr(it, "lineno", None) is not None]\n        for idx, it in enumerate(items):\n            if not isinstance(it, (ast.FunctionDef, ast.AsyncFunctionDef)):\n                continue\n            if it.name != method_name:\n                continue\n            start_line = it.lineno\n            for d in getattr(it, "decorator_list", []) or []:\n                dl = getattr(d, "lineno", None)\n                if dl is not None:\n                    start_line = min(start_line, dl)\n            end_line = getattr(it, "end_lineno", None)\n            if idx + 1 < len(items):\n                next_it = items[idx + 1]\n                next_line = getattr(next_it, "lineno", None)\n                if next_line is not None and next_line > start_line:\n                    end_line = next_line - 1\n            if end_line is None:\n                raise RuntimeError("end_lineno not available; cannot locate method end reliably.")\n            matches.append((start_line, end_line))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n\n\ndef find_class_range(source, class_name):\n    tree = ast.parse(source)\n    matches = []\n    for node in tree.body:\n        if isinstance(node, ast.ClassDef) and node.name == class_name:\n            if getattr(node, "lineno", None) is None:\n                continue\n            if getattr(node, "end_lineno", None) is None:\n                raise RuntimeError("end_lineno not available; cannot locate class end reliably.")\n            matches.append((node.lineno, node.end_lineno))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n\n\ndef find_function_range(source, func_name):\n    tree = ast.parse(source)\n    matches = []\n    items = [n for n in tree.body if getattr(n, "lineno", None) is not None]\n    for idx, node in enumerate(items):\n        if not isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n            continue\n        if node.name != func_name:\n            continue\n        start_line = node.lineno\n        for d in getattr(node, "decorator_list", []) or []:\n            dl = getattr(d, "lineno", None)\n            if dl is not None:\n                start_line = min(start_line, dl)\n        end_line = getattr(node, "end_lineno", None)\n        if idx + 1 < len(items):\n            next_node = items[idx + 1]\n            next_line = getattr(next_node, "lineno", None)\n            if next_line is not None and next_line > start_line:\n                end_line = next_line - 1\n        if end_line is None:\n            raise RuntimeError("end_lineno not available; cannot locate function end reliably.")\n        matches.append((start_line, end_line))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n\n\ndef find_inner_function_range(source, class_name, method_name, inner_name):\n    tree = ast.parse(source)\n    outer = None\n    if class_name is not None:\n        for node in tree.body:\n            if isinstance(node, ast.ClassDef) and node.name == class_name:\n                for item in node.body:\n                    if isinstance(item, (ast.FunctionDef, ast.AsyncFunctionDef)) and item.name == method_name:\n                        outer = item\n                        break\n                if outer:\n                    break\n    else:\n        for node in tree.body:\n            if isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)) and node.name == method_name:\n                outer = node\n                break\n    if outer is None:\n        return None\n    items = [n for n in outer.body if getattr(n, "lineno", None) is not None]\n    matches = []\n    for idx, node in enumerate(items):\n        if not isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n            continue\n        if node.name != inner_name:\n            continue\n        start_line = node.lineno\n        for d in getattr(node, "decorator_list", []) or []:\n            dl = getattr(d, "lineno", None)\n            if dl is not None:\n                start_line = min(start_line, dl)\n        end_line = getattr(node, "end_lineno", None)\n        if idx + 1 < len(items):\n            next_node = items[idx + 1]\n            next_line = getattr(next_node, "lineno", None)\n            if next_line is not None and next_line > start_line:\n                end_line = next_line - 1\n        if end_line is None:\n            raise RuntimeError("end_lineno not available for inner function %r" % inner_name)\n        matches.append((start_line, end_line))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n\n\ndef find_global_assign_range(source, var_name):\n    tree = ast.parse(source)\n    matches = []\n    for node in tree.body:\n        if isinstance(node, ast.Assign):\n            for t in (node.targets or []):\n                if isinstance(t, ast.Name) and t.id == var_name:\n                    if getattr(node, "lineno", None) is None:\n                        continue\n                    end_line = getattr(node, "end_lineno", None)\n                    if end_line is None:\n                        raise RuntimeError("end_lineno not available; cannot locate assignment end reliably.")\n                    matches.append((node.lineno, end_line))\n                    break\n        elif isinstance(node, ast.AnnAssign):\n            t = getattr(node, "target", None)\n            if isinstance(t, ast.Name) and t.id == var_name:\n                if getattr(node, "lineno", None) is None:\n                    continue\n                end_line = getattr(node, "end_lineno", None)\n                if end_line is None:\n                    raise RuntimeError("end_lineno not available; cannot locate assignment end reliably.")\n                matches.append((node.lineno, end_line))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n\n\ndef find_class_assign_range(source, class_name, var_name):\n    tree = ast.parse(source)\n    matches = []\n    for node in tree.body:\n        if not (isinstance(node, ast.ClassDef) and node.name == class_name):\n            continue\n        for item in (node.body or []):\n            if isinstance(item, ast.Assign):\n                for t in (item.targets or []):\n                    if isinstance(t, ast.Name) and t.id == var_name:\n                        if getattr(item, "lineno", None) is None:\n                            continue\n                        end_line = getattr(item, "end_lineno", None)\n                        if end_line is None:\n                            raise RuntimeError("end_lineno not available; cannot locate assignment end reliably.")\n                        matches.append((item.lineno, end_line))\n                        break\n            elif isinstance(item, ast.AnnAssign):\n                t = getattr(item, "target", None)\n                if isinstance(t, ast.Name) and t.id == var_name:\n                    if getattr(item, "lineno", None) is None:\n                        continue\n                    end_line = getattr(item, "end_lineno", None)\n                    if end_line is None:\n                        raise RuntimeError("end_lineno not available; cannot locate assignment end reliably.")\n                    matches.append((item.lineno, end_line))\n    if not matches:\n        return None\n    if len(matches) > 1:\n        return ("AMBIGUOUS", matches)\n    return matches[0]\n',
    'forge2/bootstrap.md': "LLM BOOTSTRAP\n=============\n\nPurpose\n-------\nRead this first. Orients you to the user's Forge2 environment.\nRun the first bundle, paste the packet back, then ask what they want to work on.\n\nEnvironment\n-----------\n- Everything runs on iPhone in Pythonista 3.\n- Root is the Pythonista Documents folder.\n- Workflow: LLM writes patch bundles -> user copies to clipboard\n  -> iOS Shortcut runs forge2_shortcut.py -> run packet returned as truth.\n- Alternatively: forge_terminal.py provides an in-app terminal interface.\n\nFile system layout\n------------------\nforge2/                   Active patcher engine\nforge2/ops/               One file per op - source of truth for op detail\nforge2_shortcut.py        PRIMARY iOS Shortcut entry point\nforge_terminal.py         In-app terminal UI\nforge_ui.py               Async UI primitives\n<your project>/           Whatever the user is building\n\nForge2 module layout\n--------------------\nforge2/config.py          Constants\nforge2/registry.py        Op registry (auto-discovers ops via pkgutil)\nforge2/parser_main.py     Bundle parser\nforge2/engine.py          Execution engine\nforge2/bridge.py          Parse -> execute -> packet -> run storage\nforge2/ops/               One file per op\n\nAdding an op\n------------\nCreate forge2/ops/my_op.py with SPEC, validate(), execute().\nRegistry auto-discovers it. Nothing else needed.\n\nFirst bundle - run immediately\n------------------------------\n  LIST_FILES .\n\n  LIST_OPS\n\nPaste the run packet back, then ask the user what they want to work on.\n\nGREP - primary exploration tool\n--------------------------------\nGREP is the fastest way to get oriented. Use it before READ_FILE,\nbefore patching, and whenever you are unsure where something lives.\nEverything is on disc - GREP your way to answers.\n\nExact search in a directory:\n  GREP forge2/\n  PATTERN: def execute\n  MATCH: exact\n\nProject-wide fuzzy search:\n  GREP .\n  PATTERN: my function\n  MATCH: fuzzy\n  EXT: .py,.md,.txt\n  LIMIT: 20\n\nNarrow by path:\n  GREP .\n  PATTERN: run_bundle\n  FILTER: forge2/\n\nSingle file:\n  GREP forge2/bridge.py\n  PATTERN: import\n\nRules:\n- GREP before READ_FILE when locating something specific.\n- Use FILTER to cut noise on project-wide searches.\n- Use EXT: .py,.md,.txt for catch-up searches across code and docs.\n- Use LIMIT to cap large result sets.\n\nHELP - always available, always current\n----------------------------------------\nDo not guess directives. Run HELP first.\n\n  LIST_OPS              See every available op\n  HELP <OP>             Full directives, examples, failure modes\n\nRules:\n- Run HELP before any op with non-obvious directives.\n- Always run HELP after FAILED_PARSE before retrying.\n\nOp overview\n-----------\nUse HELP <OP> for full detail. This is a map only.\n\nExplore (zero-risk, use freely):\n  GREP              Search files - exact, fuzzy, regex. Always try this first.\n  LIST_FILES        Directory listing\n  LIST_TARGETS      Targets + line ranges + docstrings. Prefer over READ_FILE.\n  PREVIEW           Inspect an AST target with numbered lines\n  PREVIEW_FILE      Inspect any file by line range\n  READ_FILE         Full file - use sparingly, token-heavy\n  RUN_FILE          Execute a script, capture output\n\nFile orientation workflow (lean on these before READ_FILE):\n  1. GREP to locate what you need\n  2. LIST_TARGETS to see structure + line ranges\n  3. PREVIEW_FILE with a tight line range to read just the relevant code\n\nAST write (Python targets only):\n  REPLACE           Whole method / function / class / assignment\n  REPLACE_LINES     Range between anchors inside a target\n  REPLACE_LINE      Single line by anchor\n  INSERT_AFTER      New method after an existing one\n  INSERT_BEFORE     New method before an existing one\n  INSERT_INTO       Inject at anchor inside a method\n  APPEND_INTO       Add at bottom of method body\n  PREPEND_INTO      Add at top of method body\n\nFile write (any file):\n  CREATE_FILE       Create a new file\n  REPLACE_FILE      Full rewrite\n  REPLACE_FILE_LINES  Anchor-based surgical edit\n  REPLACE_FILE_RANGE  Line-range edit\n  INSERT_FILE_LINE  Insert at line number\n  COPY_FILE / MOVE_FILE / MOVE_DIR / DELETE_FILE / DELETE_DIR\n  APPEND_LIST       Append to a module-level list\n  SET_DICT_KEY      Set a key in a module-level dict\n\nManage:\n  LIST_RUNS         Recent runs\n  REVERT_RUN        Restore snapshots from a run\n  DIFF              Changes between run and disk\n  SHORTCUT          Fire an iOS Shortcut\n\nTarget syntax (AST ops)\n-----------------------\n  file.py::ClassName.method_name    method inside a class\n  file.py::ClassName.*              entire class\n  file.py::function_name            top-level function\n  file.py::@var_name                module-level assignment\n  file.py::ClassName.@var_name      class-level assignment\n\nBody syntax\n-----------\nBEGIN_BODY / END_BODY always safe. Required when body contains\nforge op keywords at column 0.\n\nFlat files\n----------\nAST ops need a named target. For plain text and flat code use:\nPREVIEW_FILE -> REPLACE_FILE_LINES or REPLACE_FILE_RANGE.\n\nForge UI\n--------\nforge_ui.py - dark theme async UI primitives. Fire-and-forget,\nresults via callbacks only. Use hardcoded app group path for\nDOCUMENTS_ROOT, not expanduser.\n\n  forge_ui.hud('Message')\n  forge_ui.show(text, title='Output')\n  forge_ui.pick(items, title='', on_select=None)\n  forge_ui.form(fields, title='', on_submit=None)\n\nui.delay callbacks receive a positional arg - always wrap:\n  ui.delay(0.1, lambda *a: my_fn())\n\nWorking style\n-------------\n- GREP first. Faster and cheaper than READ_FILE.\n- LIST_TARGETS before touching any file — shows structure and line ranges.\n- PREVIEW_FILE with a tight range rather than READ_FILE on large files.\n- HELP before any unfamiliar op. Always after FAILED_PARSE.\n- RUN_FILE probes beat assumptions every time.\n- Prefer REPLACE over anchors when a target is large or messy.\n- BEGIN_BODY / END_BODY always safe.\n- Relative paths only.\n- Trust run packets as truth.\n- Probe with importlib.reload() when testing a freshly patched module.\n\nFailure handling\n----------------\nFAILED_NOT_FOUND         Check path and target syntax.\nFAILED_AMBIGUOUS         Use PREVIEW to disambiguate.\nSKIPPED_ANCHOR_MISMATCH  PREVIEW target, tighten anchor, or REPLACE.\nFAILED_COMPILE           Rolled back automatically. Fix syntax.\nFAILED_PARSE             Run HELP before retrying.\n",
    'forge2/bridge.py': '# -*- coding: utf-8 -*-\n"""\nforge2.bridge\n=============\nThin wrappers over forge2 local core.\n"""\n\nfrom forge2.parser_main import parse_bundle\nfrom forge2.engine import execute_ops\nfrom forge2.run_packet import format_run_packet\n\n\ndef _hints_for_error(err):\n    from forge2.registry import get_op_module, OP_SPECS\n    op_found = None\n    for op_name in sorted(OP_SPECS.keys(), key=len, reverse=True):\n        if op_name in err:\n            op_found = op_name\n            break\n    if op_found is None:\n        return \'\'\n    mod = get_op_module(op_found)\n    if mod is None:\n        return \'\'\n    hints_dict = getattr(mod, \'HINTS\', {})\n    seen = set()\n    found = []\n    err_lower = err.lower()\n    for key, hint in hints_dict.items():\n        if key.lower() in err_lower and hint not in seen:\n            seen.add(hint)\n            found.append(\'Hint: \' + hint)\n    found.append(\'See: HELP \' + op_found)\n    return \'\\n\'.join(found)\n\ndef run_bundle(bundle_text, project_root, step=None, max_steps=None):\n    from forge2.run_storage import write_run_artifacts, prune_runs, now_stamp\n\n    parsed = parse_bundle(bundle_text)\n    synthetic_results = []\n\n    if parsed.errors:\n        for err in parsed.errors:\n            msg = err\n            hint_block = _hints_for_error(err)\n            if hint_block:\n                msg += \'\\n\' + hint_block\n            synthetic_results.append({\n                \'op\': \'PARSE\',\n                \'target\': \'<bundle>\',\n                \'status\': \'FAILED_PARSE\',\n                \'message\': msg,\n                \'preview\': \'\',\n            })\n\n    elif bundle_text and bundle_text.strip() and not parsed.ops:\n        synthetic_results.append({\n            \'op\': \'PARSE\',\n            \'target\': \'<bundle>\',\n            \'status\': \'FAILED_PARSE\',\n            \'message\': \'No ops parsed from non-empty bundle\',\n            \'preview\': bundle_text[:800],\n        })\n\n    if synthetic_results:\n        packet = format_run_packet(synthetic_results, step=step, max_steps=max_steps)\n        return {\n            \'parsed\': parsed,\n            \'context\': None,\n            \'packet\': packet,\n        }\n\n    stamp = now_stamp()\n    ctx = execute_ops(parsed.ops, project_root, default_file=parsed.default_file)\n\n    try:\n        write_run_artifacts(project_root, stamp, bundle_text, ctx.results, ctx.touched_files)\n        prune_runs(project_root)\n    except Exception:\n        pass\n\n    packet = format_run_packet(ctx.results, stamp=stamp, step=step, max_steps=max_steps)\n    return {\n        \'parsed\': parsed,\n        \'context\': ctx,\n        \'packet\': packet or None,\n        \'stamp\': stamp,\n    }\n',
    'forge2/bundle_models.py': '# -*- coding: utf-8 -*-\n"""\nbundle_models.py\n================\nNormalized structures for parser output.\n\nSimple and boring on purpose.\n"""\n\ntry:\n    from dataclasses import dataclass, field\nexcept Exception:\n    dataclass = None\n    field = None\n\n\nif dataclass is not None:\n\n    @dataclass\n    class ParsedOp(object):\n        op: str\n        target: str\n        directives: dict\n        body: str = \'\'\n        default_file: str = None\n\n    @dataclass\n    class ParseResult(object):\n        ops: list\n        default_file: str = None\n        errors: list = field(default_factory=list)\n\nelse:\n\n    class ParsedOp(object):\n        def __init__(self, op, target, directives=None, body=\'\', default_file=None):\n            self.op = op\n            self.target = target\n            self.directives = directives or {}\n            self.body = body\n            self.default_file = default_file\n\n    class ParseResult(object):\n        def __init__(self, ops=None, default_file=None, errors=None):\n            self.ops = ops or []\n            self.default_file = default_file\n            self.errors = errors or []\n',
    'forge2/config.py': '# -*- coding: utf-8 -*-\n"""\nforge2.config\n=============\nConstants for forge2 runtime.\n"""\n\nRUNS_DIRNAME = \'forge2/forge2_runs\'\n\nKEEP_RUNS = 20\n',
    'forge2/directive_parsers.py': '# -*- coding: utf-8 -*-\n"""\ndirective_parsers.py\n====================\nHelpers for strict directive parsing.\n"""\n\ndef parse_key_value_line(line):\n    s = (line or \'\').strip()\n    if \':\' not in s:\n        return None, None\n    key, value = s.split(\':\', 1)\n    return key.strip(), value.strip()\n\n\ndef parse_lines_value(value):\n    raw = (value or \'\').strip()\n    if not raw:\n        return None\n    if \'-\' in raw:\n        a, b = raw.split(\'-\', 1)\n        try:\n            start = int(a.strip())\n            end = int(b.strip())\n            return (start, end)\n        except Exception:\n            return None\n    try:\n        n = int(raw)\n        return (n, n)\n    except Exception:\n        return None\n',
    'forge2/engine.py': '# -*- coding: utf-8 -*-\n"""\nengine.py\n=========\nExecution engine for forge2.\n"""\n\nfrom forge2.engine_context import EngineContext\nfrom forge2.registry import get_op_module\nfrom forge2.result_models import make_result\n\n\ndef execute_ops(parsed_ops, project_root, default_file=None):\n    ctx = EngineContext(project_root, default_file=default_file)\n\n    for op in parsed_ops:\n        result = make_result(op.op, op.target)\n        mod = get_op_module(op.op)\n\n        if mod is None:\n            result[\'status\'] = \'FAILED_PARSE\'\n            result[\'message\'] = \'Unknown op: \' + str(op.op)\n            ctx.results.append(result)\n            continue\n\n        validate = getattr(mod, \'validate\', None)\n        if validate is not None:\n            errors = validate(op)\n            if errors:\n                result[\'status\'] = \'FAILED_PARSE\'\n                msg = \'; \'.join(errors)\n                hint_block = _get_hints(mod, errors)\n                if hint_block:\n                    msg += \'\\n\' + hint_block\n                msg += \'\\nSee: HELP \' + str(op.op)\n                result[\'message\'] = msg\n                ctx.results.append(result)\n                continue\n\n        execute = getattr(mod, \'execute\', None)\n        if execute is None:\n            result[\'status\'] = \'FAILED_PARSE\'\n            result[\'message\'] = \'No execute() for op: \' + str(op.op)\n            ctx.results.append(result)\n            continue\n\n        try:\n            execute(ctx, op, result)\n        except Exception as e:\n            result[\'status\'] = \'FAILED_PARSE\'\n            msg = type(e).__name__ + \': \' + str(e)\n            hint_block = _get_hints(mod, [msg])\n            if hint_block:\n                msg += \'\\n\' + hint_block\n            msg += \'\\nSee: HELP \' + str(op.op)\n            result[\'message\'] = msg\n\n        ctx.results.append(result)\n\n    return ctx\n\ndef _get_hints(mod, errors):\n    hints_dict = getattr(mod, \'HINTS\', {})\n    if not hints_dict:\n        return \'\'\n    found = []\n    for error in errors:\n        for key, hint in hints_dict.items():\n            if key.lower() in error.lower() and hint not in found:\n                found.append(\'Hint: \' + hint)\n    return \'\\n\'.join(found)\n',
    'forge2/engine_context.py': '# -*- coding: utf-8 -*-\n"""\nengine_context.py\n=================\nShared execution context for engine-run ops.\n"""\n\nimport os\n\n\nclass EngineContext(object):\n    def __init__(self, project_root, default_file=None):\n        self.project_root = os.path.abspath(project_root)\n        self.default_file = default_file\n        self.file_cache = {}\n        self.touched_files = {}\n        self.results = []\n\n    def resolve_file(self, target):\n        target = (target or \'\').strip()\n        if os.path.isabs(target):\n            return os.path.abspath(target)\n        return os.path.abspath(os.path.join(self.project_root, target))\n\n    def in_root(self, file_abs):\n        root = os.path.realpath(self.project_root)\n        path = os.path.realpath(file_abs)\n        return path.startswith(root)\n',
    'forge2/entry.py': '# -*- coding: utf-8 -*-\n"""\nforge2.entry\n============\nLocal forge2 entry helpers.\n"""\n\nimport os\n\nfrom forge2.bridge import run_bundle\n\n\ndef run_from_text(bundle_text, project_root=None):\n    if project_root is None:\n        project_root = os.path.expanduser(\'~/Documents\')\n    return run_bundle(bundle_text, project_root)\n',
    'forge2/file_ops.py': '# -*- coding: utf-8 -*-\n"""\nfile_ops.py\n===========\nShared filesystem helpers for parser-lab file management ops.\n"""\n\nimport os\n\n\ndef resolve_under_root(project_root, target):\n    target = (target or \'\').strip()\n    if os.path.isabs(target):\n        path = os.path.abspath(target)\n    else:\n        path = os.path.abspath(os.path.join(project_root, target))\n    return path\n\n\ndef in_root(project_root, path):\n    root = os.path.realpath(project_root)\n    real = os.path.realpath(path)\n    return real.startswith(root)\n\n\ndef ensure_parent_dir(path):\n    parent = os.path.dirname(path)\n    if parent and not os.path.isdir(parent):\n        os.makedirs(parent)\n\n\ndef rel_from_root(project_root, path):\n    try:\n        return os.path.relpath(path, project_root)\n    except Exception:\n        return path\n',
    'forge2/forge_app.py': '# -*- coding: utf-8 -*-\n"""\nforge_app.py - ForgeApp entry point.\nRun this file in Pythonista to launch the app.\n"""\n\nimport os\nimport sys\nimport ui\nimport clipboard\n\nDOCUMENTS_ROOT = os.path.expanduser(\'~/Documents\')\n\n_lab_dir = os.path.join(DOCUMENTS_ROOT, \'forge2/forge_app\')\nif _lab_dir not in sys.path:\n    sys.path.insert(0, _lab_dir)\nif DOCUMENTS_ROOT not in sys.path:\n    sys.path.insert(0, DOCUMENTS_ROOT)\n\nfrom llm_workspace.forge_keyboard.ui_theme import (\n    BG, SURFACE, TEAL, TEXT2, STATUS_H,\n)\nfrom forge_app_state import AppState\nfrom forge_app_widgets import TabBar, StatusStrip\nfrom forge_app_compose import ComposePanel\nfrom forge_app_editor import EditorPanel\nfrom forge_app_runs import RunsPanel\nfrom forge_app_status import StatusPanel\nfrom forge_app_agent import AgentPanel\nfrom forge_app_favourites import FavoritesPanel\n\n\nclass ForgeApp(ui.View):\n    TAB_NAMES = [\'Compose\', \'Editor\', \'Runs\', \'Status\', \'Agent\']\n\n    def __init__(self):\n        super().__init__()\n        self.name = \'Forge App\'\n        self.background_color = BG\n        self.state = AppState()\n        self._selected_tab = 0\n        self._build()\n\n    def _build(self):\n        self.tab_bar = TabBar(self.TAB_NAMES, self._select_tab)\n        self.add_subview(self.tab_bar)\n\n        self.status_strip = StatusStrip()\n        self.add_subview(self.status_strip)\n\n        self.compose_panel = ComposePanel(self)\n        self.editor_panel = EditorPanel(self)\n        self.runs_panel = RunsPanel(self)\n        self.status_panel = StatusPanel(self)\n        self.agent_panel = AgentPanel(self)\n\n        self.panels = [\n            self.compose_panel,\n            self.editor_panel,\n            self.runs_panel,\n            self.status_panel,\n            self.agent_panel,\n        ]\n        for panel in self.panels:\n            self.add_subview(panel)\n\n        self.favorites_panel = FavoritesPanel(self)\n        self.add_subview(self.favorites_panel)\n\n        self._select_tab(0)\n        self.set_status(\'Forge App ready\', TEAL)\n\n    def set_status(self, text, color=None):\n        self.status_strip.set_status(text, color or TEXT2)\n\n    def _select_tab(self, index):\n        self._selected_tab = index\n        for i, panel in enumerate(self.panels):\n            panel.hidden = (i != index)\n        if index == 1:\n            self._maybe_load_editor_from_clipboard()\n        if index == 3:\n            self.status_panel.refresh()\n        if index == 4:\n            self.agent_panel.on_tab_focus()\n\n    def select_tab_by_name(self, name):\n        if name in self.TAB_NAMES:\n            idx = self.TAB_NAMES.index(name)\n            self.tab_bar.selected = idx\n            for btn in self.tab_bar._buttons:\n                btn.tint_color = __import__(\'llm_workspace.forge_keyboard.ui_theme\',\n                    fromlist=[\'TEXT\']).TEXT if btn.tag == idx else TEXT2\n            self.tab_bar.set_needs_display()\n            self._select_tab(idx)\n\n    def _maybe_load_editor_from_clipboard(self):\n        try:\n            clip = clipboard.get() or \'\'\n            current = self.editor_panel.get_text()\n            if clip.strip() and clip != current:\n                self.editor_panel.set_text(clip)\n                self.set_status(\'Editor loaded from clipboard\', TEAL)\n        except Exception:\n            pass\n\n    def layout(self):\n        w, h = self.width, self.height\n        tab_h = 42\n        status_h = STATUS_H\n        self.tab_bar.frame = (0, 0, w, tab_h)\n        self.status_strip.frame = (0, h - status_h, w, status_h)\n        content_y = tab_h\n        content_h = h - tab_h - status_h\n        for panel in self.panels:\n            panel.frame = (0, content_y, w, content_h)\n        self.favorites_panel.frame = (0, content_y, w, content_h)\n\n\ndef main():\n    app = ForgeApp()\n    try:\n        app.present(\'panel\', hide_title_bar=True)\n    except TypeError:\n        app.present(\'sheet\')\n\n\nif __name__ == \'__main__\':\n    main()\n',
    'forge2/forge_ui.py': '# -*- coding: utf-8 -*-\n"""\nforge_ui.py\n===========\nUI primitives for Pythonista scripts. Async, non-blocking.\nPresent and return immediately — results delivered via callbacks.\n\nAPI\n---\nhud(message, duration=1.5)\n    Brief toast. Fire and forget.\n\nshow(text, title=\'Output\')\n    Scrollable text view. No callback needed.\n\npick(items, title=\'Pick\', on_select=None)\n    Scrollable list of buttons. on_select(item_str) called on tap.\n\nform(fields, title=\'\', on_submit=None)\n    Form with text fields. on_submit(dict) called on submit.\n    Each field: {\'label\': str, \'type\': \'text\'|\'pick\', \'placeholder\': str, \'options\': [...]}\n    Returns None if dismissed without submitting.\n"""\n\nimport ui\nimport console\n\n# ── Colour palette (insert_text inspired) ─────────────────────────────────────\nBG      = \'#1C1C1E\'\nCARD    = \'#2C2C2E\'\nBORDER  = \'#3A3A3C\'\nTEXT    = \'#FFFFFF\'\nTEXT2   = \'#8E8E93\'\nACCENT  = \'#FFD60A\'\nBLUE    = \'#0A84FF\'\nGREEN   = \'#34C759\'\nRED     = \'#FF453A\'\nPURPLE  = \'#9F7AEA\'\n\n# ── Layout constants ───────────────────────────────────────────────────────────\nFONT        = \'Helvetica\'\nPAD         = 12\nRADIUS      = 12\nROW_H       = 44\nTOOLBAR_H   = 50\nBTN_H       = 36\n\n\n# ── Internal helpers ───────────────────────────────────────────────────────────\n\ndef _label(text, x, y, w, h, color=TEXT2, size=14, bold=False):\n    lbl = ui.Label(frame=(x, y, w, h))\n    lbl.text = text\n    lbl.font = (FONT + \'-Bold\' if bold else FONT, size)\n    lbl.text_color = color\n    lbl.background_color = \'clear\'\n    return lbl\n\n\ndef _button(title, x, y, w, h, bg=BLUE, fg=TEXT, action=None, radius=RADIUS):\n    btn = ui.Button(frame=(x, y, w, h))\n    btn.title = title\n    btn.font = (FONT, 15)\n    btn.background_color = bg\n    btn.tint_color = fg\n    btn.corner_radius = radius\n    if action:\n        btn.action = action\n    return btn\n\n\ndef _base(title, height=500):\n    v = ui.View()\n    v.name = str(title)\n    v.background_color = BG\n    v.width = 540\n    v.height = height\n    return v\n\n\n# ── Public API ─────────────────────────────────────────────────────────────────\n\ndef hud(message, duration=1.5):\n    """Brief toast notification."""\n    console.hud_alert(str(message), duration=duration)\n\n\ndef show(text, title=\'Output\'):\n    """\n    Display text in a scrollable dark view.\n    Presents as sheet. Closes via Done button.\n    """\n    v = _base(title, height=600)\n\n    # Title bar\n    v.add_subview(_label(title, PAD, PAD, v.width - PAD * 2, 24,\n                         color=TEXT, size=16, bold=True))\n\n    done = _button(\'Done\', v.width - 90 - PAD, PAD - 2, 90, 30,\n                   bg=RED, fg=TEXT)\n    done.action = lambda s: v.close()\n    v.add_subview(done)\n\n    # Text view\n    tv = ui.TextView(frame=(PAD, 50, v.width - PAD * 2, v.height - 60))\n    tv.flex = \'WH\'\n    tv.text = str(text)\n    tv.font = (\'Menlo\', 13)\n    tv.text_color = TEXT\n    tv.background_color = CARD\n    tv.editable = False\n    tv.corner_radius = RADIUS\n    v.add_subview(tv)\n\n    v.present(\'sheet\', hide_title_bar=True)\n\n\ndef pick(items, title=\'Pick\', on_select=None):\n    """\n    Scrollable list of tappable items.\n    on_select(item) called when tapped. View closes automatically.\n    items: list of strings.\n    """\n    height = min(TOOLBAR_H + len(items) * (ROW_H + PAD) + PAD * 2, 700)\n    v = _base(title, height=height)\n\n    # Toolbar\n    toolbar = ui.View(frame=(0, 0, v.width, TOOLBAR_H))\n    toolbar.background_color = CARD\n    v.add_subview(toolbar)\n    toolbar.add_subview(_label(title, PAD, 0, v.width - 100, TOOLBAR_H,\n                               color=TEXT, size=16, bold=True))\n    done = _button(\'Done\', v.width - 90 - PAD, (TOOLBAR_H - BTN_H) // 2,\n                   90, BTN_H, bg=RED)\n    done.action = lambda s: v.close()\n    toolbar.add_subview(done)\n\n    # Scroll\n    scroll = ui.ScrollView(frame=(0, TOOLBAR_H, v.width, v.height - TOOLBAR_H))\n    scroll.background_color = BG\n    v.add_subview(scroll)\n\n    y = PAD\n    for item in items:\n        def _make_action(it):\n            def _tap(s):\n                v.close()\n                if on_select:\n                    on_select(it)\n            return _tap\n\n        row = ui.View(frame=(PAD, y, v.width - PAD * 2, ROW_H))\n        row.background_color = CARD\n        row.corner_radius = RADIUS\n\n        btn = ui.Button(frame=(0, 0, row.width, ROW_H))\n        btn.title = str(item)\n        btn.font = (FONT, 15)\n        btn.tint_color = TEXT\n        btn.background_color = \'clear\'\n        btn.alignment = ui.ALIGN_LEFT\n        btn.action = _make_action(item)\n        row.add_subview(btn)\n        scroll.add_subview(row)\n        y += ROW_H + PAD\n\n    scroll.content_size = (v.width, y + PAD)\n    v.present(\'sheet\', hide_title_bar=True)\n\n\ndef form(fields, title=\'\', on_submit=None):\n    """\n    Form with labelled text fields (and optional pick segments).\n    on_submit(result_dict) called on submit.\n    on_submit(None) if view dismissed without submitting.\n\n    Field format:\n        {\'label\': str, \'type\': \'text\', \'placeholder\': str}\n        {\'label\': str, \'type\': \'pick\', \'options\': [str, ...]}\n    """\n    height = min(TOOLBAR_H + len(fields) * (ROW_H + PAD + 24) + 80, 700)\n    v = _base(title or \'Form\', height=height)\n\n    # Toolbar — buttons anchored left so they\'re always visible\n    toolbar = ui.View(frame=(0, 0, v.width, TOOLBAR_H))\n    toolbar.background_color = CARD\n    toolbar.flex = \'W\'\n    v.add_subview(toolbar)\n\n    cancel = _button(\'✕ Cancel\', PAD, (TOOLBAR_H - BTN_H) // 2,\n                     100, BTN_H, bg=RED)\n    cancel.action = lambda s: v.close()\n    toolbar.add_subview(cancel)\n\n    submit_btn = _button(\'✓ Submit\', PAD + 100 + 8, (TOOLBAR_H - BTN_H) // 2,\n                         100, BTN_H, bg=GREEN)\n    toolbar.add_subview(submit_btn)\n\n    title_lbl = _label(title or \'Form\', PAD + 216, 0, v.width - PAD - 216, TOOLBAR_H,\n                       color=TEXT2, size=14)\n    toolbar.add_subview(title_lbl)\n\n    # Scroll body\n    scroll = ui.ScrollView(frame=(0, TOOLBAR_H, v.width, v.height - TOOLBAR_H))\n    scroll.background_color = BG\n    scroll.flex = \'WH\'\n    v.add_subview(scroll)\n\n    y = PAD\n    field_views = {}\n\n    for field in fields:\n        label = field[\'label\']\n\n        lbl = _label(label, PAD, y, v.width - PAD * 2, 22)\n        scroll.add_subview(lbl)\n        y += 24\n\n        if field.get(\'type\') == \'pick\':\n            seg = ui.SegmentedControl(\n                frame=(PAD, y, v.width - PAD * 2, 32))\n            seg.segments = field.get(\'options\', [])\n            seg.selected_index = 0\n            seg.tint_color = BLUE\n            seg.flex = \'W\'\n            scroll.add_subview(seg)\n            field_views[label] = (\'pick\', seg, field.get(\'options\', []))\n            y += 40\n\n        else:\n            tf = ui.TextField(frame=(PAD, y, v.width - PAD * 2, ROW_H))\n            tf.placeholder = field.get(\'placeholder\', \'\')\n            tf.font = (FONT, 15)\n            tf.text_color = TEXT\n            tf.background_color = CARD\n            tf.bordered = False\n            tf.corner_radius = RADIUS\n            tf.flex = \'W\'\n            scroll.add_subview(tf)\n            field_views[label] = (\'text\', tf, None)\n            y += ROW_H\n\n        y += PAD\n\n    scroll.content_size = (v.width, y + 300)\n\n    def _submit(sender):\n        out = {}\n        for label, (ftype, widget, options) in field_views.items():\n            if ftype == \'pick\':\n                idx = widget.selected_index\n                out[label] = options[idx] if idx >= 0 else None\n            else:\n                out[label] = widget.text\n        v.close()\n        if on_submit:\n            on_submit(out)\n\n    submit_btn.action = _submit\n    v.present(\'sheet\', hide_title_bar=True)\n',
    'forge2/lexer.py': '# -*- coding: utf-8 -*-\n"""\nlexer.py\n========\nFirst-pass structural scanning for Forge bundles.\n\nKeep this tiny and strict.\n"""\n\n\ndef is_known_op_header(line):\n    s = (line or \'\').strip()\n    try:\n        from forge2.registry import OP_SPECS\n        op_names = OP_SPECS.keys()\n    except Exception:\n        return False\n    for op_name in op_names:\n        if s.startswith(op_name + \' \') or s == op_name:\n            return True\n    return False\n\n\ndef is_phase1_op_header(line):\n    # Backward-compatible alias while parser_main still uses this name.\n    return is_known_op_header(line)\n\n\ndef is_exact_begin_body(line):\n    return line == \'BEGIN_BODY\'\n\n\ndef is_exact_end_body(line):\n    return line == \'END_BODY\'\n',
    'forge2/onboarding.md': '# Forge2 — New User Onboarding\n==============================\n\n## Your Role\nYou are an LLM teacher helping a new user learn Forge2 — a clipboard-driven\nAST patcher for Pythonista on iOS. Your job is to teach by doing: explain what\nyou\'re about to run, issue a bundle, wait for the run packet, then explain what\nhappened. Let the user set the pace. If they want to go off-script and build\nsomething of their own, drop the teaching sequence and use Forge to help them\ndo it.\n\n---\n\n## How Forge Works\n\nThe user runs everything on iPhone in Pythonista. The loop is:\n\n1. You write a **bundle** — plain text ops in a specific format\n2. User copies it to clipboard and fires an iOS Shortcut or just runs forge2_shortcut.py directly works well. \n3. Forge runs the ops and returns a **run packet** which is added directly to the clipboard\n4. User pastes the run packet back to you — this is ground truth\n\nYou never see the filesystem directly. The run packet tells you what happened.\nTrust it completely.\n\nyou need to make sure the user knows all the different ways they can run bundles. idealy a shortcut pointing at forge2_shorcut.py but also running it driectly \n\n---\n\n## Bundle Format\n\n\n\nOP_NAME path/to/file.py::Target\nDIRECTIVE: value\nbody content\n\n\n- One bundle can contain multiple ops — they run in order\n- `DEFAULT_FILE path/to/file.py` sets a default so you can omit the path per op\n- `BEGIN_BODY` / `END_BODY` delimiters are always safe to use around body content\n\n---\n\n## First Bundle — Run This Now\n\nThis orients both of you. Run it, paste the packet back, and we\'ll go from there.\n\n\n\nLIST_FILES .\nDEPTH: 2\nFILES: no\nLIST_OPS\n\n\nOnce you have the packet, explain what `LIST_FILES` and `LIST_OPS` showed — the\nproject tree and every available op. This is the user\'s map of the environment.\n\n---\n\n## Teaching Sequence\n\nWork through these steps in order unless the user wants to go a different\ndirection. Each step: explain what you\'re going to do, issue the bundle, wait\nfor the packet, explain what happened.\n\n### Step 1 — HELP (Forge knows itself)\n\nThe most important habit. Before using any op you\'re unsure about, ask Forge.\n\n\n\nHELP CREATE_FILE\n\n\nExplain: Forge has built-in documentation for every op. The user can run\n`HELP <OP>` themselves any time. They never need to guess directives.\n\n---\n\n### Step 2 — CREATE_FILE (make something)\n\n\n\nCREATE_FILE calculator.py\ndef add(a, b):\nreturn a + b\ndef subtract(a, b):\nreturn a - b\ndef multiply(a, b):\nreturn a * b\ndef divide(a, b):\nif b == 0:\nraise ValueError(“Cannot divide by zero”)\nreturn a / b\nif name == ‘main’:\nprint(add(10, 5))\nprint(subtract(10, 5))\nprint(multiply(10, 5))\nprint(divide(10, 5))\n\n\nExplain: Forge wrote a file to disk. No editor needed.\n\n---\n\n### Step 3 — RUN_FILE (execute it)\n\n\n\nRUN_FILE calculator.py\n\n\nExplain: Forge can run any script and returns stdout/stderr. You can use this\nto verify changes work before moving on.\n\n---\n\n### Step 4 — LIST_TARGETS and PREVIEW (inspect before touching)\n\n\n\nLIST_TARGETS calculator.py\nPREVIEW calculator.py::divide\n\n\nExplain: `LIST_TARGETS` shows every patchable target in a file with line ranges.\n`PREVIEW` shows numbered source lines for a specific target. Always do this\nbefore patching — you need to see exactly what you\'re working with.\n\n---\n\n### Step 5 — REPLACE (patch a function)\n\nthe target being pagched must not be open in tbe editor for memory chahe reasons. this can cause a lot of issues so make sure the user knows this before patching code. better to close it then make the changes.\n\nNow improve `divide` to return a friendly string instead of raising:\n\n\n\nREPLACE calculator.py::divide\ndef divide(a, b):\nif b == 0:\nreturn “Error: division by zero”\nreturn a / b\n\n\nExplain: `REPLACE` swaps out an entire function or method. It\'s the safest\npatching op — no anchors, no fragile line matching. When in doubt, use REPLACE.\n\nRun it again to verify:\n\n\n\nRUN_FILE calculator.py\n\n\n---\n\n### Step 6 — INSERT_AFTER (add something new)\n\n\n\nINSERT_AFTER calculator.py::divide\ndef power(a, b):\nreturn a ** b\n\n\nExplain: `INSERT_AFTER` adds a new function right after the target. It skips\nautomatically if the function already exists — safe to run twice.\n\n---\n\n### Step 7 — COPY_FILE and DELETE_FILE (housekeeping)\n\n\n\nCOPY_FILE calculator.py\nDEST: calculator_backup.py\nDELETE_FILE calculator_backup.py\nCONFIRM: yes\n\n\nExplain: Forge handles file management too. `DELETE_FILE` requires `CONFIRM: yes`\nas a safety check.\n\n---\n\n## Where to Go From Here\n\nBy now the user has seen the core loop: create, run, inspect, patch, manage.\nThat covers 90% of real sessions.\n\n**Suggested next step:** offer to build the calculator out properly together —\nadd a simple `ui` interface so it runs as an iPhone app, not just a script.\nThis introduces the same workflow on a slightly larger project and gets them\ncomfortable before they bring their own code.\n\nSoft pitch: *"Want to keep going with the calculator? We could add a simple UI\nso it runs as a proper iPhone app — same workflow, just a bigger target. Or if\nyou\'ve got your own project in mind, let\'s do that instead."*\n\nIf they bring their own project:\n- Run `LIST_FILES .` to orient\n- `LIST_TARGETS` on any file before touching it\n- `HELP <OP>` before any op with unfamiliar directives\n- Trust the run packet — if something failed, diagnose before retrying\n\n---\n\n## Op Reference Map\n\nUse `HELP <OP>` for full detail. This is a map only.\n\n**Explore (zero risk):**\nGREP, LIST_FILES, LIST_TARGETS, PREVIEW, PREVIEW_FILE, READ_FILE, RUN_FILE\n\n**AST write (Python targets):**\nREPLACE, REPLACE_LINES, REPLACE_LINE, REPLACE_EXPR,\nINSERT_AFTER, INSERT_BEFORE, INSERT_INTO,\nAPPEND_INTO, PREPEND_INTO\n\n**File write (any file):**\nCREATE_FILE, REPLACE_FILE, REPLACE_FILE_RANGE, REPLACE_FILE_LINES\n\n**File manage:**\nCOPY_FILE, MOVE_FILE, DELETE_FILE, DELETE_DIR\n\n**Run/automation:**\nLIST_RUNS, REVERT_RUN, DIFF, SHORTCUT, CLIPBOARD\n\n**Always available:**\nHELP, LIST_OPS\n\n---\n\n## Standing Rules\n\n- `HELP <OP>` before any op you\'re unsure about. Always after `FAILED_PARSE`.\n- `LIST_TARGETS` + `PREVIEW` before patching any file.\n- Trust the run packet as ground truth.\n- If an op fails, diagnose before retrying — don\'t just reissue the same bundle.\n- Relative paths only.\n- `BEGIN_BODY` / `END_BODY` delimiters are always safe.\n\n\nget the user to try the bootstrap.md afterwards for a more advanced boot with less tutorial, you could find it for them usinf grep as another tutorial. run HELP GREP and then find the bootstrap.md document \n\n\n\n',
    'forge2/op_help.py': '# -*- coding: utf-8 -*-\n"""\nop_help.py\n==========\nHelpers for LIST_OPS and HELP.\n"""\n\nfrom forge2.registry import OP_MODULES, OP_SPECS\n\n\nCATEGORY_LABELS = {\n    \'inspect\': \'Inspect\',\n    \'ast_write\': \'AST write\',\n    \'file_write\': \'File write\',\n    \'file_manage\': \'File manage\',\n    \'run\': \'Run / automation\',\n    \'meta\': \'Meta\',\n    \'other\': \'Other\',\n}\n\n\ndef _clean_doc_line(text):\n    for line in (text or \'\').splitlines():\n        s = line.strip()\n        if not s:\n            continue\n        if set(s) == set(\'=\'):\n            continue\n        if s.startswith(\'ops.\'):\n            return \'\'\n        return s.rstrip(\'.\')\n    return \'\'\n\n\ndef _module_by_name():\n    out = {}\n    for mod in OP_MODULES:\n        spec = getattr(mod, \'SPEC\', None) or {}\n        name = spec.get(\'name\')\n        if name:\n            out[name] = mod\n    return out\n\n\ndef _dedupe_keep_order(items):\n    seen = set()\n    out = []\n    for item in items:\n        s = str(item or \'\').strip()\n        if not s:\n            continue\n        key = s.lower()\n        if key in seen:\n            continue\n        seen.add(key)\n        out.append(s)\n    return out\n\n\ndef _infer_category(op_name, spec):\n    target_kind = spec.get(\'target_kind\')\n    body_mode = spec.get(\'body_mode\', \'forbidden\')\n\n    if op_name in (\'LIST_OPS\', \'HELP\'):\n        return \'meta\'\n    if op_name in (\'RUN_FILE\', \'SHORTCUT\', \'CLIPBOARD\', \'SUMMARISE\'):\n        return \'run\'\n    if op_name in (\'LIST_RUNS\', \'REVERT_RUN\', \'DIFF\'):\n        return \'run\'\n    if op_name == \'LIST_TARGETS\':\n        return \'inspect\'\n    if target_kind == \'ast\':\n        return \'ast_write\' if body_mode == \'required\' else \'inspect\'\n    if target_kind == \'file\':\n        if op_name.startswith(\'READ\') or op_name.startswith(\'PREVIEW\') or op_name in (\'LIST_FILES\', \'GREP\'):\n            return \'inspect\'\n        if body_mode == \'required\':\n            return \'file_write\'\n        if op_name.startswith(\'DELETE\') or op_name.startswith(\'MOVE\') or op_name.startswith(\'COPY\') or op_name.startswith(\'CREATE\'):\n            return \'file_manage\'\n        return \'file_manage\'\n    return \'other\'\n\n\ndef _generic_subject(op_name, spec):\n    if op_name == \'LIST_OPS\':\n        return [\'No target.\']\n    if op_name == \'HELP\':\n        return [\'Registered op name, for example READ_FILE.\']\n\n    target_kind = spec.get(\'target_kind\')\n    if target_kind == \'file\':\n        return [\'Relative file or directory path inside project root.\']\n    if target_kind == \'ast\':\n        return [\n            \'AST target string, for example file.py::Class.method_name.\',\n            \'Use @name for assignments and Class.* for a whole class.\',\n        ]\n    return [\'See op-specific usage.\']\n\n\ndef _generic_summary(op_name, spec, mod):\n    help_meta = getattr(mod, \'HELP\', None) or {}\n    if spec.get(\'summary\'):\n        return spec[\'summary\']\n\n    if help_meta.get(\'summary\'):\n        return help_meta[\'summary\']\n    doc_line = _clean_doc_line(getattr(mod, \'__doc__\', \'\') or \'\')\n    if doc_line:\n        return doc_line\n    category = _infer_category(op_name, spec)\n    if category == \'inspect\':\n        return \'Inspect project state.\'\n    if category == \'ast_write\':\n        return \'Modify a Python AST target.\'\n    if category == \'file_write\':\n        return \'Modify a text file.\'\n    if category == \'file_manage\':\n        return \'Manage files or directories.\'\n    if category == \'run\':\n        return \'Run or automate a task.\'\n    if category == \'meta\':\n        return \'Describe forge2 capabilities.\'\n    return \'Forge2 op.\'\n\n\ndef _directive_example(key):\n    if key == \'LINES\':\n        return \'LINES: 12-23\'\n    if key == \'ANCHOR\':\n        return \'ANCHOR: substring to match\'\n    if key == \'ANCHOR_START\':\n        return \'ANCHOR_START: first marker\'\n    if key == \'ANCHOR_END\':\n        return \'ANCHOR_END: second marker\'\n    if key == \'DEST\':\n        return \'DEST: path/to/destination\'\n    if key == \'POSITION\':\n        return \'POSITION: after\'\n    if key == \'PATTERN\':\n        return \'PATTERN: search term\'\n    if key == \'MATCH\':\n        return \'MATCH: exact\'\n    if key == \'FILTER\':\n        return \'FILTER: .py\'\n    if key == \'CONTEXT\':\n        return \'CONTEXT: 3\'\n    if key == \'INDENT\':\n        return \'INDENT: auto\'\n    if key == \'OCCURRENCE\':\n        return \'OCCURRENCE: 1\'\n    if key == \'EXPECT\':\n        return \'EXPECT: 1\'\n    if key == \'ARGS\':\n        return \'ARGS: one two\'\n    if key == \'CONFIRM\':\n        return \'CONFIRM: yes\'\n    return key + \': \'\n\n\ndef _generic_minimal_example(op_name, spec):\n    if op_name == \'LIST_OPS\':\n        return [\'LIST_OPS\']\n    if op_name == \'HELP\':\n        return [\'HELP READ_FILE\']\n\n    target_kind = spec.get(\'target_kind\')\n    subject = \'\'\n    if target_kind == \'file\':\n        subject = \' path/to/file.txt\'\n    elif target_kind == \'ast\':\n        subject = \' file.py::MyClass.method_name\'\n\n    lines = [op_name + subject]\n\n    required = sorted(spec.get(\'required_directives\', set()))\n    for key in required:\n        lines.append(_directive_example(key))\n\n    body_mode = spec.get(\'body_mode\', \'forbidden\')\n    if body_mode == \'required\':\n        lines.extend([\n            \'BEGIN_BODY\',\n            \'# replacement content\',\n            \'END_BODY\',\n        ])\n    return lines\n\n\ndef _generic_common_failures(op_name, spec):\n    out = []\n    if spec.get(\'target_kind\') in (\'file\', \'ast\'):\n        out.append(\'Missing or invalid target.\')\n    for key in sorted(spec.get(\'required_directives\', set())):\n        out.append(\'Missing required directive: %s.\' % key)\n    if spec.get(\'body_mode\') == \'required\':\n        out.append(\'Missing BEGIN_BODY / END_BODY.\')\n    if spec.get(\'target_kind\') == \'file\':\n        out.append(\'Absolute or escaping paths are rejected.\')\n    if spec.get(\'target_kind\') == \'ast\':\n        out.append(\'Target must resolve to a real AST target.\')\n    return out\n\n\ndef _generic_safe_usage(op_name, spec):\n    out = []\n    category = _infer_category(op_name, spec)\n    if category in (\'ast_write\', \'file_write\'):\n        out.append(\'Inspect the target first before writing.\')\n        out.append(\'Verify the result after patching.\')\n    if spec.get(\'target_kind\') == \'file\':\n        out.append(\'Use relative paths only.\')\n    if spec.get(\'target_kind\') == \'ast\':\n        out.append(\'Prefer exact target syntax over guessing.\')\n    return out\n\n\ndef _display_body_mode(spec):\n    mode = spec.get(\'body_mode\', \'forbidden\')\n    if mode == \'none\':\n        return \'forbidden\'\n    return mode\n\n\ndef render_ops_list():\n    modules = _module_by_name()\n    grouped = {}\n    for op_name in sorted(OP_SPECS.keys()):\n        mod = modules.get(op_name)\n        spec = OP_SPECS.get(op_name, {})\n        category = _infer_category(op_name, spec)\n        grouped.setdefault(category, []).append((op_name, _generic_summary(op_name, spec, mod)))\n\n    lines = [\'=== FORGE2 OPS ===\', \'\']\n    for category in (\'inspect\', \'ast_write\', \'file_write\', \'file_manage\', \'run\', \'meta\', \'other\'):\n        items = grouped.get(category) or []\n        if not items:\n            continue\n        lines.append(CATEGORY_LABELS.get(category, category))\n        for op_name, summary in items:\n            lines.append(\'- %-18s %s\' % (op_name, summary))\n        lines.append(\'\')\n    return \'\\n\'.join(lines).rstrip() + \'\\n\'\n\n\ndef render_op_help(op_name):\n    name = (op_name or \'\').strip().upper()\n    modules = _module_by_name()\n    mod = modules.get(name)\n    spec = OP_SPECS.get(name)\n    if mod is None or spec is None:\n        return None\n\n    help_meta = getattr(mod, \'HELP\', None) or {}\n    summary = _generic_summary(name, spec, mod)\n    subject = help_meta.get(\'subject\') or _generic_subject(name, spec)\n    required = sorted(spec.get(\'required_directives\', set()))\n    optional = sorted([k for k in spec.get(\'allowed_directives\', set()) if k not in required])\n    body_mode = _display_body_mode(spec)\n    category = _infer_category(name, spec)\n\n    common_failures = _dedupe_keep_order(\n        list(help_meta.get(\'common_failures\') or []) + _generic_common_failures(name, spec)\n    )\n    safe_usage = _dedupe_keep_order(\n        list(help_meta.get(\'safe_usage\') or []) + _generic_safe_usage(name, spec)\n    )\n    minimal_example = help_meta.get(\'minimal_example\') or _generic_minimal_example(name, spec)\n    related_ops = _dedupe_keep_order(help_meta.get(\'related_ops\') or [])\n\n    lines = [\n        \'=== FORGE2 OP HELP ===\',\n        \'OP: \' + name,\n        \'KIND: \' + category,\n        \'SUMMARY: \' + summary,\n        \'\',\n        \'SUBJECT:\',\n    ]\n    for item in subject:\n        lines.append(\'- \' + item)\n\n    lines.append(\'\')\n    lines.append(\'REQUIRED DIRECTIVES:\')\n    if required:\n        for item in required:\n            lines.append(\'- \' + item)\n    else:\n        lines.append(\'- none\')\n\n    lines.append(\'\')\n    lines.append(\'OPTIONAL DIRECTIVES:\')\n    if optional:\n        for item in optional:\n            lines.append(\'- \' + item)\n    else:\n        lines.append(\'- none\')\n\n    lines.append(\'\')\n    lines.append(\'BODY:\')\n    lines.append(\'- \' + body_mode)\n\n    lines.append(\'\')\n    lines.append(\'COMMON FAILURES:\')\n    if common_failures:\n        for item in common_failures:\n            lines.append(\'- \' + item)\n    else:\n        lines.append(\'- none\')\n\n    lines.append(\'\')\n    lines.append(\'SAFE USAGE:\')\n    if safe_usage:\n        for item in safe_usage:\n            lines.append(\'- \' + item)\n    else:\n        lines.append(\'- none\')\n\n    if related_ops:\n        lines.append(\'\')\n        lines.append(\'RELATED OPS:\')\n        for item in related_ops:\n            lines.append(\'- \' + item)\n\n    lines.append(\'\')\n    lines.append(\'MINIMAL EXAMPLE:\')\n    for item in minimal_example:\n        lines.append(item)\n\n    return \'\\n\'.join(lines).rstrip() + \'\\n\'\n',
    'forge2/ops/__init__.py': '# forge2 ops package marker\n',
    'forge2/ops/append_into_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.append_into_ast\n===================\nAppend code at end of AST target body.\n"""\n\nSPEC = {\n    \'name\': \'APPEND_INTO\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Append code at the end of a resolved AST target body.\',\n    \'subject\': [\n        \'AST target string, for example file.py::Class.method_name.\',\n        \'Best for adding code near the end of a function, method, or class body.\',\n    ],\n    \'common_failures\': [\n        \'Target does not resolve to a real AST target.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n    ],\n    \'safe_usage\': [\n        \'Use PREVIEW first if you are not sure where the body currently ends.\',\n        \'Use this when a simple end-of-body insertion is cleaner than anchor matching.\',\n    ],\n    \'minimal_example\': [\n        \'APPEND_INTO forge_app_lab/forge_app_state.py::build_starter_bundle\',\n        \'BEGIN_BODY\',\n        "return \'# done\'",\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW\', \'PREPEND_INTO\', \'INSERT_INTO\'],\n}\n\nHINTS = {\n    \'body required\': \'Provide the code to append as the body\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'APPEND_INTO requires a target\')\n    if not parsed_op.body:\n        errors.append(\'APPEND_INTO requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n    from forge2.source_ops import insert_after_lines, get_line_indent\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    insert_line = resolved[\'end\'] - 1\n    ref_line = before.splitlines(True)[resolved[\'start\'] - 1]\n    indent = get_line_indent(ref_line) + \'    \'\n\n    patched = insert_after_lines(\n        before,\n        insert_line,\n        parsed_op.body,\n        indent,\n        tight=True,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': patched,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'appended into %s lines %d-%d\' % (\n        resolved[\'kind\'],\n        resolved[\'start\'],\n        resolved[\'end\'],\n    )\n',
    'forge2/ops/append_list.py': '# -*- coding: utf-8 -*-\n"""\nops.append_list\n===============\nAppend an item to a module-level list assignment.\n\nTarget: file.py::@var_name\nBody:   the item to append (a dict literal, string, etc.)\n"""\n\nSPEC = {\n    \'name\': \'APPEND_LIST\',\n    \'target_kind\': \'raw\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Append an item to a module-level list assignment.\',\n    \'subject\': [\n        \'file.py::@var_name — must point to a module-level list.\',\n    ],\n    \'common_failures\': [\n        \'Target is not a list assignment.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n        \'Body is not valid Python.\',\n    ],\n    \'safe_usage\': [\n        \'Use for appending to lists like GOTCHAS, MILESTONES in long_memory.py.\',\n        \'Body should be the item only, not the surrounding brackets.\',\n    ],\n    \'minimal_example\': [\n        \'APPEND_LIST docs/long_memory.py::@GOTCHAS\',\n        \'BEGIN_BODY\',\n        \'{\',\n        \'    "date": "2026-03-29",\',\n        \'    "title": "Example",\',\n        \'    "note": "A note.",\',\n        \'    "tags": ["example"],\',\n        \'}\',\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'SET_DICT_KEY\', \'PREVIEW_FILE\', \'REPLACE_FILE_LINES\'],\n}\n\nHINTS = {\n    \'target\': \'Provide file.py::@list_name — the @ prefix targets a module-level list assignment\',\n    \'body required\': \'Provide the item to append as the body\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'APPEND_LIST requires a target (file.py::@var_name)\')\n    if not parsed_op.body:\n        errors.append(\'APPEND_LIST requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    import ast, os\n\n    target = (parsed_op.target or \'\').strip()\n\n    # Split file path and var name\n    if \'::@\' not in target:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'Target must be file.py::@var_name\'\n        return\n\n    file_ref, var_name = target.split(\'::@\', 1)\n    file_ref  = (parsed_op.default_file or file_ref).strip() if not file_ref.strip() else file_ref.strip()\n    var_name  = var_name.strip()\n    file_abs  = os.path.normpath(os.path.join(ctx.project_root, file_ref))\n\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = f\'File not found: {file_ref}\'\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\') as f:\n        source = f.read()\n\n    # Parse and find the list assignment\n    try:\n        tree = ast.parse(source)\n    except SyntaxError as e:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = f\'File parse error: {e}\'\n        return\n\n    target_node = None\n    for node in ast.walk(tree):\n        if isinstance(node, ast.Assign):\n            for t in node.targets:\n                if isinstance(t, ast.Name) and t.id == var_name:\n                    if isinstance(node.value, ast.List):\n                        target_node = node\n    if target_node is None:\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = f\'No module-level list assignment @{var_name} found\'\n        return\n\n    # Find the closing ] by scanning from end_lineno backwards\n    lines = source.splitlines(True)\n    end_line_idx = target_node.end_lineno - 1  # 0-based\n\n    # Walk backwards from end to find the line with the closing ]\n    close_idx = end_line_idx\n    while close_idx >= 0:\n        stripped = lines[close_idx].rstrip()\n        if stripped.endswith(\']\') or stripped == \']\':\n            break\n        close_idx -= 1\n\n    if close_idx < 0:\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = f\'Could not locate closing ] for @{var_name}\'\n        return\n\n    # Determine indent from existing items or default 4 spaces\n    indent = \'    \'\n    for line in lines[target_node.lineno: close_idx]:\n        s = line.lstrip()\n        if s:\n            indent = line[: len(line) - len(s)]\n            break\n\n    # Build the new item lines, indented\n    item_lines = parsed_op.body.rstrip(\'\\n\').splitlines()\n    indented   = \'\\n\'.join(indent + l if l.strip() else l for l in item_lines)\n\n    # Insert before the closing ]\n    insert_text = indented + \',\\n\'\n    lines.insert(close_idx, insert_text)\n    patched = \'\'.join(lines)\n\n    # Compile check\n    try:\n        compile(patched, file_abs, \'exec\')\n    except SyntaxError as e:\n        result[\'status\'] = \'FAILED_COMPILE\'\n        result[\'message\'] = f\'Compile failed after patch: {e}\'\n        return\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\'before\': source, \'after\': patched}\n\n    result[\'file\']    = file_ref\n    result[\'status\']  = \'APPLIED\'\n    result[\'message\'] = f\'Appended item to @{var_name}\'\n',
    'forge2/ops/clipboard.py': '# -*- coding: utf-8 -*-\n"""\nops.clipboard\n=============\nCopy the raw contents of a file to the iOS clipboard.\nNo packet output - just the file content.\n\nUsage:\n    CLIPBOARD path/to/file.py\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'CLIPBOARD\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Copy the raw contents of a file to the iOS clipboard.\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'CLIPBOARD requires a file path\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.file_ops import resolve_under_root, in_root\n\n    src_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n\n    if not in_root(ctx.project_root, src_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Path escapes project root\'\n        return\n\n    if not os.path.isfile(src_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    try:\n        with open(src_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n            content = f.read()\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Read error: \' + str(e)\n        return\n\n    try:\n        import clipboard as _cb\n        _cb.set(content)\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Clipboard error: \' + str(e)\n        return\n\n    result[\'file\']    = parsed_op.target\n    result[\'status\']  = \'APPLIED\'\n    result[\'silent\']  = True\n    result[\'message\'] = \'Copied to clipboard: %s (%d chars)\' % (\n        parsed_op.target, len(content)\n    )\n',
    'forge2/ops/copy_file.py': '# -*- coding: utf-8 -*-\n"""\nops.copy_file\n=============\nCopy-file op.\n"""\n\nimport os\nimport shutil\n\nfrom forge2.file_ops import resolve_under_root, in_root, ensure_parent_dir\n\nSPEC = {\n    \'name\': \'COPY_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'DEST\']),\n    \'required_directives\': set([\'DEST\']),\n}\n\nHELP = {\n    \'summary\': \'Copy one file to a new destination under project root.\',\n    \'subject\': [\'Relative source file path inside project root.\'],\n    \'common_failures\': [\n        \'Missing DEST directive.\',\n        \'Source not found.\',\n        \'Destination already exists.\',\n        \'Source or destination escapes project root.\',\n    ],\n    \'safe_usage\': [\n        \'Use this when you want a backup or duplicate before riskier edits.\',\n        \'DEST should normally include the full new file path, not just a folder.\',\n    ],\n    \'minimal_example\': [\n        \'COPY_FILE docs/old.txt\',\n        \'DEST: docs/old_backup.txt\',\n    ],\n    \'related_ops\': [\'MOVE_FILE\', \'READ_FILE\', \'LIST_FILES\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'COPY_FILE requires a target path\')\n    if not (parsed_op.directives.get(\'DEST\') or \'\').strip():\n        errors.append(\'Missing required directive for COPY_FILE: DEST\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    src_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n    dst_abs = resolve_under_root(ctx.project_root, parsed_op.directives.get(\'DEST\'))\n\n    if not (in_root(ctx.project_root, src_abs) and in_root(ctx.project_root, dst_abs)):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Path escapes project root\'\n        return\n\n    if not os.path.isfile(src_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Source not found: \' + parsed_op.target\n        return\n\n    if os.path.exists(dst_abs):\n        result[\'status\'] = \'SKIPPED_ALREADY_PRESENT\'\n        result[\'message\'] = \'Destination already exists: \' + parsed_op.directives.get(\'DEST\')\n        return\n\n    ensure_parent_dir(dst_abs)\n    shutil.copy2(src_abs, dst_abs)\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%s -> %s\' % (parsed_op.target, parsed_op.directives.get(\'DEST\'))\n',
    'forge2/ops/create_file.py': '# -*- coding: utf-8 -*-\n"""\nops.create_file\n===============\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'CREATE_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Create a new file at a given path with the provided body content.\',\n}\n\nHINTS = {\n    \'body required\': \'Wrap file contents in BEGIN_BODY / END_BODY\',\n    \'target\': \'Provide a relative path for the new file — e.g. CREATE_FILE utils/newfile.py\',\n    \'already exists\': \'File already exists — use REPLACE_FILE to overwrite\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'CREATE_FILE requires a target path\')\n    if not parsed_op.body:\n        errors.append(\'CREATE_FILE requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if os.path.exists(file_abs):\n        result[\'status\'] = \'SKIPPED_ALREADY_PRESENT\'\n        result[\'message\'] = \'File already exists: \' + parsed_op.target\n        return\n\n    parent = os.path.dirname(file_abs)\n    if parent and not os.path.isdir(parent):\n        os.makedirs(parent)\n\n    body = parsed_op.body\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n\n        f.write(body)\n\n    ctx.file_cache[file_abs] = body\n    ctx.touched_files[file_abs] = {\n        \'before\': None,\n        \'after\': body,\n    }\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Created: \' + parsed_op.target\n',
    'forge2/ops/delete_dir.py': '# -*- coding: utf-8 -*-\n"""\nops.delete_dir\n==============\nDelete-directory op for quick cleanup.\n"""\n\nimport os\nimport shutil\n\nfrom forge2.file_ops import resolve_under_root, in_root\n\nSPEC = {\n    \'name\': \'DELETE_DIR\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'CONFIRM\']),\n    \'required_directives\': set([\'CONFIRM\']),\n    "summary": \'Permanently delete a directory and all its contents. Requires CONFIRM: yes.\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'DELETE_DIR requires a target path\')\n    if (parsed_op.directives.get(\'CONFIRM\') or \'\').lower() != \'yes\':\n        errors.append(\'DELETE_DIR requires CONFIRM: yes\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    dir_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n\n    if not in_root(ctx.project_root, dir_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isdir(dir_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Directory not found: \' + parsed_op.target\n        return\n\n    shutil.rmtree(dir_abs)\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Deleted dir: \' + parsed_op.target\n',
    'forge2/ops/delete_file.py': '# -*- coding: utf-8 -*-\n"""\nops.delete_file\n===============\nDelete-file op.\n"""\n\nimport os\n\nfrom forge2.file_ops import resolve_under_root, in_root\n\nSPEC = {\n    \'name\': \'DELETE_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'CONFIRM\']),\n    \'required_directives\': set([\'CONFIRM\']),\n}\n\nHELP = {\n    \'summary\': \'Delete a file under project root.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'Missing CONFIRM: yes.\',\n        \'File not found.\',\n        \'Target escapes project root.\',\n    ],\n    \'safe_usage\': [\n        \'Use this only when you intend to remove the file entirely.\',\n        \'Consider COPY_FILE first if you may want a backup.\',\n    ],\n    \'minimal_example\': [\n        \'DELETE_FILE docs/tmp.txt\',\n        \'CONFIRM: yes\',\n    ],\n    \'related_ops\': [\'COPY_FILE\', \'MOVE_FILE\', \'LIST_FILES\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'DELETE_FILE requires a target path\')\n    if (parsed_op.directives.get(\'CONFIRM\') or \'\').lower() != \'yes\':\n        errors.append(\'DELETE_FILE requires CONFIRM: yes\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n\n    if not in_root(ctx.project_root, file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    os.remove(file_abs)\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Deleted: \' + parsed_op.target\n',
    'forge2/ops/diff.py': '# -*- coding: utf-8 -*-\n"""\nops.diff\n========\nShow changed lines between before and after snapshots for a run.\n\nUsage:\n    DIFF 20260324_203520   - diff a specific run\n    DIFF                   - diff the most recent run\n"""\n\nimport os\nimport json\n\nSPEC = {\n    \'name\': \'DIFF\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'none\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Show changes between a run\\\'s file snapshots and current disk state.\',\n}\n\n\ndef validate(parsed_op):\n    return []\n\n\ndef _diff_lines(before, after, context=1):\n    before_lines = (before or \'\').splitlines()\n    after_lines = (after or \'\').splitlines()\n    max_len = max(len(before_lines), len(after_lines))\n    changed = []\n\n    for i in range(max_len):\n        b = before_lines[i] if i < len(before_lines) else None\n        a = after_lines[i] if i < len(after_lines) else None\n        if b != a:\n            changed.append(i)\n\n    if not changed:\n        return []\n\n    out = []\n    emitted = set()\n    for idx in changed:\n        start = max(0, idx - context)\n        end = min(max_len - 1, idx + context)\n        for i in range(start, end + 1):\n            if i in emitted:\n                continue\n            emitted.add(i)\n            b = before_lines[i] if i < len(before_lines) else None\n            a = after_lines[i] if i < len(after_lines) else None\n            lineno = i + 1\n            if b != a:\n                if b is not None:\n                    out.append(\'  line %d:  - %s\' % (lineno, b))\n                if a is not None:\n                    out.append(\'           + %s\' % a)\n            else:\n                out.append(\'  line %d:    %s\' % (lineno, b or \'\'))\n\n    return out\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.run_storage import runs_root, list_runs\n\n    stamp = (parsed_op.target or \'\').strip()\n\n    if not stamp:\n        runs = list_runs(ctx.project_root)\n        if not runs:\n            result[\'status\'] = \'APPLIED\'\n            result[\'message\'] = \'No runs found\'\n            result[\'preview\'] = \'DIFF [no runs]\'\n            return\n        stamp = runs[0]\n\n    rr = runs_root(ctx.project_root)\n    manifest_path = os.path.join(rr, stamp, \'manifest.json\')\n\n    if not os.path.isfile(manifest_path):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Run not found: \' + stamp\n        return\n\n    try:\n        with open(manifest_path, \'r\', encoding=\'utf-8\') as f:\n            manifest = json.load(f)\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Manifest unreadable: \' + str(e)\n        return\n\n    touched = manifest.get(\'touched\') or []\n    snap_dir = os.path.join(rr, stamp, \'snapshots\')\n    lines = [\'DIFF %s [%d file(s)]\\n\' % (stamp, len(touched))]\n\n    for t in touched:\n        rel = t.get(\'rel\', \'?\')\n        snap_path = os.path.join(snap_dir, rel)\n        current_path = os.path.join(ctx.project_root, rel)\n\n        try:\n            with open(snap_path, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n                before = f.read()\n        except Exception:\n            before = \'\'\n\n        try:\n            with open(current_path, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n                after = f.read()\n        except Exception:\n            after = \'\'\n\n        diff = _diff_lines(before, after)\n        lines.append(rel)\n        if diff:\n            lines.extend(diff)\n        else:\n            lines.append(\'  (no changes - already reverted or overwritten)\')\n        lines.append(\'\')\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d file(s) diffed\' % len(touched)\n    result[\'preview\'] = \'\\n\'.join(lines).rstrip()\n',
    'forge2/ops/grep_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.grep_ast\n============\nSearch for a pattern across project files.\n\nDirectives:\n    PATTERN     required - string to search for\n    MATCH       exact|fuzzy|regex (default: exact)\n    FILTER      optional - substring that must appear in relative file path\n    LIMIT       optional - max number of hits to return (default: unlimited)\n    EXT         optional - comma-separated file extensions (default: .py)\n\nTarget:\n    - A file path to search one file\n    - A directory path to search recursively under that directory\n    - \'.\' to search recursively under the whole project root\n\nExamples:\n    EXT: .py\n    EXT: .py,.md,.txt\n"""\n\nimport os\nimport re\n\n_SKIP_DIRS = {\n    \'.git\',\n    \'.Trash\',\n    \'__pycache__\',\n    \'forge2_runs\',\n    \'site-packages\',\n    \'site-packages-2\',\n    \'site-packages-3\',\n    \'stash\',\n    \'stash_extensions\',\n    \'patch_runs\',\n    \'snapshots\',\n}\n\nSPEC = {\n    \'name\': \'GREP\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'none\',\n    \'allowed_directives\': {\'PATTERN\', \'MATCH\', \'FILTER\', \'LIMIT\', \'EXT\'},\n    \'required_directives\': {\'PATTERN\'},\n}\n\nHELP = {\n    \'summary\': \'Search across project files using exact, fuzzy, or regex matching. Supports file, directory, or whole-project targets.\',\n    \'subject\': [\'Relative file path, directory path, or . for project-wide search.\'],\n    \'common_failures\': [\n        \'Missing PATTERN directive.\',\n        \'MATCH must be exact, fuzzy, or regex.\',\n        \'Invalid regex pattern.\',\n        \'LIMIT must be an integer.\',\n        \'EXT must be a comma-separated list like .py,.md,.txt\',\n        \'Target escapes project root.\',\n        \'Missing or invalid target.\',\n    ],\n    \'safe_usage\': [\n        \'Use exact mode first when you know the literal text.\',\n        \'Use FILTER to narrow by relative path when project-wide search is too noisy.\',\n        \'Use EXT to search docs as well as code, for example .py,.md,.txt.\',\n        \'Use LIMIT to cap very large result sets.\',\n        \'Use . for whole-project search, a directory for recursive subtree search, or a file for a single-file search.\',\n        \'Default quick search: PATTERN only. This gives exact matching over .py files.\',\n        \'Catch-up search: use MATCH: fuzzy with EXT: .py,.md,.txt and usually LIMIT: 20.\',\n        \'Fuzzy mode is token-based and case-insensitive, so it works well for vague onboarding or project catch-up prompts.\',\n    ],\n    \'minimal_example\': [\n        \'GREP .\',\n        \'PATTERN: MEMORY_SYNC\',\n        \'\',\n        \'GREP .\',\n        \'PATTERN: journal onboarding\',\n        \'MATCH: fuzzy\',\n        \'FILTER: journal/\',\n        \'EXT: .py,.md,.txt\',\n        \'LIMIT: 20\',\n    ],\n    \'related_ops\': [\'LIST_FILES\', \'READ_FILE\', \'PREVIEW_FILE\'],\n}\n\nHINTS = {\n    \'pattern\': \'Add PATTERN: <text> — the search term to find\',\n    \'target\': \'Provide a file path or directory to search — e.g. GREP forge2/ or GREP myfile.py\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if \'PATTERN\' not in parsed_op.directives:\n        errors.append(\'GREP requires PATTERN directive\')\n\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    if match_mode not in (\'exact\', \'fuzzy\', \'regex\'):\n        errors.append(\'GREP MATCH must be exact, fuzzy, or regex, got: \' + str(match_mode))\n\n    limit_raw = parsed_op.directives.get(\'LIMIT\')\n    if limit_raw not in (None, \'\'):\n        try:\n            limit = int(limit_raw)\n            if limit < 1:\n                errors.append(\'GREP LIMIT must be >= 1\')\n        except Exception:\n            errors.append(\'GREP LIMIT must be an integer\')\n\n    ext_raw = (parsed_op.directives.get(\'EXT\') or \'.py\').strip()\n    if not ext_raw:\n        errors.append(\'GREP EXT cannot be empty\')\n    else:\n        parts = [p.strip() for p in ext_raw.split(\',\') if p.strip()]\n        if not parts:\n            errors.append(\'GREP EXT cannot be empty\')\n        for part in parts:\n            if not part.startswith(\'.\'):\n                errors.append(\'GREP EXT entries must start with "." e.g. .py,.md\')\n\n    return errors\n\n\ndef _normalize_ws(s):\n    return \' \'.join(s.split())\n\n\ndef _fuzzy_match(pattern, line):\n    needle = _normalize_ws(pattern).lower()\n    hay = _normalize_ws(line).lower()\n    if not needle:\n        return False\n    tokens = [t for t in needle.split(\' \') if t]\n    return all(tok in hay for tok in tokens)\n\n\ndef _parse_exts(ext_raw):\n    raw = (ext_raw or \'.py\').strip()\n    parts = [p.strip() for p in raw.split(\',\') if p.strip()]\n    return tuple(parts or [\'.py\'])\n\n\ndef _collect_files(ctx, target_raw, exts):\n    target_abs = os.path.realpath(os.path.join(ctx.project_root, target_raw))\n    if not ctx.in_root(target_abs):\n        return None, \'Target escapes project root\'\n\n    if os.path.isfile(target_abs):\n        if target_abs.endswith(exts):\n            return [target_abs], None\n        return [], None\n\n    if not os.path.isdir(target_abs):\n        return None, \'File not found: \' + target_raw\n\n    files = []\n    for dirpath, dirnames, filenames in os.walk(target_abs):\n        dirnames[:] = [d for d in dirnames if d not in _SKIP_DIRS]\n        for fn in filenames:\n            if fn.endswith(exts):\n                files.append(os.path.join(dirpath, fn))\n    files.sort()\n    return files, None\n\n\ndef execute(ctx, parsed_op, result):\n    pattern = parsed_op.directives.get(\'PATTERN\', \'\')\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    path_filter = (parsed_op.directives.get(\'FILTER\') or \'\').strip()\n    limit_raw = parsed_op.directives.get(\'LIMIT\')\n    limit = None if limit_raw in (None, \'\') else int(limit_raw)\n    exts = _parse_exts(parsed_op.directives.get(\'EXT\'))\n    target_raw = (parsed_op.target or \'.\').strip() or \'.\'\n\n    if not pattern:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'GREP requires PATTERN\'\n        return\n\n    files, err = _collect_files(ctx, target_raw, exts)\n    if err:\n        status = \'FAILED_INVALID_PATH\' if \'escapes project root\' in err else \'FAILED_IO\'\n        result[\'status\'] = status\n        result[\'message\'] = err\n        return\n\n    regex_error = None\n    scanned = 0\n    total_hits = 0\n    grouped_hits = {}\n\n    for fpath in files:\n        rel = os.path.relpath(fpath, ctx.project_root)\n\n        if path_filter and path_filter not in rel:\n            continue\n\n        try:\n            src = ctx.file_cache.get(fpath)\n            if src is None:\n                with open(fpath, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n                    src = f.read()\n        except Exception:\n            continue\n\n        scanned += 1\n\n        for lineno, line in enumerate(src.splitlines(), 1):\n            matched = False\n\n            if match_mode == \'regex\':\n                try:\n                    matched = re.search(pattern, line) is not None\n                except re.error as e:\n                    regex_error = str(e)\n                    break\n            elif match_mode == \'fuzzy\':\n                matched = _fuzzy_match(pattern, line)\n            else:\n                matched = pattern in line\n\n            if matched:\n                grouped_hits.setdefault(rel, []).append((lineno, line.rstrip()))\n                total_hits += 1\n                if limit is not None and total_hits >= limit:\n                    break\n\n        if regex_error:\n            break\n        if limit is not None and total_hits >= limit:\n            break\n\n    if regex_error:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'Invalid regex: \' + regex_error\n        return\n\n    files_hit = len(grouped_hits)\n\n    header_lines = [\n        "GREP %r [%d hits across %d files scanned, %d files hit]" % (\n            pattern, total_hits, scanned, files_hit\n        )\n    ]\n    if path_filter:\n        header_lines.append("FILTER=%r" % path_filter)\n    if exts:\n        header_lines.append("EXT=%s" % \',\'.join(exts))\n    if limit is not None:\n        header_lines.append("LIMIT=%d" % limit)\n\n    out = [\'\\n\'.join(header_lines)]\n\n    if not grouped_hits:\n        out.append(\'(no hits)\')\n    else:\n        for rel in sorted(grouped_hits):\n            out.append(rel)\n            for lineno, text in grouped_hits[rel]:\n                out.append(\'  %d: %s\' % (lineno, text))\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d hits across %d files scanned\' % (total_hits, scanned)\n    result[\'preview\'] = \'\\n\'.join(out)\n',
    'forge2/ops/help_op.py': '# -*- coding: utf-8 -*-\n"""\nops.help_op\n===========\nShow syntax and guidance for one registered forge2 op.\n"""\n\nSPEC = {\n    \'name\': \'HELP\',\n    \'target_kind\': \'none\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Show syntax, required directives, common failures, and examples for one op.\',\n    \'subject\': [\'Registered op name, for example READ_FILE.\'],\n    \'common_failures\': [\n        \'Unknown op name.\',\n    ],\n    \'safe_usage\': [\n        \'Use uppercase op names for clarity.\',\n        \'Pair with LIST_OPS when exploring the system.\',\n    ],\n    \'minimal_example\': [\n        \'HELP REPLACE_FILE_RANGE\',\n    ],\n    \'related_ops\': [\'LIST_OPS\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'HELP requires an op name target\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.op_help import render_op_help\n\n    text = render_op_help(parsed_op.target)\n    if text is None:\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = \'Unknown op: \' + (parsed_op.target or \'\').strip()\n        return\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Help for \' + (parsed_op.target or \'\').strip().upper()\n    result[\'preview\'] = text\n',
    'forge2/ops/insert_after_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.insert_after_ast\n====================\nAST insert-after target op.\n"""\n\nSPEC = {\n    \'name\': \'INSERT_AFTER\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Insert a new method or function immediately after an existing AST target.\',\n}\n\nHINTS = {\n    \'body required\': \'Provide the new method or function as the body\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n    \'already present\': \'A def with that name already exists in the file — use REPLACE instead\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'INSERT_AFTER requires a target\')\n    if not parsed_op.body:\n        errors.append(\'INSERT_AFTER requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n    from forge2.source_ops import insert_after_lines, get_line_indent\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    insert_line = resolved[\'end\']\n    ref_line = before.splitlines(True)[resolved[\'start\'] - 1]\n    indent = get_line_indent(ref_line)\n\n    patched = insert_after_lines(\n        before,\n        insert_line,\n        parsed_op.body,\n        indent,\n        tight=False,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': patched,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'inserted after %s lines %d-%d\' % (\n        resolved[\'kind\'],\n        resolved[\'start\'],\n        resolved[\'end\'],\n    )\n',
    'forge2/ops/insert_before_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.insert_before_ast\n=====================\nAST insert-before target op.\n"""\n\nSPEC = {\n    \'name\': \'INSERT_BEFORE\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Insert a new method or function immediately before an existing AST target.\',\n}\n\nHINTS = {\n    \'body required\': \'Provide the new method or function as the body\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n    \'already present\': \'A def with that name already exists in the file — use REPLACE instead\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'INSERT_BEFORE requires a target\')\n    if not parsed_op.body:\n        errors.append(\'INSERT_BEFORE requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n    from forge2.source_ops import insert_after_lines, get_line_indent\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    insert_line = resolved[\'start\'] - 1\n    ref_line = before.splitlines(True)[resolved[\'start\'] - 1]\n    indent = get_line_indent(ref_line)\n\n    patched = insert_after_lines(\n        before,\n        insert_line,\n        parsed_op.body,\n        indent,\n        tight=False,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': patched,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'inserted before %s lines %d-%d\' % (\n        resolved[\'kind\'],\n        resolved[\'start\'],\n        resolved[\'end\'],\n    )\n',
    'forge2/ops/insert_file_line.py': '# -*- coding: utf-8 -*-\n"""\nops.insert_file_line\n====================\nInsert lines into a plain text file at a given line number.\nNo anchors needed. Body is spliced in before or after LINE: N.\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'INSERT_FILE_LINE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set([\'LINE\', \'POSITION\']),\n    \'required_directives\': set([\'LINE\']),\n}\n\nHELP = {\n    \'summary\': \'Insert lines into a plain text file at a given line number.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'optional_directives\': [\n        \'POSITION: before|after (default after)\',\n    ],\n    \'common_failures\': [\n        \'LINE out of range for the current file.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n        \'POSITION must be before or after.\',\n    ],\n    \'safe_usage\': [\n        \'PREVIEW_FILE the target area first to confirm the line number.\',\n        \'Use POSITION: before to insert above a section heading.\',\n        \'Use POSITION: after to append below a line.\',\n        \'Prefer this over REPLACE_FILE_RANGE when you only want to insert without replacing.\',\n    ],\n    \'minimal_example\': [\n        \'INSERT_FILE_LINE journal/prompt.md\',\n        \'LINE: 40\',\n        \'POSITION: after\',\n        \'BEGIN_BODY\',\n        \'## New Section\',\n        \'\',\n        \'Content here.\',\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW_FILE\', \'REPLACE_FILE_RANGE\', \'REPLACE_FILE_LINES\'],\n}\n\nHINTS = {\n    \'line\': \'Add LINE: N — the line number to insert at\',\n    \'body required\': \'Provide the content to insert as the body\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'INSERT_FILE_LINE requires a target path\')\n    if not parsed_op.body:\n        errors.append(\'INSERT_FILE_LINE requires body content\')\n\n    line_val = parsed_op.directives.get(\'LINE\')\n    if not isinstance(line_val, int) or line_val < 1:\n        errors.append(\'INSERT_FILE_LINE requires LINE: N (integer >= 1)\')\n\n    position = parsed_op.directives.get(\'POSITION\', \'after\')\n    if position not in (\'before\', \'after\'):\n        errors.append(\'POSITION must be before or after\')\n\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        before = f.read()\n\n    src_lines = before.splitlines()\n    total     = len(src_lines)\n\n    line_n   = parsed_op.directives.get(\'LINE\')\n    position = parsed_op.directives.get(\'POSITION\', \'after\')\n\n    if line_n > total:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'LINE out of range: file has %d lines\' % total\n        return\n\n    new_lines = parsed_op.body.splitlines()\n\n    if position == \'before\':\n        insert_at = line_n - 1\n    else:\n        insert_at = line_n\n\n    out_lines = src_lines[:insert_at] + new_lines + src_lines[insert_at:]\n    after = \'\\n\'.join(out_lines)\n    if before.endswith(\'\\n\') or parsed_op.body.endswith(\'\\n\'):\n        after += \'\\n\'\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\']    = parsed_op.target\n    result[\'status\']  = \'APPLIED\'\n    result[\'message\'] = \'Inserted %d lines %s line %d\' % (len(new_lines), position, line_n)\n',
    'forge2/ops/insert_into_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.insert_into_ast\n===================\nInject code inside an AST target at an anchor line.\n\nDirectives:\n    ANCHOR      required - substring to find in target body\n    POSITION    before|after (default: after)\n    INDENT      auto|same|child (default: auto)\n                  auto  -> child if anchor ends with \':\', else same\n                  same  -> match anchor line indent\n                  child -> anchor indent + 4 spaces\n    MATCH       exact|fuzzy (default: exact)\n    OCCURRENCE  N - which match to use (default: 1)\n    EXPECT      N - require exactly N matches (default: 1)\n"""\n\nSPEC = {\n    \'name\': \'INSERT_INTO\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': {\'ANCHOR\', \'POSITION\', \'INDENT\', \'MATCH\', \'OCCURRENCE\', \'EXPECT\'},\n    \'required_directives\': {\'ANCHOR\'},\n}\n\nHELP = {\n    \'summary\': \'Insert new code inside a resolved AST target relative to an anchor line.\',\n    \'subject\': [\n        \'AST target string, for example file.py::Class.method_name.\',\n        \'Use this when you want to inject code without replacing the whole target.\',\n    ],\n    \'common_failures\': [\n        \'Missing ANCHOR directive.\',\n        \'Anchor not found or not unique enough.\',\n        \'POSITION must be before or after.\',\n        \'INDENT must be auto, same, or child.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n    ],\n    \'safe_usage\': [\n        \'PREVIEW the target first and use a unique anchor when possible.\',\n        \'Use EXPECT and OCCURRENCE when the anchor may appear more than once.\',\n        \'Use INDENT: child when intentionally inserting under a block header.\',\n    ],\n    \'minimal_example\': [\n        \'INSERT_INTO forge_app_lab/forge_app_state.py::build_starter_bundle\',\n        \'ANCHOR: if op == \\\'READ_FILE\\\':\',\n        \'POSITION: after\',\n        \'INDENT: child\',\n        \'BEGIN_BODY\',\n        "if op == \'HELP\':",\n        "    return \'HELP %s\\\\n\' % subject",\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW\', \'REPLACE\', \'APPEND_INTO\', \'PREPEND_INTO\'],\n}\n\nHINTS = {\n    \'anchor\': \'Add ANCHOR: <text> — unique substring on the line to insert near\',\n    \'body required\': \'Provide the code to insert as the body\',\n    \'matched 0\': \'Anchor not found — use PREVIEW to check exact text in target\',\n    \'position\': \'POSITION must be before or after\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'INSERT_INTO requires a target\')\n    if not parsed_op.body:\n        errors.append(\'INSERT_INTO requires body content\')\n    if \'ANCHOR\' not in parsed_op.directives:\n        errors.append(\'Missing required directive for INSERT_INTO: ANCHOR\')\n    position = parsed_op.directives.get(\'POSITION\', \'after\')\n    if position not in (\'before\', \'after\'):\n        errors.append(\'INSERT_INTO POSITION must be before or after, got: \' + str(position))\n    indent_mode = parsed_op.directives.get(\'INDENT\', \'auto\')\n    if indent_mode not in (\'auto\', \'same\', \'child\'):\n        errors.append(\'INSERT_INTO INDENT must be auto, same, or child, got: \' + str(indent_mode))\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n    from forge2.source_ops import insert_after_lines, get_line_indent\n    from forge2.anchor_tools import resolve_single_anchor\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    all_lines = before.splitlines()\n    start = resolved[\'start\']\n    end = resolved[\'end\']\n    target_lines = all_lines[start - 1:end]\n\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    occurrence = parsed_op.directives.get(\'OCCURRENCE\', 1)\n    expect = parsed_op.directives.get(\'EXPECT\', 1)\n\n    try:\n        occurrence = int(occurrence)\n    except Exception:\n        occurrence = 1\n    try:\n        expect = int(expect)\n    except Exception:\n        expect = 1\n\n    rel_idx, err = resolve_single_anchor(\n        target_lines,\n        parsed_op.directives.get(\'ANCHOR\', \'\'),\n        match_mode=match_mode,\n        occurrence=occurrence,\n        expect=expect,\n    )\n    if err:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR: \' + err\n        return\n\n    abs_line = start + rel_idx\n\n    anchor_line = all_lines[abs_line - 1]\n    anchor_indent = get_line_indent(anchor_line)\n\n    indent_mode = parsed_op.directives.get(\'INDENT\', \'auto\')\n    if indent_mode == \'child\':\n        indent = anchor_indent + \'    \'\n    elif indent_mode == \'same\':\n        indent = anchor_indent\n    else:\n        if anchor_line.rstrip().endswith(\':\'):\n            indent = anchor_indent + \'    \'\n        else:\n            indent = anchor_indent\n\n    position = parsed_op.directives.get(\'POSITION\', \'after\')\n    insert_line = abs_line if position == \'after\' else abs_line - 1\n\n    after = insert_after_lines(\n        before,\n        insert_line,\n        parsed_op.body,\n        indent,\n        tight=True,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'inserted %s anchor line %d in %s\' % (\n        position, abs_line, resolved[\'kind\']\n    )\n',
    'forge2/ops/list_files.py': '# -*- coding: utf-8 -*-\n"""\nops.list_files\n==============\nDirectory listing op.\n"""\n\nimport os\n\nfrom forge2.file_ops import resolve_under_root, in_root, rel_from_root\n\nSPEC = {\n    \'name\': \'LIST_FILES\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'FILTER\', \'DEPTH\', \'FILES\', \'ALL\']),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'List directory contents as a tree. Auto-excludes noisy system folders. Shows README.md summaries inline.\',\n    \'subject\': [\'Relative directory path inside project root. Defaults to . if omitted.\'],\n    \'directives\': {\n        \'DEPTH\': \'How many levels deep to recurse (default 1, use 2+ for tree view)\',\n        \'FILES\': \'Show files in output — yes or no (default yes)\',\n        \'FILTER\': \'Only include files with this suffix e.g. .py\',\n        \'ALL\': \'Set to yes to disable auto-exclusions and show everything (default no)\',\n    },\n    \'common_failures\': [\n        \'Directory not found.\',\n        \'Target escapes project root.\',\n        \'Using a file path where a directory is expected.\',\n    ],\n    \'safe_usage\': [\n        \'Use DEPTH: 2 FILES: no for a clean project overview at session start.\',\n        \'Auto-excludes .Trash, site-packages, stash, script_snapshots, Examples, Templates.\',\n        \'Use ALL: yes to override exclusions when you need the full listing.\',\n        \'README.md first lines are shown as · summaries under each directory.\',\n    ],\n    \'minimal_example\': [\n        \'LIST_FILES .\',\n        \'DEPTH: 2\',\n        \'FILES: no\',\n    ],\n    \'related_ops\': [\'READ_FILE\', \'PREVIEW_FILE\', \'GREP\'],\n}\n\n\ndef validate(parsed_op):\n    return []\n\n\n_EXCLUDED = {\n    \'.Trash\',\n    \'site-packages\', \'site-packages-2\', \'site-packages-3\',\n    \'stash\', \'stash_extensions\',\n    \'script_snapshots\',\n    \'Examples\', \'Templates\',\n}\n\ndef execute(ctx, parsed_op, result):\n    target = (parsed_op.target or \'.\').strip() or \'.\'\n    dir_abs = resolve_under_root(ctx.project_root, target)\n\n    if not in_root(ctx.project_root, dir_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isdir(dir_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Directory not found: \' + target\n        return\n\n    ext_filter = (parsed_op.directives.get(\'FILTER\') or \'\').strip()\n    show_files = (parsed_op.directives.get(\'FILES\') or \'yes\').strip().lower() != \'no\'\n    show_all = (parsed_op.directives.get(\'ALL\') or \'no\').strip().lower() == \'yes\'\n\n    try:\n        max_depth = int((parsed_op.directives.get(\'DEPTH\') or \'1\').strip())\n    except ValueError:\n        max_depth = 1\n\n    lines = []\n\n    def _readme_summary(dir_path):\n        readme = os.path.join(dir_path, \'README.md\')\n        if not os.path.isfile(readme):\n            return None\n        try:\n            with open(readme, encoding=\'utf-8\') as f:\n                for line in f:\n                    stripped = line.strip()\n                    if not stripped or stripped.startswith(\'#\'):\n                        continue\n                    return stripped\n        except OSError:\n            pass\n        return None\n\n    def _walk(abs_path, depth):\n        try:\n            entries = sorted(os.listdir(abs_path))\n        except OSError:\n            return\n        for name in entries:\n            full = os.path.join(abs_path, name)\n            is_dir = os.path.isdir(full)\n            indent = \'  \' * depth\n            if is_dir:\n                if not show_all and name in _EXCLUDED:\n                    continue\n                lines.append(indent + name + \'/\')\n                summary = _readme_summary(full)\n                if summary:\n                    lines.append(indent + \'  · \' + summary)\n                if depth + 1 < max_depth:\n                    _walk(full, depth + 1)\n            else:\n                if ext_filter and not name.endswith(ext_filter):\n                    continue\n                if not show_files:\n                    continue\n                try:\n                    size = os.path.getsize(full)\n                    lines.append(indent + \'%s (%d bytes)\' % (name, size))\n                except OSError:\n                    lines.append(indent + name)\n\n    _walk(dir_abs, 0)\n\n    rel = rel_from_root(ctx.project_root, dir_abs)\n    result[\'file\'] = rel\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d entries\' % len(lines)\n    result[\'preview\'] = \'LIST_FILES %s [%d entries]\\n\' % (rel, len(lines)) + \'\\n\'.join(lines)\n',
    'forge2/ops/list_ops.py': '# -*- coding: utf-8 -*-\n"""\nops.list_ops\n============\nList active forge2 ops.\n"""\n\nSPEC = {\n    \'name\': \'LIST_OPS\',\n    \'target_kind\': \'none\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'List all currently registered forge2 ops grouped by category.\',\n    \'subject\': [\'No target.\'],\n    \'common_failures\': [],\n    \'safe_usage\': [\n        \'Use this to discover active ops instead of relying on old docs.\',\n        \'Use HELP <OP_NAME> for syntax and pain points.\',\n    ],\n    \'minimal_example\': [\n        \'LIST_OPS\',\n    ],\n    \'related_ops\': [\'HELP\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if (parsed_op.target or \'\').strip():\n        errors.append(\'LIST_OPS does not take a target\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.op_help import render_ops_list\n\n    text = render_ops_list()\n    count = len([line for line in text.splitlines() if line.startswith(\'- \')])\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d ops\' % count\n    result[\'preview\'] = text\n',
    'forge2/ops/list_runs.py': '# -*- coding: utf-8 -*-\n"""\nops.list_runs\n=============\nList recent forge2 runs with a readable summary.\n\nUsage:\n    LIST_RUNS\n    LIST_RUNS 10\n"""\n\nimport os\nimport json\n\nSPEC = {\n    \'name\': \'LIST_RUNS\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'none\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'List recent forge2 runs with their IDs, timestamps, and op summaries.\',\n}\n\n\ndef validate(parsed_op):\n    return []\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.run_storage import runs_root, list_runs\n\n    limit_raw = (parsed_op.target or \'\').strip()\n    try:\n        limit = int(limit_raw)\n    except Exception:\n        limit = 50\n\n    runs = list_runs(ctx.project_root)[:limit]\n\n    if not runs:\n        result[\'status\'] = \'APPLIED\'\n        result[\'message\'] = \'No runs found\'\n        result[\'preview\'] = \'LIST_RUNS [0 runs]\'\n        return\n\n    rr = runs_root(ctx.project_root)\n    write_runs = []\n    for stamp in runs:\n        mp = os.path.join(rr, stamp, \'manifest.json\')\n        if not os.path.isfile(mp):\n            continue\n        try:\n            with open(mp, \'r\', encoding=\'utf-8\') as f:\n                m = json.load(f)\n            if m.get(\'touched\'):\n                write_runs.append(stamp)\n        except Exception:\n            pass\n\n    if not write_runs:\n        result[\'status\'] = \'APPLIED\'\n        result[\'message\'] = \'No runs with file changes found\'\n        result[\'preview\'] = \'LIST_RUNS [0 runs]\'\n        return\n\n    lines = [\'LIST_RUNS [%d runs]\\n\' % len(write_runs)]\n    for stamp in write_runs:\n\n        manifest_path = os.path.join(rr, stamp, \'manifest.json\')\n        if not os.path.isfile(manifest_path):\n            lines.append(\'%s  (no manifest)\' % stamp)\n            continue\n        try:\n            with open(manifest_path, \'r\', encoding=\'utf-8\') as f:\n                manifest = json.load(f)\n        except Exception:\n            lines.append(\'%s  (unreadable)\' % stamp)\n            continue\n\n        touched = manifest.get(\'touched\') or []\n        results_data = manifest.get(\'results\') or []\n\n        applied = sum(1 for r in results_data if r.get(\'status\') == \'APPLIED\')\n        failed = sum(1 for r in results_data if (r.get(\'status\') or \'\').startswith(\'FAILED\'))\n        skipped = len(results_data) - applied - failed\n\n        ops_summary = []\n        seen_ops = {}\n        for r in results_data:\n            op = r.get(\'op\') or \'?\'\n            st = r.get(\'status\') or \'?\'\n            key = op + \'/\' + st\n            seen_ops[key] = seen_ops.get(key, 0) + 1\n\n        for key, count in seen_ops.items():\n            op, st = key.split(\'/\', 1)\n            if count > 1:\n                ops_summary.append(\'%s x%d\' % (op, count))\n            else:\n                ops_summary.append(op)\n\n        files = [t.get(\'rel\', \'?\') for t in touched[:3]]\n        files_str = \', \'.join(files)\n        if len(touched) > 3:\n            files_str += \' (+%d more)\' % (len(touched) - 3)\n\n        status_str = \'A:%d\' % applied\n        if skipped:\n            status_str += \' S:%d\' % skipped\n        if failed:\n            status_str += \' F:%d\' % failed\n\n        line = \'%s  [%s]  %s\' % (\n            stamp,\n            status_str,\n            files_str or \'(no files touched)\',\n        )\n        lines.append(line)\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d runs\' % len(runs)\n    result[\'preview\'] = \'\\n\'.join(lines)\n',
    'forge2/ops/list_targets.py': '# -*- coding: utf-8 -*-\n"""\nops.list_targets\n================\nPhase-2 AST target listing bridge.\n"""\n\n\n\nSPEC = {\n    \'name\': \'LIST_TARGETS\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'List patchable AST targets found in a Python file.\',\n    \'subject\': [\'Relative Python file path inside project root.\'],\n    \'common_failures\': [\n        \'File path is missing or invalid.\',\n        \'Target escapes project root.\',\n        \'Non-Python files may yield zero useful targets.\',\n    ],\n    \'safe_usage\': [\n        \'Use this before PREVIEW or REPLACE when you do not know the exact target name.\',\n        \'This is often the fastest way to discover class, method, function, and assignment targets.\',\n    ],\n    \'minimal_example\': [\n        \'LIST_TARGETS forge_app_lab/forge_app_state.py\',\n    ],\n    \'related_ops\': [\'PREVIEW\', \'REPLACE\', \'INSERT_INTO\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'LIST_TARGETS requires a file path\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    import ast\n    import os\n\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    try:\n        with open(file_abs, \'r\', encoding=\'utf-8\') as f:\n            src = f.read()\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Cannot read file: %s\' % e\n        return\n\n    try:\n        tree = ast.parse(src)\n    except SyntaxError as e:\n        result[\'status\'] = \'FAILED\'\n        result[\'message\'] = \'SyntaxError: %s\' % e\n        return\n\n    try:\n        rel = os.path.relpath(file_abs, ctx.project_root)\n    except ValueError:\n        rel = os.path.basename(file_abs)\n\n    def first_doc(node):\n        try:\n            raw = ast.get_docstring(node) or \'\'\n            for line in raw.splitlines():\n                line = line.strip()\n                if line:\n                    return line\n        except Exception:\n            pass\n        return \'\'\n\n    def range_str(node):\n        if node.lineno == node.end_lineno:\n            return \'[%d]\' % node.lineno\n        return \'[%d-%d]\' % (node.lineno, node.end_lineno)\n\n    def fmt(target, rng, doc, indent=0):\n        pad = \'  \' * indent\n        line = \'%-58s %s\' % (pad + target, rng)\n        if doc:\n            line += \'  # \' + doc[:60]\n        return line\n\n    targets = []\n    preview_lines = []\n\n    for node in tree.body:\n        if isinstance(node, ast.ClassDef):\n            doc = first_doc(node)\n            t = \'%s::%s.*\' % (rel, node.name)\n            preview_lines.append(fmt(t, range_str(node), doc))\n            targets.append(t)\n            for item in node.body:\n                if isinstance(item, (ast.FunctionDef, ast.AsyncFunctionDef)):\n                    ft = \'%s::%s.%s\' % (rel, node.name, item.name)\n                    preview_lines.append(fmt(ft, range_str(item), first_doc(item), indent=1))\n                    targets.append(ft)\n                elif isinstance(item, ast.Assign):\n                    for t2 in (item.targets or []):\n                        if isinstance(t2, ast.Name):\n                            at = \'%s::%s.@%s\' % (rel, node.name, t2.id)\n                            preview_lines.append(fmt(at, \'[%d]\' % item.lineno, \'\', indent=1))\n                            targets.append(at)\n                elif isinstance(item, ast.AnnAssign):\n                    t2 = getattr(item, \'target\', None)\n                    if isinstance(t2, ast.Name):\n                        at = \'%s::%s.@%s\' % (rel, node.name, t2.id)\n                        preview_lines.append(fmt(at, \'[%d]\' % item.lineno, \'\', indent=1))\n                        targets.append(at)\n        elif isinstance(node, (ast.FunctionDef, ast.AsyncFunctionDef)):\n            t = \'%s::%s\' % (rel, node.name)\n            preview_lines.append(fmt(t, range_str(node), first_doc(node)))\n            targets.append(t)\n        elif isinstance(node, ast.Assign):\n            for t2 in (node.targets or []):\n                if isinstance(t2, ast.Name):\n                    t = \'%s::@%s\' % (rel, t2.id)\n                    preview_lines.append(fmt(t, \'[%d]\' % node.lineno, \'\'))\n                    targets.append(t)\n        elif isinstance(node, ast.AnnAssign):\n            t2 = getattr(node, \'target\', None)\n            if isinstance(t2, ast.Name):\n                t = \'%s::@%s\' % (rel, t2.id)\n                preview_lines.append(fmt(t, \'[%d]\' % node.lineno, \'\'))\n                targets.append(t)\n\n    header = \'LIST_TARGETS %s [%d targets]\' % (rel, len(targets))\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d targets\' % len(targets)\n    result[\'preview\'] = header + \'\\n\' + \'\\n\'.join(preview_lines)\n    result[\'file\'] = parsed_op.target\n',
    'forge2/ops/memory_sync.py': '# -*- coding: utf-8 -*-\n"""\nops.memory_sync\n===============\nEmit a tight memory-sync prompt card for Forge2 sessions.\n\nThis op does not write memory itself. It tells the LLM how to inspect\njournal-backed memory, decide what should be refreshed, and emit a runnable\nForge bundle to update memory entries and durable long memory.\n\nIntended use:\n- Mainly for end-of-session development / coding sync\n- Not for routine daily-life journalling\n- Journal holds the detail; memory holds compressed boot state\n"""\n\nSPEC = {\n    \'name\': \'MEMORY_SYNC\',\n    \'target_kind\': \'none\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Emit a memory-sync prompt card for journal-backed operational memory and selective durable long-memory updates.\',\n    \'subject\': [\'Optional focus target, for example forge2, journal, ios, tile_calc, or a project area.\'],\n    \'common_failures\': [],\n    \'safe_usage\': [\n        \'Use MEMORY_SYNC near the end of a development or coding session.\',\n        \'Use it when you want the model to compress recent work into boot-ready memory rather than keep everything in chat.\',\n        \'Daily-life notes should usually stay in the journal without memory sync.\',\n        \'Pass a short focus target to bias the sync toward one area.\',\n    ],\n    \'minimal_example\': [\n        \'MEMORY_SYNC\',\n        \'MEMORY_SYNC forge2\',\n    ],\n    \'related_ops\': [\'RUN_FILE\', \'READ_FILE\', \'GREP\', \'REPLACE_FILE\', \'APPEND_LIST\', \'EXPLORER\'],\n}\n\n\ndef validate(parsed_op):\n    return []\n\n\ndef _suggested_bundle(focus):\n    focus = (focus or \'\').strip()\n    lines = [\n        \'RUN_FILE journal/cli.py\',\n        \'ARGS: kind memory\',\n        \'\',\n        \'READ_FILE docs/long_memory.py\',\n    ]\n    if focus:\n        lines.extend([\n            \'\',\n            \'GREP .\',\n            \'PATTERN: %s\' % focus,\n            \'MATCH: fuzzy\',\n        ])\n    return \'\\n\'.join(lines)\n\n\ndef execute(ctx, parsed_op, result):\n    focus = (parsed_op.target or \'\').strip()\n\n    lines = [\'MEMORY_SYNC MODE\']\n\n    if focus:\n        lines.extend([\n            \'\',\n            \'Focus:\',\n            focus,\n        ])\n\n    lines.extend([\n        \'\',\n        \'What this is for:\',\n        \'Use this near the end of a development session to compress what matters into boot-ready memory.\',\n        \'For normal daily-life use, prefer adding journal entries and skip memory sync unless there is a clear continuity need.\',\n        \'\',\n        \'Goal:\',\n        \'Compress the current session into lean journal-backed operational memory and only selective durable long-term memory.\',\n        \'\',\n        \'Read first:\',\n        \'- RUN_FILE journal/cli.py  /  ARGS: kind memory\',\n        \'- READ_FILE docs/long_memory.py\',\n        \'\',\n        \'Source of truth:\',\n        \'- Journal memory entries are primary.\',\n        \'- llm_workspace/memory.py is generated cache only.\',\n        \'- Regenerate cache with RUN_FILE journal/memory_sync.py after journal-memory updates.\',\n        \'- Journal stores detail; memory stores compressed boot state.\',\n        \'\',\n        \'Update priority:\',\n        \'- Default / most common update: last_session.\',\n        \'- Update active_project, session_goal, flags, or next_bundles only if something meaningfully changed.\',\n        \'- Update identity rarely.\',\n        \'- Update long memory only when something durable, reusable, or repeatedly important emerged.\',\n        \'\',\n        \'Do not store:\',\n        \'- temporary chatter\',\n        \'- obvious step-by-step implementation noise\',\n        \'- duplicate summaries of recent journal/code entries\',\n        \'- details that are already easy to recover and do not improve future boot speed\',\n        \'- low-value repetition\',\n        \'\',\n        \'Operational memory entries:\',\n        \'- identity\',\n        \'- active_project\',\n        \'- session_goal\',\n        \'- flags\',\n        \'- last_session\',\n        \'- next_bundles\',\n        \'\',\n        \'How to treat operational memory:\',\n        \'- last_session is the normal refresh target.\',\n        \'- The other entries are optional, not mandatory.\',\n        \'- Prefer one good memory update over several weak ones.\',\n        \'- Keep entries compressed, current, and action-relevant.\',\n        \'\',\n        \'Long-memory sections:\',\n        \'- ARCHITECTURE: durable system design decisions\',\n        \'- WORKFLOWS: repeatable ways of working\',\n        \'- GOTCHAS: recurring problems, quirks, or pitfalls\',\n        \'- PROJECT_IDEAS: ideas worth revisiting later\',\n        \'- TRAINING: durable coaching or training-system ideas\',\n        \'- IOS_LIMITS: platform constraints or capabilities worth remembering\',\n        \'- FORGE_OPS: important op behaviour or meta-op concepts\',\n        \'- MILESTONES: shipped additions or major project moments\',\n        \'\',\n        \'How to treat long memory:\',\n        \'- Write only when the lesson will still matter later.\',\n        \'- Prefer updating as few sections as necessary.\',\n        \'- Skip normal session churn, one-off bug notes, and temporary context.\',\n        \'\',\n        \'Expected workflow:\',\n        \'- Inspect existing memory with kind memory.\',\n        \'- Decide the smallest useful update.\',\n        \'- Usually this means refreshing last_session only.\',\n        \'- Emit RUN_FILE journal/cli.py add memory ... calls only for entries that truly need refreshing.\',\n        \'- Then run RUN_FILE journal/memory_sync.py to regenerate llm_workspace/memory.py.\',\n        \'- Update docs/long_memory.py only where clearly warranted.\',\n        \'\',\n        \'Output:\',\n        \'- Brief memory decision summary\',\n        \'- Then a runnable Forge bundle only\',\n        \'\',\n        \'Bundle targets:\',\n        \'- RUN_FILE journal/cli.py  /  ARGS: add memory ...\',\n        \'- RUN_FILE journal/memory_sync.py\',\n        \'- docs/long_memory.py::@SECTION_NAME only where warranted\',\n        \'\',\n        \'Success:\',\n        \'The bundle makes the smallest useful memory update, keeps journal memory current, regenerates cache, and adds only selective durable long-memory entries.\',\n        \'\',\n        \'Suggested first bundle:\',\n        _suggested_bundle(focus),\n    ])\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Memory sync card\'\n    result[\'preview\'] = \'\\n\'.join(lines)\n',
    'forge2/ops/move_dir.py': '# -*- coding: utf-8 -*-\n"""\nops.move_dir\n============\nMove/rename a directory within the project root.\n"""\n\nimport os\nimport shutil\n\nfrom forge2.file_ops import resolve_under_root, in_root, ensure_parent_dir\n\nSPEC = {\n    \'name\': \'MOVE_DIR\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': {\'DEST\'},\n    \'required_directives\': {\'DEST\'},\n    "summary": \'Move or rename a directory to a new path inside project root.\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'MOVE_DIR requires a target path\')\n    if not (parsed_op.directives.get(\'DEST\') or \'\').strip():\n        errors.append(\'Missing required directive for MOVE_DIR: DEST\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    src_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n    dst_abs = resolve_under_root(ctx.project_root, parsed_op.directives.get(\'DEST\'))\n\n    if not (in_root(ctx.project_root, src_abs) and in_root(ctx.project_root, dst_abs)):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Path escapes project root\'\n        return\n\n    if not os.path.isdir(src_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Source directory not found: \' + parsed_op.target\n        return\n\n    if os.path.exists(dst_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Destination already exists: \' + parsed_op.directives.get(\'DEST\')\n        return\n\n    ensure_parent_dir(dst_abs)\n    shutil.move(src_abs, dst_abs)\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%s -> %s\' % (parsed_op.target, parsed_op.directives.get(\'DEST\'))\n',
    'forge2/ops/move_file.py': '# -*- coding: utf-8 -*-\n"""\nops.move_file\n=============\nMove/rename-file op.\n"""\n\nimport os\n\nfrom forge2.file_ops import resolve_under_root, in_root, ensure_parent_dir\n\nSPEC = {\n    \'name\': \'MOVE_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'DEST\']),\n    \'required_directives\': set([\'DEST\']),\n}\n\nHELP = {\n    \'summary\': \'Move or rename a file to a new path under project root.\',\n    \'subject\': [\'Relative source file path inside project root.\'],\n    \'common_failures\': [\n        \'Missing DEST directive.\',\n        \'Source not found.\',\n        \'Destination already exists.\',\n        \'Source or destination escapes project root.\',\n    ],\n    \'safe_usage\': [\n        \'Use this for renames and path cleanups after confirming the new destination.\',\n        \'Unlike COPY_FILE, this removes the original source path.\',\n    ],\n    \'minimal_example\': [\n        \'MOVE_FILE old_name.txt\',\n        \'DEST: archive/new_name.txt\',\n    ],\n    \'related_ops\': [\'COPY_FILE\', \'LIST_FILES\', \'DELETE_FILE\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'MOVE_FILE requires a target path\')\n    if not (parsed_op.directives.get(\'DEST\') or \'\').strip():\n        errors.append(\'Missing required directive for MOVE_FILE: DEST\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    src_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n    dst_abs = resolve_under_root(ctx.project_root, parsed_op.directives.get(\'DEST\'))\n\n    if not (in_root(ctx.project_root, src_abs) and in_root(ctx.project_root, dst_abs)):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Path escapes project root\'\n        return\n\n    if not os.path.isfile(src_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Source not found: \' + parsed_op.target\n        return\n\n    if os.path.exists(dst_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'Destination already exists: \' + parsed_op.directives.get(\'DEST\')\n        return\n\n    ensure_parent_dir(dst_abs)\n    os.rename(src_abs, dst_abs)\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%s -> %s\' % (parsed_op.target, parsed_op.directives.get(\'DEST\'))\n',
    'forge2/ops/pack_op.py': '# -*- coding: utf-8 -*-\n"""\nops.pack\n========\nPack a set of files into a self-installing Python script.\n\nUsage:\n    PACK my_backup\n    EXCLUDE: forge2/forge2_runs/, forge2/forge_app/\n    EXT: *\n    BEGIN_BODY\n    forge2/\n    forge2_shortcut.py\n    END_BODY\n\nOutput: packed/my_backup.py — run it to restore all files to their original paths.\n"""\n\nimport os\n\nDOCUMENTS_ROOT = \'/private/var/mobile/Containers/Shared/AppGroup/2BDBE521-A867-4CC9-932E-BB723BB7DBAC/Pythonista3/Documents\'\n\nSPEC = {\n    \'name\': \'PACK\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': {\'EXCLUDE\', \'EXT\'},\n    \'required_directives\': set(),\n    \'summary\': \'Pack files and directories into a self-installing Python script saved to packed/.\',\n}\n\nHINTS = {\n    \'body required\': \'List paths to include in the body — one file or directory per line\',\n    \'target\': \'Provide an output name e.g. PACK forge2_backup (no extension needed)\',\n}\n\nHELP = {\n    \'summary\': \'Pack files and directories into a self-installing Python script saved to packed/.\',\n    \'subject\': [\'Output filename without extension — saved to packed/<name>.py\'],\n    \'body\': [\n        \'One include path per line.\',\n        \'Directories are walked recursively.\',\n        \'EXT controls which extensions are collected from dirs (default: all).\',\n    ],\n    \'common_failures\': [\n        \'No paths in body.\',\n        \'Include path not found.\',\n        \'Target name missing.\',\n    ],\n    \'safe_usage\': [\n        \'Run the output script once to restore — it writes files relative to its own location.\',\n        \'Use EXCLUDE to skip run dirs, secrets, and caches.\',\n        \'Combine dirs and individual files freely in the body.\',\n    ],\n    \'minimal_example\': [\n        \'PACK forge2_backup\',\n        \'EXCLUDE: forge2/forge2_runs/, forge2/forge_app/\',\n        \'BEGIN_BODY\',\n        \'forge2/\',\n        \'forge2_shortcut.py\',\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'COPY_FILE\', \'CREATE_FILE\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'PACK requires a target name\')\n    if not parsed_op.body or not parsed_op.body.strip():\n        errors.append(\'PACK requires body content — list paths to include\')\n    return errors\n\n\ndef _parse_excludes(parsed_op):\n    raw = parsed_op.directives.get(\'EXCLUDE\', \'\')\n    return [e.strip() for e in raw.split(\',\') if e.strip()]\n\n\ndef _parse_ext(parsed_op):\n    raw = parsed_op.directives.get(\'EXT\', \'*\').strip()\n    if raw == \'*\':\n        return None  # None means all extensions\n    return [e.strip().lstrip(\'.\') for e in raw.split(\',\') if e.strip()]\n\n\ndef _should_exclude(rel, excludes):\n    for ex in excludes:\n        if rel == ex or rel.startswith(ex):\n            return True\n    if rel.endswith(\'.bak\'):\n        return True\n    return False\n\n\ndef _collect_files(root, include_paths, excludes, exts):\n    collected = []\n    for path in include_paths:\n        path = path.strip()\n        if not path:\n            continue\n        abs_path = os.path.join(root, path)\n        if os.path.isdir(abs_path):\n            for dirpath, dirnames, filenames in os.walk(abs_path):\n                dirnames[:] = sorted(\n                    x for x in dirnames\n                    if not x.startswith(\'.\') and x != \'__pycache__\'\n                )\n                for fn in sorted(filenames):\n                    if fn.startswith(\'.\'):\n                        continue\n                    if exts is not None:\n                        ext = fn.rsplit(\'.\', 1)[-1] if \'.\' in fn else \'\'\n                        if ext not in exts:\n                            continue\n                    file_abs = os.path.join(dirpath, fn)\n                    rel = os.path.relpath(file_abs, root)\n                    if not _should_exclude(rel, excludes):\n                        collected.append(rel)\n        elif os.path.isfile(abs_path):\n            rel = os.path.relpath(abs_path, root)\n            if not _should_exclude(rel, excludes):\n                collected.append(rel)\n        else:\n            collected.append((\'MISSING\', path))\n    return collected\n\n\ndef _build_installer(files_dict, out_name):\n    entries = []\n    for rel, content in sorted(files_dict.items()):\n        entries.append(\'    %r: %r,\' % (rel, content))\n    payload = \'\\n\'.join(entries)\n\n    return (\n        \'# -*- coding: utf-8 -*-\\n\'\n        \'"""\\n\'\n        \'%s.py\\n\'\n        \'==========\\n\'\n        \'Self-installing pack. Run once to restore all files.\\n\'\n        \'"""\\n\'\n        \'\\n\'\n        \'import os\\n\'\n        \'\\n\'\n        \'DOCUMENTS_ROOT = %r\\n\'\n        \'\\n\'\n        \'FILES = {\\n\'\n        \'%s\\n\'\n        \'}\\n\'\n        \'\\n\'\n        \'def install(root=None):\\n\'\n        \'    if root is None:\\n\'\n        \'        root = DOCUMENTS_ROOT\\n\'\n        \'    written = 0\\n\'\n        \'    for rel, content in FILES.items():\\n\'\n        \'        abs_path = os.path.join(root, rel)\\n\'\n        \'        dir_path = os.path.dirname(abs_path)\\n\'\n        \'        if not os.path.isdir(dir_path):\\n\'\n        \'            os.makedirs(dir_path)\\n\'\n        \'        with open(abs_path, \\\'w\\\', encoding=\\\'utf-8\\\') as f:\\n\'\n        \'            f.write(content)\\n\'\n        \'        print(\\\'  wrote: \\\' + rel)\\n\'\n        \'        written += 1\\n\'\n        \'    print(\\\'\\\')\\n\'\n        \'    print(\\\'Done. %%d files written to: \\\' %% written + root)\\n\'\n\n        \'\\n\'\n        \'if __name__ == \\\'__main__\\\':\\n\'\n        \'    install()\\n\'\n    ) % (out_name, DOCUMENTS_ROOT, payload)\n\n\ndef execute(ctx, parsed_op, result):\n    root = ctx.project_root\n    target_name = (parsed_op.target or \'\').strip()\n    out_name = target_name[:-3] if target_name.endswith(\'.py\') else target_name\n\n    out_name = target_name if not target_name.endswith(\'.py\') else target_name[:-3]\n\n    include_paths = [\n        line.strip()\n        for line in (parsed_op.body or \'\').splitlines()\n        if line.strip()\n    ]\n    excludes = _parse_excludes(parsed_op)\n    exts = _parse_ext(parsed_op)\n\n    collected = _collect_files(root, include_paths, excludes, exts)\n\n    missing = [p for p in collected if isinstance(p, tuple)]\n    collected = [p for p in collected if not isinstance(p, tuple)]\n\n    files_dict = {}\n    skipped = []\n    for rel in collected:\n        abs_path = os.path.join(root, rel)\n        try:\n            with open(abs_path, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n                files_dict[rel] = f.read()\n        except Exception as e:\n            skipped.append(\'%s (%s)\' % (rel, e))\n\n    if not files_dict:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'No files collected — check your include paths\'\n        return\n\n    # Ensure packed/ dir exists\n    packed_dir = os.path.join(root, \'packed\')\n    if not os.path.isdir(packed_dir):\n        os.makedirs(packed_dir)\n\n    out_path = os.path.join(packed_dir, out_name + \'.py\')\n    installer = _build_installer(files_dict, out_name)\n\n    with open(out_path, \'w\', encoding=\'utf-8\') as f:\n        f.write(installer)\n\n    lines = [\'%d files packed to packed/%s.py\' % (len(files_dict), out_name)]\n    if missing:\n        lines.append(\'Missing: \' + \', \'.join(p for _, p in missing))\n    if skipped:\n        lines.append(\'Skipped: \' + \', \'.join(skipped))\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'\\n\'.join(lines)\n',
    'forge2/ops/pip_op.py': '# -*- coding: utf-8 -*-\n"""\nops.pip_op\n==========\nInstall or uninstall a pure-Python package from PyPI.\nDownloads a none-any wheel, extracts to site-packages,\nverifies via import, and registers in .pypi_packages.\n"""\n\nimport os\nimport json\nimport zipfile\nimport importlib\nimport configparser\nimport shutil\nfrom urllib import request\n\nSPEC = {\n    \'name\': \'PIP\',\n    \'target_kind\': \'package\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': {\'ACTION\', \'IMPORT\', \'VERSION\', \'CONFIRM\'},\n    \'required_directives\': {\'ACTION\'},\n}\n\nHELP = {\n    \'summary\': \'Install or uninstall a pure-Python package from PyPI.\',\n    \'subject\': [\'Package name as it appears on PyPI (e.g. chardet, httpx).\'],\n    \'common_failures\': [\n        \'No pure-Python wheel available - package requires compiled extensions.\',\n        \'Network unreachable.\',\n        \'Package not found on PyPI.\',\n        \'IMPORT name differs from package name - use IMPORT: directive (e.g. beautifulsoup4 -> bs4).\',\n        \'ACTION: uninstall requires CONFIRM: yes.\',\n    ],\n    \'safe_usage\': [\n        \'Install verifies success via import after extraction.\',\n        \'Already-importable packages are skipped with SKIPPED_ALREADY_PRESENT.\',\n        \'Uninstall uses .pypi_packages registry, falls back to site-packages lookup.\',\n    ],\n    \'minimal_example\': [\n        \'PIP chardet\',\n        \'ACTION: install\',\n        \'\',\n        \'PIP chardet\',\n        \'ACTION: uninstall\',\n        \'CONFIRM: yes\',\n    ],\n    \'related_ops\': [\'RUN_FILE\', \'READ_FILE\'],\n}\n\nTMP_WHEEL = os.path.join(\n    os.path.expanduser(\'~/Documents\'),\n    \'_forge_pip_tmp.whl\'\n\n)\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'PIP requires a package name as target\')\n    action = (parsed_op.directives.get(\'ACTION\') or \'\').lower()\n    if action not in (\'install\', \'uninstall\'):\n        errors.append(\'ACTION must be: install | uninstall\')\n    if action == \'uninstall\':\n        if (parsed_op.directives.get(\'CONFIRM\') or \'\').lower() != \'yes\':\n            errors.append(\'ACTION: uninstall requires CONFIRM: yes\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    action = parsed_op.directives.get(\'ACTION\', \'\').lower()\n    package = parsed_op.target.strip()\n    import_name = (parsed_op.directives.get(\'IMPORT\') or package).strip()\n\n    sp = os.path.join(ctx.project_root, \'site-packages\')\n    pypi_db = os.path.join(sp, \'.pypi_packages\')\n\n    if action == \'install\':\n        _install(result, package, import_name, sp, pypi_db)\n    else:\n        _uninstall(result, package, import_name, sp, pypi_db)\n\n\ndef _install(result, package, import_name, sp, pypi_db):\n    # Already importable?\n    importlib.invalidate_caches()\n    try:\n        importlib.import_module(import_name)\n        result[\'status\'] = \'SKIPPED_ALREADY_PRESENT\'\n        result[\'message\'] = f"\'{import_name}\' is already importable"\n        return\n    except ImportError:\n        pass\n\n    # Fetch PyPI metadata\n    try:\n        with request.urlopen(f\'https://pypi.org/pypi/{package}/json\', timeout=15) as r:\n            meta = json.loads(r.read().decode())\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = f\'PyPI fetch failed: {e}\'\n        return\n\n    info = meta[\'info\']\n    version = info[\'version\']\n    summary = info.get(\'summary\', \'\')\n\n    # Find pure-python wheel\n    wheel = None\n    for f in meta.get(\'urls\', []):\n        if f[\'filename\'].endswith(\'.whl\') and \'none-any\' in f[\'filename\']:\n            wheel = f\n            break\n\n    if not wheel:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = (\n            f"No pure-Python wheel for \'{package}\' {version}. "\n            f"Package may require compiled extensions."\n        )\n        return\n\n    # Download\n    try:\n        with request.urlopen(wheel[\'url\'], timeout=30) as r:\n            data = r.read()\n        with open(TMP_WHEEL, \'wb\') as f:\n            f.write(data)\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = f\'Download failed: {e}\'\n        return\n\n    # Extract (skip .dist-info)\n    installed = []\n    try:\n        with zipfile.ZipFile(TMP_WHEEL) as zf:\n            for name in zf.namelist():\n                if \'.dist-info/\' in name:\n                    continue\n                dest = os.path.join(sp, name)\n                os.makedirs(os.path.dirname(dest), exist_ok=True)\n                with zf.open(name) as src, open(dest, \'wb\') as dst:\n                    dst.write(src.read())\n                installed.append(dest)\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = f\'Extraction failed: {e}\'\n        return\n    finally:\n        if os.path.exists(TMP_WHEEL):\n            os.remove(TMP_WHEEL)\n\n    # Verify import\n    importlib.invalidate_caches()\n    try:\n        importlib.import_module(import_name)\n    except ImportError as e:\n        result[\'status\'] = \'FAILED_INSTALL\'\n        result[\'message\'] = f\'Extracted but import failed: {e}\'\n        return\n\n    # Register in .pypi_packages\n    cfg = configparser.ConfigParser()\n    if os.path.exists(pypi_db):\n        cfg.read(pypi_db, encoding=\'utf-8\')\n    cfg[package] = {\n        \'url\': \'pypi\',\n        \'version\': version,\n        \'summary\': summary,\n        \'files\': \',\'.join(installed),\n        \'dependency\': \'\',\n    }\n    with open(pypi_db, \'w\', encoding=\'utf-8\') as f:\n        cfg.write(f)\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = f"Installed \'{package}\' {version} ({len(installed)} files)"\n\n\ndef _uninstall(result, package, import_name, sp, pypi_db):\n    cfg = configparser.ConfigParser()\n    if os.path.exists(pypi_db):\n        cfg.read(pypi_db, encoding=\'utf-8\')\n\n    section = next((s for s in cfg.sections() if s.lower() == package.lower()), None)\n\n    removed = []\n    if section:\n        files_str = cfg[section].get(\'files\', \'\')\n        for raw in files_str.split(\',\'):\n            raw = raw.strip()\n            if not raw:\n                continue\n            if \'site-packages/\' in raw:\n                rel = raw.split(\'site-packages/\', 1)[1]\n                abs_path = os.path.join(sp, rel)\n            else:\n                abs_path = raw\n\n            if os.path.isfile(abs_path):\n                os.remove(abs_path)\n                removed.append(abs_path)\n            elif os.path.isdir(abs_path):\n                shutil.rmtree(abs_path)\n                removed.append(abs_path)\n\n        cfg.remove_section(section)\n        with open(pypi_db, \'w\', encoding=\'utf-8\') as f:\n            cfg.write(f)\n\n    else:\n        pkg_dir = os.path.join(sp, import_name)\n        pkg_file = os.path.join(sp, import_name + \'.py\')\n        if os.path.isdir(pkg_dir):\n            shutil.rmtree(pkg_dir)\n            removed.append(pkg_dir)\n        elif os.path.isfile(pkg_file):\n            os.remove(pkg_file)\n            removed.append(pkg_file)\n        else:\n            result[\'status\'] = \'FAILED_NOT_FOUND\'\n            result[\'message\'] = f"\'{package}\' not found in .pypi_packages or site-packages"\n            return\n\n    # Evict from module cache so same-session import checks reflect reality\n    import sys\n    sys.modules.pop(import_name, None)\n\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = f"Uninstalled \'{package}\' ({len(removed)} items removed)"\n',
    'forge2/ops/prepend_into_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.prepend_into_ast\n====================\nPrepend code at start of AST target body.\n"""\n\nSPEC = {\n    \'name\': \'PREPEND_INTO\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Prepend code at the start of a resolved AST target body.\',\n    \'subject\': [\n        \'AST target string, for example file.py::Class.method_name.\',\n        \'Best for adding setup code near the top of a function, method, or class body.\',\n    ],\n    \'common_failures\': [\n        \'Target does not resolve to a real AST target.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n    ],\n    \'safe_usage\': [\n        \'Use PREVIEW first if you want to confirm where the body begins.\',\n        \'Use this when a simple start-of-body insertion is cleaner than anchor matching.\',\n    ],\n    \'minimal_example\': [\n        \'PREPEND_INTO forge_app_lab/forge_app_state.py::build_starter_bundle\',\n        \'BEGIN_BODY\',\n        "subject = target or file_rel or \'\'",\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW\', \'APPEND_INTO\', \'INSERT_INTO\'],\n}\n\nHINTS = {\n    \'body required\': \'Provide the code to prepend as the body\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'PREPEND_INTO requires a target\')\n    if not parsed_op.body:\n        errors.append(\'PREPEND_INTO requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n    from forge2.source_ops import insert_after_lines, get_line_indent\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    insert_line = resolved[\'start\']\n    ref_line = before.splitlines(True)[resolved[\'start\'] - 1]\n    indent = get_line_indent(ref_line) + \'    \'\n\n    patched = insert_after_lines(\n        before,\n        insert_line,\n        parsed_op.body,\n        indent,\n        tight=True,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': patched,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'prepended into %s lines %d-%d\' % (\n        resolved[\'kind\'],\n        resolved[\'start\'],\n        resolved[\'end\'],\n    )\n',
    'forge2/ops/preview_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.preview_ast\n===============\nAST preview op.\n"""\n\nSPEC = {\n    \'name\': \'PREVIEW\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Preview a resolved AST target with numbered source lines.\',\n    \'subject\': [\n        \'AST target string, for example file.py::Class.method_name.\',\n        \'Use @name for assignments and Class.* for a whole class.\',\n    ],\n    \'common_failures\': [\n        \'Target does not resolve to a real AST target.\',\n        \'Using a plain file path instead of AST target syntax.\',\n    ],\n    \'safe_usage\': [\n        \'Use this before AST patch ops when you are not fully sure of current content.\',\n        \'Use LIST_TARGETS first if you do not know the exact target name.\',\n    ],\n    \'minimal_example\': [\n        \'PREVIEW forge_app_lab/forge_app_state.py::build_starter_bundle\',\n    ],\n    \'related_ops\': [\'LIST_TARGETS\', \'REPLACE\', \'INSERT_INTO\'],\n}\n\nHINTS = {\n    \'nonetype\': \'PREVIEW requires an AST target — use file.py::Class.method syntax. For plain files use PREVIEW_FILE instead\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n    \'attribute\': \'PREVIEW requires an AST target — use file.py::Class.method syntax. For plain files use PREVIEW_FILE instead\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'PREVIEW requires a target\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.target_resolver import resolve_ast_target\n\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    src = resolved[\'source_text\']\n    lines = src.splitlines()\n    start = resolved[\'start\']\n    end = resolved[\'end\']\n\n    snippet = []\n    for i in range(start, end + 1):\n        snippet.append(\'%04d: %s\' % (i, lines[i - 1]))\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%s lines %d-%d\' % (resolved[\'kind\'], start, end)\n    result[\'preview\'] = (\n        parsed_op.target + \' [%s lines %d-%d]\\n\' % (resolved[\'kind\'], start, end)\n        + \'\\n\'.join(snippet)\n    )\n',
    'forge2/ops/preview_file.py': '# -*- coding: utf-8 -*-\n"""\nops.preview_file\n================\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'PREVIEW_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'ANCHOR\', \'CONTEXT\', \'LINES\', \'MATCH\']),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Preview a file or a specific line range with numbered lines.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'File not found.\',\n        \'Target escapes project root.\',\n        \'For exact line spans, use LINES: start-end.\',\n    ],\n    \'safe_usage\': [\n        \'Prefer this over READ_FILE when you only need a slice of a larger file.\',\n        \'Use this before REPLACE_FILE_RANGE to confirm exact lines.\',\n    ],\n    \'minimal_example\': [\n        \'PREVIEW_FILE forge_app.py\',\n        \'LINES: 12-20\',\n    ],\n    \'related_ops\': [\'READ_FILE\', \'REPLACE_FILE_RANGE\', \'REPLACE_FILE_LINES\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'PREVIEW_FILE requires a target path\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        content = f.read()\n\n    lines = content.splitlines()\n    lines_range = parsed_op.directives.get(\'LINES\')\n\n    if lines_range:\n        start, end = lines_range\n        start = max(1, start)\n        end = min(len(lines), end)\n        numbered = [\'%04d: %s\' % (i, lines[i - 1]) for i in range(start, end + 1)]\n        result[\'preview\'] = parsed_op.target + \' [lines %d-%d]\\n\' % (start, end) + \'\\n\'.join(numbered)\n        result[\'status\'] = \'APPLIED\'\n        result[\'message\'] = \'Lines %d-%d\' % (start, end)\n        result[\'file\'] = parsed_op.target\n        return\n\n    numbered = [\'%04d: %s\' % (i + 1, ln) for i, ln in enumerate(lines)]\n    result[\'preview\'] = parsed_op.target + \' [%d lines]\\n\' % len(numbered) + \'\\n\'.join(numbered)\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d lines\' % len(numbered)\n    result[\'file\'] = parsed_op.target\n',
    'forge2/ops/read_file.py': '# -*- coding: utf-8 -*-\n"""\nops.read_file\n=============\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'READ_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Read a full text file and return numbered lines.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'File not found.\',\n        \'Target escapes project root.\',\n    ],\n    \'safe_usage\': [\n        \'Use PREVIEW_FILE when you only need a line range.\',\n        \'Prefer this for small files or when full context matters.\',\n    ],\n    \'minimal_example\': [\n        \'READ_FILE forge2/registry.py\',\n    ],\n    \'related_ops\': [\'PREVIEW_FILE\', \'LIST_FILES\', \'GREP\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'READ_FILE requires a target path\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        content = f.read()\n\n    numbered = [\'%04d: %s\' % (i + 1, ln) for i, ln in enumerate(content.splitlines())]\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%d lines\' % len(numbered)\n    result[\'preview\'] = parsed_op.target + \' [%d lines]\\n\' % len(numbered) + \'\\n\'.join(numbered)\n',
    'forge2/ops/replace_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_ast\n===============\nAST whole-target replace op.\n"""\n\nfrom forge2.target_resolver import resolve_ast_target\nfrom forge2.source_ops import replace_line_range\n\nSPEC = {\n    \'name\': \'REPLACE\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Replace an entire resolved AST target such as a method, function, class, or assignment.\',\n    \'body\': [\n        \'Wrap replacement code in BEGIN_BODY / END_BODY.\',\n        \'The body must include the full def or assignment — do not omit the signature.\',\n    ],\n    \'subject\': [\n        \'AST target string, for example file.py::Class.method_name.\',\n        \'Use @name for assignments and Class.* for a whole class.\',\n    ],\n    \'common_failures\': [\n        \'Target does not resolve to a real AST target.\',\n        \'Missing BEGIN_BODY / END_BODY — always required.\',\n        \'Attempting to patch loose top-level code that is not an AST target.\',\n    ],\n    \'safe_usage\': [\n        \'Prefer this over anchor-based AST edits when replacing a whole target.\',\n        \'Use PREVIEW first if current content may have drifted.\',\n        \'This is usually the safest AST write op for large edits.\',\n    ],\n    \'minimal_example\': [\n        \'REPLACE forge_app_lab/forge_app_state.py::build_starter_bundle\',\n        \'BEGIN_BODY\',\n        \'def build_starter_bundle(op_name, file_rel=None, target=None):\',\n        "    return \'%s %s\\\\n\' % (op_name, target or file_rel or \'\')",\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW\', \'REPLACE_LINE\', \'REPLACE_LINES\', \'INSERT_INTO\'],\n}\n\nHINTS = {\n    \'body required\': \'Provide the full replacement function or class as the body\',\n    \'not found\': \'Target not found — run LIST_TARGETS on the file to see valid targets\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE requires a target\')\n    if not parsed_op.body:\n        errors.append(\'REPLACE requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    after = replace_line_range(\n        before,\n        resolved[\'start\'],\n        resolved[\'end\'],\n        parsed_op.body,\n    )\n\n    if after == before:\n        result[\'file\'] = resolved[\'file_ref\']\n        result[\'status\'] = \'SKIPPED_ALREADY_APPLIED\'\n        result[\'message\'] = \'%s lines %d-%d unchanged\' % (\n            resolved[\'kind\'],\n            resolved[\'start\'],\n            resolved[\'end\'],\n        )\n        return\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'%s lines %d-%d replaced\' % (\n        resolved[\'kind\'],\n        resolved[\'start\'],\n        resolved[\'end\'],\n    )\n',
    'forge2/ops/replace_file.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_file\n================\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'REPLACE_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Replace the full contents of an existing text file.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'File not found.\',\n        \'Target escapes project root.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n    ],\n    \'safe_usage\': [\n        \'Use this for full rewrites of docs, JSON, Markdown, or other plain text files.\',\n        \'Prefer AST ops for Python when you only need to change a specific target.\',\n        \'Preview or read the file first if you are not intentionally overwriting everything.\',\n    ],\n    \'minimal_example\': [\n        \'REPLACE_FILE docs/example.txt\',\n        \'BEGIN_BODY\',\n        \'hello world\',\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'READ_FILE\', \'PREVIEW_FILE\', \'REPLACE_FILE_LINES\', \'REPLACE_FILE_RANGE\'],\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE_FILE requires a target path\')\n    if not parsed_op.body:\n        errors.append(\'REPLACE_FILE requires body content\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        before = f.read()\n\n    after = parsed_op.body\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Replaced file: \' + parsed_op.target\n',
    'forge2/ops/replace_file_lines.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_file_lines\n======================\nAnchor-based line range replacement in plain text files.\n\nDirectives:\n    ANCHOR_START    required - substring to find start line\n    ANCHOR_END      required - substring to find end line\n    MATCH           exact|fuzzy (default: exact)\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'REPLACE_FILE_LINES\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'optional\',\n\n    \'allowed_directives\': {\'ANCHOR_START\', \'ANCHOR_END\', \'MATCH\'},\n    \'required_directives\': {\'ANCHOR_START\', \'ANCHOR_END\'},\n}\n\nHELP = {\n    \'summary\': \'Replace a block in a plain text file using start and end anchors. Requires ANCHOR_START, ANCHOR_END, and a BEGIN_BODY / END_BODY block — all three together.\',\n    \'body\': [\n        \'Wrap replacement lines in BEGIN_BODY / END_BODY.\',\n        \'The body replaces everything from ANCHOR_START to ANCHOR_END inclusive.\',\n    ],\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'Missing ANCHOR_START or ANCHOR_END.\',\n        \'Anchors match zero times or more than once.\',\n        \'ANCHOR_END appears before ANCHOR_START.\',\n        \'Missing BEGIN_BODY / END_BODY — always required alongside the anchors.\',\n    ],\n    \'safe_usage\': [\n        \'Use this when line numbers are unstable but text anchors are reliable.\',\n        \'Pick unique anchors — PREVIEW_FILE first to confirm exact text.\',\n        \'All three parts are required: ANCHOR_START, ANCHOR_END, and BEGIN_BODY / END_BODY.\',\n    ],\n    \'minimal_example\': [\n        \'REPLACE_FILE_LINES llm_workspace/memory.py\',\n        \'ANCHOR_START: FLAGS = [\',\n        \'ANCHOR_END: ]\',\n        \'BEGIN_BODY\',\n        \'FLAGS = [\',\n        "    \'example flag\',",\n        \']\',\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW_FILE\', \'REPLACE_FILE\', \'REPLACE_FILE_RANGE\'],\n}\n\nHINTS = {\n    \'ANCHOR_START\': \'Add ANCHOR_START: <text> — unique substring marking the start line\',\n    \'ANCHOR_END\': \'Add ANCHOR_END: <text> — unique substring marking the end line\',\n    \'body required\': \'Wrap replacement lines in BEGIN_BODY / END_BODY\',\n\n    \'target\': \'Provide a relative file path as the target\',\n    \'ANCHOR_END appears before\': \'ANCHOR_END must appear after ANCHOR_START in the file\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE_FILE_LINES requires a target\')\n    if \'ANCHOR_START\' not in parsed_op.directives:\n        errors.append(\'REPLACE_FILE_LINES requires ANCHOR_START\')\n    if \'ANCHOR_END\' not in parsed_op.directives:\n        errors.append(\'REPLACE_FILE_LINES requires ANCHOR_END\')\n    if not parsed_op.body:\n        pass  # empty body allowed — BEGIN_BODY/END_BODY with no content deletes the range\n\n    return errors\n\n\ndef _normalize(s):\n    return \' \'.join((s or \'\').strip().split())\n\n\ndef _find_anchor(lines, anchor, match_mode):\n    needle = anchor or \'\'\n    hits = []\n    for idx, line in enumerate(lines):\n        if match_mode == \'fuzzy\':\n            if _normalize(needle) in _normalize(line):\n                hits.append(idx)\n        else:\n            if needle in line:\n                hits.append(idx)\n    return hits\n\n\ndef execute(ctx, parsed_op, result):\n    target_raw = (parsed_op.target or \'\').strip()\n    file_abs = os.path.realpath(os.path.join(ctx.project_root, target_raw))\n\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + target_raw\n        return\n\n    src = ctx.file_cache.get(file_abs)\n    if src is None:\n        with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n            src = f.read()\n\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    anchor_start = parsed_op.directives.get(\'ANCHOR_START\', \'\')\n    anchor_end = parsed_op.directives.get(\'ANCHOR_END\', \'\')\n\n    lines = src.splitlines()\n\n    hits_s = _find_anchor(lines, anchor_start, match_mode)\n    hits_e = _find_anchor(lines, anchor_end, match_mode)\n\n    if len(hits_s) != 1:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR_START %r matched %d times, expected 1\' % (anchor_start, len(hits_s))\n        return\n    if len(hits_e) != 1:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR_END %r matched %d times, expected 1\' % (anchor_end, len(hits_e))\n        return\n\n    line_s = hits_s[0]\n    line_e = hits_e[0]\n\n    if line_e < line_s:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'ANCHOR_END appears before ANCHOR_START\'\n        return\n\n    replacement = (parsed_op.body or \'\').splitlines()\n    new_lines = lines[:line_s] + replacement + lines[line_e + 1:]\n    patched = \'\\n\'.join(new_lines)\n    if src.endswith(\'\\n\'):\n        patched += \'\\n\'\n\n    if patched == src:\n        result[\'status\'] = \'SKIPPED_ALREADY_APPLIED\'\n        result[\'message\'] = \'No change\'\n        return\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\'before\': src, \'after\': patched}\n\n    result[\'file\'] = os.path.relpath(file_abs, ctx.project_root)\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Lines %d-%d replaced\' % (line_s + 1, line_e + 1)\n',
    'forge2/ops/replace_file_range.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_file_range\n======================\n"""\n\nimport os\n\nSPEC = {\n    \'name\': \'REPLACE_FILE_RANGE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'required\',\n    \'allowed_directives\': set([\'LINES\']),\n    \'required_directives\': set([\'LINES\']),\n}\n\nHELP = {\n    \'summary\': \'Replace an explicit line range in a text file.\',\n    \'subject\': [\'Relative file path inside project root.\'],\n    \'common_failures\': [\n        \'Use LINES: start-end, not START_LINE / END_LINE.\',\n        \'LINES out of range for the current file.\',\n        \'Missing BEGIN_BODY / END_BODY.\',\n    ],\n    \'safe_usage\': [\n        \'PREVIEW_FILE the target range first.\',\n        \'Verify the result after patching.\',\n        \'Use this for text files or exact top-of-file cleanup when AST patching is not appropriate.\',\n    ],\n    \'minimal_example\': [\n        \'REPLACE_FILE_RANGE forge_app.py\',\n        \'LINES: 12-23\',\n        \'BEGIN_BODY\',\n        "DOCUMENTS_ROOT = \'/path/to/Documents\'",\n        \'END_BODY\',\n    ],\n    \'related_ops\': [\'PREVIEW_FILE\', \'REPLACE_FILE\', \'REPLACE_FILE_LINES\'],\n}\n\nHINTS = {\n    \'lines\': \'Add LINES: start-end — e.g. LINES: 10-20. Use PREVIEW_FILE first to confirm line numbers\',\n    \'body required\': \'Wrap replacement lines in BEGIN_BODY / END_BODY\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE_FILE_RANGE requires a target path\')\n    if not parsed_op.body:\n        errors.append(\'REPLACE_FILE_RANGE requires body content\')\n\n    lines_range = parsed_op.directives.get(\'LINES\')\n    if not isinstance(lines_range, tuple) or len(lines_range) != 2:\n        errors.append(\'REPLACE_FILE_RANGE requires LINES: start-end\')\n    else:\n        a, b = lines_range\n        if a < 1 or b < a:\n            errors.append(\'Invalid LINES range\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = ctx.resolve_file(parsed_op.target)\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        before = f.read()\n\n    src_lines = before.splitlines()\n    total = len(src_lines)\n\n    start, end = parsed_op.directives.get(\'LINES\')\n    if end > total:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'LINES out of range: file has %d lines\' % total\n        return\n\n    new_lines = parsed_op.body.splitlines()\n    out_lines = src_lines[:start - 1] + new_lines + src_lines[end:]\n    after = \'\\n\'.join(out_lines)\n    if before.endswith(\'\\n\') or parsed_op.body.endswith(\'\\n\'):\n        after += \'\\n\'\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = parsed_op.target\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'Replaced lines %d-%d\' % (start, end)\n',
    'forge2/ops/replace_line_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_line_ast\n====================\nSingle-line AST replacement.\n"""\n\nfrom forge2.target_resolver import resolve_ast_target\nfrom forge2.source_ops import replace_line_range\nfrom forge2.anchor_tools import resolve_single_anchor\n\nSPEC = {\n    \'name\': \'REPLACE_LINE\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'optional\',\n\n    \'allowed_directives\': set([\'ANCHOR\', \'MATCH\', \'OCCURRENCE\', \'EXPECT\']),\n    \'required_directives\': set([\'ANCHOR\']),\n    "summary": \'Replace a single line inside an AST target matched by an ANCHOR substring.\',\n}\n\nHINTS = {\n    \'anchor\': \'Add ANCHOR: <text> — unique substring on the line to replace\',\n    \'body required\': \'Provide the replacement line as the body (no BEGIN_BODY needed)\',\n    \'matched 0\': \'Anchor not found — use PREVIEW to check exact text in target\',\n    \'matched 2\': \'Anchor matches more than once — add OCCURRENCE: N or use a more specific anchor\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE_LINE requires a target\')\n    if not parsed_op.body:\n        pass  # empty body allowed — BEGIN_BODY/END_BODY with no content deletes the line\n\n    if \'ANCHOR\' not in parsed_op.directives:\n        errors.append(\'Missing required directive for REPLACE_LINE: ANCHOR\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    all_lines = before.splitlines()\n    start = resolved[\'start\']\n    end = resolved[\'end\']\n    target_lines = all_lines[start - 1:end]\n\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    occurrence = parsed_op.directives.get(\'OCCURRENCE\', 1)\n    expect = parsed_op.directives.get(\'EXPECT\', 1)\n\n    try:\n        occurrence = int(occurrence)\n    except Exception:\n        occurrence = 1\n    try:\n        expect = int(expect)\n    except Exception:\n        expect = 1\n\n    rel_idx, err = resolve_single_anchor(\n        target_lines,\n        parsed_op.directives.get(\'ANCHOR\', \'\'),\n        match_mode=match_mode,\n        occurrence=occurrence,\n        expect=expect,\n    )\n    if err:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR: \' + err\n        return\n\n    abs_line = start + rel_idx\n\n    after = replace_line_range(\n        before,\n        abs_line,\n        abs_line,\n        parsed_op.body,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'line %d replaced inside %s\' % (abs_line, resolved[\'kind\'])\n',
    'forge2/ops/replace_lines_ast.py': '# -*- coding: utf-8 -*-\n"""\nops.replace_lines_ast\n=====================\nAnchor-based AST range replacement.\n"""\n\nfrom forge2.target_resolver import resolve_ast_target\nfrom forge2.source_ops import replace_line_range\nfrom forge2.anchor_tools import resolve_single_anchor\n\nSPEC = {\n    \'name\': \'REPLACE_LINES\',\n    \'target_kind\': \'ast\',\n    \'body_mode\': \'optional\',\n\n    \'allowed_directives\': set([\'ANCHOR_START\', \'ANCHOR_END\', \'MATCH\', \'OCCURRENCE\', \'EXPECT\']),\n    \'required_directives\': set([\'ANCHOR_START\', \'ANCHOR_END\']),\n    "summary": \'Replace a range of lines inside an AST target between ANCHOR_START and ANCHOR_END.\',\n}\n\nHINTS = {\n    \'anchor_start\': \'Add ANCHOR_START: <text> — unique substring marking the start line\',\n    \'anchor_end\': \'Add ANCHOR_END: <text> — unique substring marking the end line\',\n    \'body required\': \'Wrap replacement lines in BEGIN_BODY / END_BODY\',\n    \'anchor_end appears before\': \'ANCHOR_END must appear after ANCHOR_START in the target\',\n    \'matched 0\': \'Anchor not found — use PREVIEW to check exact text in target\',\n    \'matched 2\': \'Anchor is ambiguous — use more context or a more specific anchor\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REPLACE_LINES requires a target\')\n    if not parsed_op.body:\n        pass  # empty body allowed — BEGIN_BODY/END_BODY with no content deletes the range\n\n    if \'ANCHOR_START\' not in parsed_op.directives:\n        errors.append(\'Missing required directive for REPLACE_LINES: ANCHOR_START\')\n    if \'ANCHOR_END\' not in parsed_op.directives:\n        errors.append(\'Missing required directive for REPLACE_LINES: ANCHOR_END\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    resolved = resolve_ast_target(\n        ctx.project_root,\n        parsed_op.target,\n        default_file=parsed_op.default_file,\n    )\n\n    if not resolved.get(\'ok\'):\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = resolved.get(\'error\') or \'Target not found\'\n        return\n\n    file_abs = resolved[\'file_abs\']\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    before = resolved[\'source_text\']\n    all_lines = before.splitlines()\n    start = resolved[\'start\']\n    end = resolved[\'end\']\n\n    target_lines = all_lines[start - 1:end]\n\n    match_mode = parsed_op.directives.get(\'MATCH\', \'exact\')\n    occurrence = parsed_op.directives.get(\'OCCURRENCE\', 1)\n    expect = parsed_op.directives.get(\'EXPECT\', 1)\n\n    try:\n        occurrence = int(occurrence)\n    except Exception:\n        occurrence = 1\n    try:\n        expect = int(expect)\n    except Exception:\n        expect = 1\n\n    start_idx, err = resolve_single_anchor(\n        target_lines,\n        parsed_op.directives.get(\'ANCHOR_START\', \'\'),\n        match_mode=match_mode,\n        occurrence=occurrence,\n        expect=expect,\n    )\n    if err:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR_START: \' + err\n        return\n\n    end_idx, err = resolve_single_anchor(\n        target_lines,\n        parsed_op.directives.get(\'ANCHOR_END\', \'\'),\n        match_mode=match_mode,\n        occurrence=occurrence,\n        expect=expect,\n    )\n    if err:\n        result[\'status\'] = \'SKIPPED_ANCHOR_MISMATCH\'\n        result[\'message\'] = \'ANCHOR_END: \' + err\n        return\n\n    if end_idx < start_idx:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'ANCHOR_END occurs before ANCHOR_START\'\n        return\n\n    abs_start = start + start_idx\n    abs_end = start + end_idx\n\n    after = replace_line_range(\n        before,\n        abs_start,\n        abs_end,\n        parsed_op.body,\n    )\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n\n        f.write(after)\n\n    ctx.file_cache[file_abs] = after\n    ctx.touched_files[file_abs] = {\n        \'before\': before,\n        \'after\': after,\n    }\n\n    result[\'file\'] = resolved[\'file_ref\']\n    result[\'status\'] = \'APPLIED\'\n    result[\'message\'] = \'lines %d-%d replaced inside %s\' % (abs_start, abs_end, resolved[\'kind\'])\n',
    'forge2/ops/revert_run.py': '# -*- coding: utf-8 -*-\n"""\nops.revert_run\n==============\nRevert a previous forge2 run by stamp.\n\nUsage:\n    REVERT_RUN 20260324_165056\n"""\n\nSPEC = {\n    \'name\': \'REVERT_RUN\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'none\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Restore files to their pre-run state using snapshots from a previous run.\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'REVERT_RUN requires a run stamp as target\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    from forge2.run_storage import revert_run\n    stamp = (parsed_op.target or \'\').strip()\n    ok, msg = revert_run(ctx.project_root, stamp)\n    result[\'status\'] = \'APPLIED\' if ok else \'FAILED_IO\'\n    result[\'message\'] = msg\n',
    'forge2/ops/run_file.py': '# -*- coding: utf-8 -*-\n"""\nops.run_file\n============\nExecute a Python file under project root and capture stdout/stderr.\n"""\n\nimport io\nimport os\nimport sys\nimport traceback\nimport threading\n\nfrom forge2.file_ops import resolve_under_root, in_root\n\nSPEC = {\n    \'name\': \'RUN_FILE\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': set([\'ARGS\']),\n    \'required_directives\': set(),\n}\n\nHELP = {\n    \'summary\': \'Execute a Python file under project root and capture stdout and stderr.\',\n    \'subject\': [\'Relative Python file path inside project root.\'],\n    \'common_failures\': [\n        \'File not found.\',\n        \'Target escapes project root.\',\n        \'Script exits non-zero.\',\n        \'Execution timeout after 20 seconds.\',\n    ],\n    \'safe_usage\': [\n        \'Use this for probes and verification after patches.\',\n        \'When testing imported modules after patching, remember module cache can affect results.\',\n        \'Use ARGS when the script expects command-line parameters.\',\n    ],\n    \'minimal_example\': [\n        \'RUN_FILE test_probe.py\',\n        \'ARGS: alpha beta\',\n    ],\n    \'related_ops\': [\'READ_FILE\', \'PREVIEW_FILE\', \'DIFF\'],\n}\n\nHINTS = {\n    \'target\': \'Provide a relative path to the .py file to run — e.g. RUN_FILE utils/myscript.py\',\n    \'not found\': \'File not found — check the path is relative to project root\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'RUN_FILE requires a target path\')\n    return errors\n\n\ndef _run_code(file_abs, argv, capture):\n    old_argv = sys.argv[:]\n    old_stdout = sys.stdout\n    old_stderr = sys.stderr\n    ns = {\n        \'__name__\': \'__main__\',\n        \'__file__\': file_abs,\n        \'__package__\': None,\n    }\n\n    out_buf = io.StringIO()\n    err_buf = io.StringIO()\n\n    try:\n        sys.argv = argv[:]\n        sys.stdout = out_buf\n        sys.stderr = err_buf\n\n        with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n            code = f.read()\n\n        compiled = compile(code, file_abs, \'exec\')\n        exec(compiled, ns, ns)\n        capture[\'exit_code\'] = 0\n    except SystemExit as e:\n        code = e.code\n        if isinstance(code, int):\n            capture[\'exit_code\'] = code\n        else:\n            capture[\'exit_code\'] = 1\n            if code not in (None, \'\'):\n                print(code, file=err_buf)\n    except Exception:\n        capture[\'exit_code\'] = 1\n        traceback.print_exc(file=err_buf)\n    finally:\n        capture[\'stdout\'] = out_buf.getvalue()\n        capture[\'stderr\'] = err_buf.getvalue()\n        sys.argv = old_argv\n        sys.stdout = old_stdout\n        sys.stderr = old_stderr\n\n\ndef execute(ctx, parsed_op, result):\n    file_abs = resolve_under_root(ctx.project_root, parsed_op.target)\n\n    if not in_root(ctx.project_root, file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = \'File not found: \' + parsed_op.target\n        return\n\n    raw_args = (parsed_op.directives.get(\'ARGS\') or \'\').strip()\n    extra_args = raw_args.split() if raw_args else []\n    argv = [file_abs] + extra_args\n\n    capture = {\n        \'exit_code\': None,\n        \'stdout\': \'\',\n        \'stderr\': \'\',\n    }\n\n    t = threading.Thread(target=_run_code, args=(file_abs, argv, capture))\n    t.daemon = True\n    t.start()\n    t.join(_TIMEOUT_SECONDS)\n\n    if t.is_alive():\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'timeout after %ss\' % _TIMEOUT_SECONDS\n        result[\'preview\'] = \'RUN_FILE %s [timeout]\' % parsed_op.target\n        result[\'file\'] = parsed_op.target\n        return\n\n    exit_code = capture.get(\'exit_code\')\n    stdout = capture.get(\'stdout\') or \'\'\n    stderr = capture.get(\'stderr\') or \'\'\n\n    result[\'file\'] = parsed_op.target\n    result[\'preview\'] = _format_preview(parsed_op.target, exit_code, stdout, stderr)\n\n    if exit_code == 0:\n        result[\'status\'] = \'APPLIED\'\n        result[\'message\'] = \'exit 0\'\n    else:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'exit %s\' % exit_code\n\n\ndef _format_preview(target, exit_code, stdout, stderr):\n    lines = [\'RUN_FILE %s [exit %s]\' % (target, exit_code)]\n    if stdout:\n        lines.append(\'--- stdout ---\')\n        lines.append(stdout.rstrip(\'\\n\'))\n    if stderr:\n        lines.append(\'--- stderr ---\')\n        lines.append(stderr.rstrip(\'\\n\'))\n    return \'\\n\'.join(lines).rstrip() + \'\\n\'\n\n\n_TIMEOUT_SECONDS = 20\n',
    'forge2/ops/set_dict_key.py': '# -*- coding: utf-8 -*-\n"""\nops.set_dict_key\n================\nSet or replace a single key in a module-level dict assignment.\n\nTarget: file.py::@var_name\nDirectives:\n    KEY:   the key to set (string)\n    VALUE: the value as a Python literal (string, number, list, etc.)\n"""\n\nSPEC = {\n    \'name\': \'SET_DICT_KEY\',\n    \'target_kind\': \'raw\',\n    \'body_mode\': \'forbidden\',\n    \'allowed_directives\': {\'KEY\', \'VALUE\'},\n    \'required_directives\': {\'KEY\', \'VALUE\'},\n}\n\nHELP = {\n    \'summary\': \'Set or replace a single key in a module-level dict assignment.\',\n    \'subject\': [\n        \'file.py::@var_name — must point to a module-level dict.\',\n    ],\n    \'common_failures\': [\n        \'Target is not a dict assignment.\',\n        \'KEY or VALUE directive missing.\',\n        \'VALUE is not a valid Python literal.\',\n    ],\n    \'safe_usage\': [\n        \'Use for updating single keys in memory.py dicts like LAST_SESSION.\',\n        \'VALUE must be a valid Python literal: string in quotes, number, list, etc.\',\n    ],\n    \'minimal_example\': [\n        \'SET_DICT_KEY llm_workspace/memory.py::@SESSION_GOAL\',\n        \'KEY: today\',\n        \'VALUE: "Trig tile rework"\',\n    ],\n    \'related_ops\': [\'APPEND_LIST\', \'REPLACE\', \'PREVIEW_FILE\'],\n}\n\nHINTS = {\n    \'target\': \'Provide file.py::@dict_name — the @ prefix targets a module-level dict assignment\',\n    \'key\': \'Add KEY: <name> — the dict key to set\',\n    \'body required\': \'Provide the value as the body\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'SET_DICT_KEY requires a target (file.py::@var_name)\')\n    if not parsed_op.directives.get(\'KEY\'):\n        errors.append(\'SET_DICT_KEY requires KEY directive\')\n    if \'VALUE\' not in parsed_op.directives:\n        errors.append(\'SET_DICT_KEY requires VALUE directive\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    import ast, os, re\n\n    target = (parsed_op.target or \'\').strip()\n\n    if \'::@\' not in target:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = \'Target must be file.py::@var_name\'\n        return\n\n    file_ref, var_name = target.split(\'::@\', 1)\n    file_ref = file_ref.strip()\n    var_name = var_name.strip()\n    key_name = parsed_op.directives.get(\'KEY\', \'\').strip()\n    value_str = parsed_op.directives.get(\'VALUE\', \'\').strip()\n\n    # Validate value is a Python literal\n    try:\n        ast.literal_eval(value_str)\n    except Exception:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = f\'VALUE is not a valid Python literal: {value_str!r}\'\n        return\n\n    file_abs = os.path.normpath(os.path.join(ctx.project_root, file_ref))\n    if not ctx.in_root(file_abs):\n        result[\'status\'] = \'FAILED_INVALID_PATH\'\n        result[\'message\'] = \'Target escapes project root\'\n        return\n\n    if not os.path.isfile(file_abs):\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = f\'File not found: {file_ref}\'\n        return\n\n    with open(file_abs, \'r\', encoding=\'utf-8\') as f:\n        source = f.read()\n\n    try:\n        tree = ast.parse(source)\n    except SyntaxError as e:\n        result[\'status\'] = \'FAILED_PARSE\'\n        result[\'message\'] = f\'File parse error: {e}\'\n        return\n\n    # Find the dict assignment\n    target_node = None\n    for node in ast.walk(tree):\n        if isinstance(node, ast.Assign):\n            for t in node.targets:\n                if isinstance(t, ast.Name) and t.id == var_name:\n                    if isinstance(node.value, ast.Dict):\n                        target_node = node\n\n    if target_node is None:\n        result[\'status\'] = \'FAILED_NOT_FOUND\'\n        result[\'message\'] = f\'No module-level dict assignment @{var_name} found\'\n        return\n\n    lines = source.splitlines(True)\n\n    # Check if key already exists in the dict\n    existing_key_node = None\n    for k, v in zip(target_node.value.keys, target_node.value.values):\n        if isinstance(k, ast.Constant) and str(k.value) == key_name:\n            existing_key_node = (k, v)\n            break\n\n    if existing_key_node:\n        # Replace the value on the value node\'s line\n        k_node, v_node = existing_key_node\n        line_idx   = v_node.lineno - 1\n        line       = lines[line_idx]\n        col_start  = v_node.col_offset\n        col_end    = v_node.end_col_offset\n        lines[line_idx] = line[:col_start] + value_str + line[col_end:]\n        action = \'updated\'\n    else:\n        # Insert new key before closing }\n        end_line_idx = target_node.end_lineno - 1\n        close_idx    = end_line_idx\n        while close_idx >= 0:\n            stripped = lines[close_idx].rstrip()\n            if stripped.endswith(\'}\') or stripped == \'}\':\n                break\n            close_idx -= 1\n\n        if close_idx < 0:\n            result[\'status\'] = \'FAILED_NOT_FOUND\'\n            result[\'message\'] = f\'Could not locate closing }} for @{var_name}\'\n            return\n\n        # Detect indent from existing dict content\n        indent = \'    \'\n        for line in lines[target_node.lineno: close_idx]:\n            s = line.lstrip()\n            if s:\n                indent = line[: len(line) - len(s)]\n                break\n\n        insert_text = f\'{indent}"{key_name}": {value_str},\\n\'\n        lines.insert(close_idx, insert_text)\n        action = \'inserted\'\n\n    patched = \'\'.join(lines)\n\n    try:\n        compile(patched, file_abs, \'exec\')\n    except SyntaxError as e:\n        result[\'status\'] = \'FAILED_COMPILE\'\n        result[\'message\'] = f\'Compile failed after patch: {e}\'\n        return\n\n    with open(file_abs, \'w\', encoding=\'utf-8\') as f:\n        f.write(patched)\n\n    ctx.file_cache[file_abs] = patched\n    ctx.touched_files[file_abs] = {\'before\': source, \'after\': patched}\n\n    result[\'file\']    = file_ref\n    result[\'status\']  = \'APPLIED\'\n    result[\'message\'] = f\'{action} key "{key_name}" in @{var_name}\'\n',
    'forge2/ops/shortcut.py': '# -*- coding: utf-8 -*-\n"""\nops.shortcut\n============\nFire an iOS Shortcut by name.\n\nUsage:\n    SHORTCUT My Shortcut Name\n\nFires the Shortcut via the shortcuts:// URL scheme.\nFire and forget - no return value captured.\n"""\n\nimport webbrowser\ntry:\n    from urllib.parse import quote\nexcept ImportError:\n    from urllib import quote\n\nSPEC = {\n    \'name\': \'SHORTCUT\',\n    \'target_kind\': \'file\',\n    \'body_mode\': \'none\',\n    \'allowed_directives\': set(),\n    \'required_directives\': set(),\n    "summary": \'Fire a named iOS Shortcut by name.\',\n}\n\n\ndef validate(parsed_op):\n    errors = []\n    if not (parsed_op.target or \'\').strip():\n        errors.append(\'SHORTCUT requires a shortcut name as target\')\n    return errors\n\n\ndef execute(ctx, parsed_op, result):\n    name = (parsed_op.target or \'\').strip()\n    url = \'shortcuts://run-shortcut?name=\' + quote(name)\n    try:\n        webbrowser.open(url)\n        result[\'status\'] = \'APPLIED\'\n        result[\'message\'] = \'Fired: \' + name\n    except Exception as e:\n        result[\'status\'] = \'FAILED_IO\'\n        result[\'message\'] = type(e).__name__ + \': \' + str(e)\n',
    'forge2/parser_main.py': '# -*- coding: utf-8 -*-\n"""\nparser_main.py\n==============\nForge2 parser entry point.\n"""\n\nfrom forge2.bundle_models import ParsedOp, ParseResult\nfrom forge2.directive_parsers import parse_key_value_line, parse_lines_value\nfrom forge2.lexer import is_exact_begin_body, is_exact_end_body\nfrom forge2.registry import OP_SPECS\nfrom forge2.validators import validate_op_shape\n\n\ndef _match_op_header(raw_line):\n    if raw_line and raw_line[0] in (\' \', \'\\t\'):\n        return None, None\n    s = (raw_line or \'\').strip()\n    matches = []\n    for op_name in OP_SPECS.keys():\n        prefix = op_name + \' \'\n        if s == op_name or s.startswith(prefix):\n            matches.append(op_name)\n    if not matches:\n        return None, None\n    matches.sort(key=len, reverse=True)\n    op_name = matches[0]\n    target = s[len(op_name):].strip()\n    return op_name, target\n\ndef parse_bundle(text):\n    """\n    Forge2 bundle parser.\n    BEGIN_BODY / END_BODY are optional delimiters.\n    Use them when body content contains lines that look like op headers.\n    Without them, any non-directive content after directives is collected\n    as the body until the next op header.\n    """\n    if not text or not text.strip():\n        return ParseResult(ops=[], default_file=None, errors=[])\n\n    lines = text.splitlines()\n    ops = []\n    errors = []\n    default_file = None\n    i = 0\n\n    body_forbidden_ops = set(\n        name for name, spec in OP_SPECS.items()\n        if spec.get(\'body_mode\') == \'forbidden\'\n    )\n\n    def is_valid_directive_key(key):\n        """Directive keys are ALL_CAPS with underscores only — no spaces."""\n        return bool(key) and key == key.upper() and \' \' not in key\n\n    while i < len(lines):\n        while i < len(lines) and not lines[i].strip():\n            i += 1\n        if i >= len(lines):\n            break\n\n        raw = lines[i]\n        s = raw.strip()\n\n        if s.startswith(\'DEFAULT_FILE \'):\n            default_file = s[len(\'DEFAULT_FILE \'):].strip() or None\n            i += 1\n            continue\n\n        op_name, target = _match_op_header(raw)\n        if not op_name:\n            errors.append(\'Expected known op header at line %d: %r\' % (i + 1, raw))\n            return ParseResult(ops=ops, default_file=default_file, errors=errors)\n\n        i += 1\n        directives = {}\n        body = \'\'\n\n        while i < len(lines):\n            line = lines[i]\n\n            # Explicit body delimiter\n            if is_exact_begin_body(line):\n                i += 1\n                body_lines = []\n                while i < len(lines) and not is_exact_end_body(lines[i]):\n                    body_lines.append(lines[i])\n                    i += 1\n                if i >= len(lines):\n                    errors.append(\'Missing END_BODY for %s %s\' % (op_name, target))\n                    return ParseResult(ops=ops, default_file=default_file, errors=errors)\n                i += 1\n                body = \'\\n\'.join(body_lines).rstrip() + \'\\n\' if body_lines else \'\'\n                break\n\n            # Next op header — end of this op\n            next_op_name, _ = _match_op_header(line)\n            if next_op_name or line.strip().startswith(\'DEFAULT_FILE \'):\n                break\n\n            if line.strip():\n                key, value = parse_key_value_line(line)\n                if key is not None and is_valid_directive_key(key):\n                    # Valid directive\n                    if key == \'LINES\':\n                        parsed = parse_lines_value(value)\n                        if parsed is None:\n                            errors.append(\'Invalid LINES directive at line %d: %r\' % (i + 1, line))\n                            return ParseResult(ops=ops, default_file=default_file, errors=errors)\n                        directives[key] = parsed\n                    elif key == \'LINE\':\n                        try:\n                            directives[key] = int(value.strip())\n                        except ValueError:\n                            errors.append(\'Invalid LINE directive at line %d: %r\' % (i + 1, line))\n                            return ParseResult(ops=ops, default_file=default_file, errors=errors)\n                    else:\n                        directives[key] = value\n                else:\n                    # Not a directive — implicit body starts here\n                    if op_name in body_forbidden_ops:\n                        errors.append(\'Unexpected non-directive line at line %d: %r\' % (i + 1, line))\n                        return ParseResult(ops=ops, default_file=default_file, errors=errors)\n                    body_lines = []\n                    while i < len(lines):\n                        next_op, _ = _match_op_header(lines[i])\n                        if next_op or lines[i].strip().startswith(\'DEFAULT_FILE \'):\n                            break\n                        body_lines.append(lines[i])\n                        i += 1\n                    body = \'\\n\'.join(body_lines).rstrip() + \'\\n\' if body_lines else \'\'\n                    break\n\n            i += 1\n\n        ops.append(ParsedOp(\n            op=op_name,\n            target=target,\n            directives=directives,\n            body=body,\n            default_file=default_file,\n        ))\n\n    final_errors = []\n    for op in ops:\n        final_errors.extend(validate_op_shape(op.op, op.directives, op.body))\n\n    return ParseResult(ops=ops, default_file=default_file, errors=final_errors)\n\n\ndef main():\n    demo = \'CREATE_FILE demo.txt\\nBEGIN_BODY\\nhello\\nEND_BODY\\n\'\n    result = parse_bundle(demo)\n    print(\'ops =\', len(result.ops))\n    print(\'errors =\', len(result.errors))\n\n\nif __name__ == \'__main__\':\n    main()\n',
    'forge2/registry.py': '# -*- coding: utf-8 -*-\n"""\nregistry.py\n===========\nAuto-discovers all ops from forge2/ops/*.py at import time.\n\nTo add an op: create forge2/ops/new_op.py with SPEC/validate/execute.\nNothing else needed.\n"""\nimport importlib, pkgutil\nimport forge2.ops as _ops_pkg\n\nOP_MODULES = [\n    importlib.import_module(f\'forge2.ops.{m.name}\')\n    for m in pkgutil.iter_modules(_ops_pkg.__path__)\n]\n\nOPS_BY_NAME = {}\nOP_SPECS = {}\nfor mod in OP_MODULES:\n    spec = getattr(mod, \'SPEC\', None) or {}\n    name = spec.get(\'name\')\n    if name:\n        OPS_BY_NAME[name] = mod\n        OP_SPECS[name] = spec\n\ndef get_op_module(op_name):\n    return OPS_BY_NAME.get(op_name)\n',
    'forge2/result_models.py': '# -*- coding: utf-8 -*-\n"""\nresult_models.py\n================\nNormalized execution-result helpers.\n"""\n\ndef make_result(op_name, target):\n    return {\n        \'op\': op_name,\n        \'target\': target,\n        \'status\': \'UNKNOWN\',\n        \'message\': \'\',\n        \'preview\': \'\',\n        \'file\': \'\',\n    }\n',
    'forge2/run_packet.py': '# -*- coding: utf-8 -*-\n"""\nrun_packet.py\n=============\nSimple run-packet formatter for parser-lab engine.\n"""\n\ndef format_run_packet(results, stamp=None, step=None, max_steps=None):\n    # filter out silent results for packet display\n    visible = [r for r in results if not r.get(\'silent\')]\n\n    # if all ops were silent and all applied, return empty string\n    if not visible:\n        all_applied = all(r.get(\'status\') == \'APPLIED\' for r in results)\n        if all_applied:\n            return \'\'\n\n    applied = sum(1 for r in results if r.get(\'status\') == \'APPLIED\')\n    failed  = sum(1 for r in results if (r.get(\'status\') or \'\').startswith(\'FAILED\'))\n    skipped = len(results) - applied - failed\n\n    lines = [\'=== FORGE2 RUN ===\']\n    if stamp:\n        lines.append(\'Run: \' + stamp)\n    if step is not None and max_steps is not None:\n        lines.append(\'Step: %d/%d\' % (step, max_steps))\n    lines += [\'\', \'Ops:\']\n\n    for r in visible:\n        line = \'- %s | %s | %s\' % (\n            r.get(\'status\') or \'UNKNOWN\',\n            r.get(\'op\') or \'?\',\n            r.get(\'target\') or \'?\',\n        )\n        msg = r.get(\'message\') or \'\'\n        if msg:\n            line += \' :: \' + msg\n        lines.append(line)\n\n    lines += [\'\', \'APPLIED=%d  SKIPPED=%d  FAILED=%d\' % (applied, skipped, failed)]\n\n    previews = [r for r in visible if r.get(\'preview\')]\n    if previews:\n        lines.append(\'\')\n        lines.append(\'=== PREVIEW ===\')\n        for r in previews:\n            lines.append(r.get(\'preview\'))\n\n    return \'\\n\'.join(lines).rstrip() + \'\\n\'\n',
    'forge2/run_storage.py': '# -*- coding: utf-8 -*-\n"""\nforge2.run_storage\n==================\nRun persistence, pruning, and revert for forge2.\nSelf-contained - no forge1 imports.\n"""\n\nimport os\nimport json\nimport hashlib\nimport datetime\n\n\ndef _sha256(text):\n    return hashlib.sha256((text or \'\').encode(\'utf-8\')).hexdigest()\n\n\ndef _read(path):\n    with open(path, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n        return f.read()\n\n\ndef _write(path, text):\n    ensure_dir(os.path.dirname(path))\n    with open(path, \'w\', encoding=\'utf-8\') as f:\n        f.write(text)\n\n\ndef ensure_dir(path):\n    if path and not os.path.isdir(path):\n        os.makedirs(path)\n\n\ndef now_stamp():\n    return datetime.datetime.now().strftime(\'%Y%m%d_%H%M%S\')\n\n\ndef runs_root(project_root):\n    from forge2.config import RUNS_DIRNAME\n    return os.path.join(project_root, RUNS_DIRNAME)\n\n\ndef list_runs(project_root):\n    rr = runs_root(project_root)\n    if not os.path.isdir(rr):\n        return []\n    items = [n for n in os.listdir(rr)\n             if os.path.isdir(os.path.join(rr, n))]\n    items.sort(reverse=True)\n    return items\n\n\ndef prune_runs(project_root):\n    from forge2.config import KEEP_RUNS\n    rr = runs_root(project_root)\n    if not os.path.isdir(rr):\n        return\n    runs = list_runs(project_root)\n    if len(runs) <= KEEP_RUNS:\n        return\n    for name in runs[KEEP_RUNS:]:\n        p = os.path.join(rr, name)\n        try:\n            for root, dirs, files in os.walk(p, topdown=False):\n                for fn in files:\n                    try:\n                        os.remove(os.path.join(root, fn))\n                    except Exception:\n                        pass\n                for dn in dirs:\n                    try:\n                        os.rmdir(os.path.join(root, dn))\n                    except Exception:\n                        pass\n            os.rmdir(p)\n        except Exception:\n            pass\n\n\ndef write_run_artifacts(project_root, stamp, bundle_text, results, touched_files):\n    rr = runs_root(project_root)\n    run_dir = os.path.join(rr, stamp)\n    snap_dir = os.path.join(run_dir, \'snapshots\')\n    log_dir = os.path.join(run_dir, \'logs\')\n    ensure_dir(snap_dir)\n    ensure_dir(log_dir)\n\n    _write(os.path.join(run_dir, \'bundle.txt\'), (bundle_text or \'\').strip() + \'\\n\')\n\n    touched_list = []\n    for file_abs, meta in touched_files.items():\n        rel = os.path.relpath(file_abs, project_root)\n        snap_path = os.path.join(snap_dir, rel)\n        _write(snap_path, meta.get(\'before\') or \'\')\n        touched_list.append({\n            \'rel\': rel,\n            \'snapshot_rel\': os.path.relpath(snap_path, run_dir),\n            \'before_sha\': _sha256(meta.get(\'before\') or \'\'),\n            \'after_sha\': _sha256(meta.get(\'after\') or \'\'),\n        })\n\n    manifest = {\n        \'stamp\': stamp,\n        \'root\': os.path.abspath(project_root),\n        \'bundle_sha\': _sha256(bundle_text or \'\'),\n        \'results\': results,\n\n        \'results\': results,\n\n        \'touched\': touched_list,\n    }\n    _write(os.path.join(run_dir, \'manifest.json\'),\n           json.dumps(manifest, ensure_ascii=False, indent=2) + \'\\n\')\n\n    summary_lines = [\'Run: \' + stamp, \'\']\n    for r in results:\n        st = r.get(\'status\') or \'UNKNOWN\'\n        line = (\'%-22s %-16s %s  %s\' % (\n            st, r.get(\'op\', \'?\'), r.get(\'target\', \'?\'), r.get(\'message\', \'\')\n        )).rstrip()\n        summary_lines.append(line)\n    _write(os.path.join(log_dir, \'run_summary.txt\'),\n           \'\\n\'.join(summary_lines) + \'\\n\')\n\n    with open(os.path.join(log_dir, \'run_log.jsonl\'), \'w\', encoding=\'utf-8\') as f:\n        for r in results:\n            f.write(json.dumps(r, ensure_ascii=False) + \'\\n\')\n\n\ndef revert_run(project_root, run_stamp):\n    run_dir = os.path.join(runs_root(project_root), run_stamp)\n    manifest_path = os.path.join(run_dir, \'manifest.json\')\n\n    if not os.path.isfile(manifest_path):\n        return False, \'Manifest not found for run: \' + run_stamp\n\n    try:\n        manifest = json.loads(_read(manifest_path))\n    except Exception as e:\n        return False, \'Manifest unreadable: \' + type(e).__name__ + \': \' + str(e)\n\n    touched = manifest.get(\'touched\') or []\n    snap_dir = os.path.join(run_dir, \'snapshots\')\n\n    if not os.path.isdir(snap_dir):\n        return False, \'Snapshots folder missing for run: \' + run_stamp\n\n    restored = 0\n    failed = 0\n    errors = []\n\n    for t in touched:\n        rel = t.get(\'rel\')\n        if not rel:\n            continue\n        snap_path = os.path.join(snap_dir, rel)\n        target_path = os.path.abspath(os.path.join(project_root, rel))\n        try:\n            src = _read(snap_path)\n            _write(target_path, src)\n            restored += 1\n        except Exception as e:\n            failed += 1\n            errors.append(\'%s: %s: %s\' % (rel, type(e).__name__, str(e)))\n\n    if failed:\n        msg = \'Revert completed with errors. Restored %d, failed %d.\' % (restored, failed)\n        if errors:\n            msg += \' \' + \'; \'.join(errors[:3])\n        return False, msg\n\n    return True, \'Reverted run %s (%d file(s) restored).\' % (run_stamp, restored)\n',
    'forge2/source_ops.py': '# -*- coding: utf-8 -*-\n"""\nsource_ops.py\n=============\nSmall source-text helpers for forge2 write ops.\n"""\n\nimport textwrap\n\n\ndef get_line_indent(line):\n    s = line or \'\'\n    return s[:len(s) - len(s.lstrip(\' \\t\'))]\n\n\ndef replace_line_range(source_text, start_line, end_line, new_body):\n    """\n    Replace 1-based inclusive line range with new_body text.\n    """\n    lines = source_text.splitlines(True)\n    if not lines:\n        return (new_body or \'\').strip(\'\\n\') + \'\\n\'\n\n    if start_line < 1 or end_line < start_line or end_line > len(lines):\n        raise ValueError(\'Invalid line range %d..%d for %d-line source\' % (\n            start_line, end_line, len(lines)\n        ))\n\n    before = lines[:start_line - 1]\n    after = lines[end_line:]\n\n    indent = get_line_indent(lines[start_line - 1])\n    replacement_block = textwrap.dedent((new_body or \'\').strip(\'\\n\'))\n\n    new_lines = []\n    for line in replacement_block.splitlines():\n        if line.strip():\n            new_lines.append(indent + line + \'\\n\')\n        else:\n            new_lines.append(\'\\n\')\n\n    if after and after[0].strip():\n        new_lines.append(\'\\n\')\n\n    return \'\'.join(before) + \'\'.join(new_lines) + \'\'.join(after)\n\n\ndef insert_after_lines(source_text, line_no, insert_block, indent, tight=False):\n    """\n    Insert after line_no (1-based).\n    tight=True suppresses auto blank lines before/after the inserted block.\n    """\n    src_lines = source_text.splitlines(True)\n\n    if line_no < 0 or line_no > len(src_lines):\n        raise ValueError(\'Invalid insert position after line %d in %d-line source\' % (\n            line_no, len(src_lines)\n        ))\n\n    insert_block = textwrap.dedent((insert_block or \'\').strip(\'\\n\'))\n\n    ins_lines = []\n    for line in insert_block.splitlines():\n        if line.strip():\n            ins_lines.append(indent + line + \'\\n\')\n        else:\n            ins_lines.append(\'\\n\')\n\n    before = src_lines[:line_no]\n    after = src_lines[line_no:]\n\n    if not tight:\n        if before and before[-1].strip():\n            ins_lines.insert(0, \'\\n\')\n        if after and after[0].strip():\n            ins_lines.append(\'\\n\')\n\n    if src_lines and not src_lines[-1].endswith(\'\\n\'):\n        src_lines[-1] = src_lines[-1] + \'\\n\'\n\n    return \'\'.join(before) + \'\'.join(ins_lines) + \'\'.join(after)\n',
    'forge2/target_resolver.py': '# -*- coding: utf-8 -*-\n"""\ntarget_resolver.py\n==================\nTarget parsing and AST target resolution for forge2.\n"""\n\nimport os\n\nfrom forge2.ast_bridge import resolve_target_range\n\n\ndef parse_target_ref(target_ref, default_file=None):\n    raw = (target_ref or \'\').strip()\n    if \'::\' in raw:\n        file_ref, rhs = raw.split(\'::\', 1)\n    else:\n        file_ref, rhs = default_file, raw\n\n    if not file_ref:\n        return None\n\n    file_ref = file_ref.strip()\n    rhs = (rhs or \'\').strip()\n\n    class_name = None\n    target_name = rhs\n\n    if \'.\' in rhs:\n        lhs, tail = rhs.split(\'.\', 1)\n        if tail:\n            class_name = lhs.strip()\n            target_name = tail.strip()\n\n    return {\n        \'file_ref\': file_ref,\n        \'class_name\': class_name,\n        \'target_name\': target_name,\n    }\n\n\ndef resolve_ast_target(project_root, target_ref, default_file=None):\n    parsed = parse_target_ref(target_ref, default_file=default_file)\n    if not parsed:\n        return None\n\n    file_ref = parsed[\'file_ref\']\n    if os.path.isabs(file_ref):\n        file_abs = os.path.abspath(file_ref)\n    else:\n        file_abs = os.path.abspath(os.path.join(project_root, file_ref))\n\n    if not os.path.isfile(file_abs):\n        return {\n            \'ok\': False,\n            \'error\': \'File not found: \' + str(file_ref),\n        }\n\n    with open(file_abs, \'r\', encoding=\'utf-8\', errors=\'replace\') as f:\n\n        src = f.read()\n\n    start, end, kind = resolve_target_range(\n        src,\n        parsed[\'class_name\'],\n        parsed[\'target_name\'],\n    )\n\n    if not start or not end:\n        return {\n            \'ok\': False,\n            \'error\': \'Target not found: \' + str(target_ref),\n        }\n\n    return {\n        \'ok\': True,\n        \'file_abs\': file_abs,\n        \'file_ref\': file_ref,\n        \'class_name\': parsed[\'class_name\'],\n        \'target_name\': parsed[\'target_name\'],\n        \'kind\': kind,\n        \'start\': start,\n        \'end\': end,\n        \'source_text\': src,\n    }\n',
    'forge2/validators.py': '# -*- coding: utf-8 -*-\n"""\nvalidators.py\n=============\nValidation helpers for parsed ops.\n"""\n\nfrom forge2.registry import OP_SPECS\n\n\ndef validate_op_shape(op_name, directives, body):\n    spec = OP_SPECS.get(op_name)\n    if spec is None:\n        return [\'Unknown op: \' + str(op_name)]\n\n    errors = []\n\n    allowed = spec.get(\'allowed_directives\', set())\n    required = spec.get(\'required_directives\', set())\n    body_mode = spec.get(\'body_mode\', \'forbidden\')\n\n    for key in directives.keys():\n        if key not in allowed:\n            errors.append(\'Directive not allowed for %s: %s\' % (op_name, key))\n\n    for key in required:\n        if key not in directives:\n            errors.append(\'Missing required directive for %s: %s\' % (op_name, key))\n\n    has_body = bool(body)\n\n    if body_mode == \'required\' and not has_body:\n        errors.append(\'Body required for \' + op_name)\n    elif body_mode == \'forbidden\' and has_body:\n        errors.append(\'Body not allowed for \' + op_name)\n\n    return errors\n',
    'forge2_shortcut.py': '# -*- coding: utf-8 -*-\n"""\nforge2_shortcut.py\n==================\nSide-by-side Shortcut entry point for forge2.\n\nInitial version:\n- reads clipboard bundle\n- runs forge2 bridge\n- copies run packet back to clipboard\n- prints packet\n"""\n\nimport os\nimport clipboard\n\nfrom forge2.entry import run_from_text\n\n\ndef run():\n    bundle_text = clipboard.get() or \'\'\n    result = run_from_text(bundle_text, project_root=os.path.expanduser(\'~/Documents\'))\n    packet = result[\'packet\']\n    clipboard.set(packet)\n\n    try:\n        import console as _con\n        ctx = result.get(\'context\')\n        results = ctx.results if ctx else []\n        for r in results:\n            status = r.get(\'status\') or \'UNKNOWN\'\n            op = r.get(\'op\') or \'?\'\n            target = r.get(\'target\') or \'?\'\n            msg = r.get(\'message\') or \'\'\n            line = status + \' | \' + op + \' | \' + target\n            if msg:\n                line += \' :: \' + msg\n            if status == \'APPLIED\':\n                _con.set_color(0.2, 0.9, 0.4)\n            elif status.startswith(\'SKIPPED\'):\n                _con.set_color(1.0, 0.8, 0.0)\n            elif status.startswith(\'FAILED\'):\n                _con.set_color(1.0, 0.3, 0.3)\n            else:\n                _con.set_color(0.8, 0.8, 0.8)\n            print(line)\n            _con.set_color(1, 1, 1)\n        print()\n        print(packet)\n    except Exception:\n        print(packet)\n\nif __name__ == \'__main__\':\n    run()\n',
    'forge_terminal.py': '# -*- coding: utf-8 -*-\n"""\nforge_terminal.py\n=================\nPersistent terminal UI for forge2.\nRun bundles from clipboard or type them directly.\nOutput accumulates across runs.\n"""\n\nimport ui\nimport os\nimport sys\nimport time\n\nsys.path.insert(0, os.path.expanduser(\'~/Documents\'))\n\nDARK_BG     = \'#0d0d0d\'\nPANEL_BG    = \'#1a1a1a\'\nINPUT_BG    = \'#111111\'\nTEXT_GREEN  = \'#2ecc71\'\nTEXT_YELLOW = \'#f1c40f\'\nTEXT_RED    = \'#e74c3c\'\nTEXT_DIM    = \'#555555\'\nTEXT_WHITE  = \'#f0f0f0\'\nFONT_MONO   = (\'Menlo\', 13)\nFONT_SMALL  = (\'Menlo\', 11)\n\n\nclass ForgeTerminal(ui.View):\n\n    def __init__(self):\n        self.background_color = DARK_BG\n        self._build()\n\n    def _build(self):\n        # Output area\n        output = ui.TextView()\n        output.editable = False\n        output.selectable = True\n        output.font = FONT_MONO\n        output.text_color = TEXT_WHITE\n        output.background_color = PANEL_BG\n        output.corner_radius = 6\n        self._output = output\n        self.add_subview(output)\n\n        # Autocomplete bar\n        ac_bar = ui.ScrollView()\n        ac_bar.background_color = \'#1a1a1a\'\n        ac_bar.shows_horizontal_scroll_indicator = False\n        self._ac_bar = ac_bar\n        self._ac_pills = []\n        self.add_subview(ac_bar)\n\n        # Bundle input area\n        bundle_input = ui.TextView()\n        bundle_input.font = FONT_MONO\n        bundle_input.text_color = TEXT_WHITE\n        bundle_input.background_color = INPUT_BG\n        bundle_input.corner_radius = 6\n        bundle_input.tint_color = TEXT_GREEN\n        bundle_input.placeholder = \'Type a bundle here...\'\n        bundle_input.delegate = self\n        self._input = bundle_input\n        self.add_subview(bundle_input)\n\n        # Dismiss keyboard button\n        dismiss_btn = ui.Button()\n        dismiss_btn.title = \'⌨\'\n        dismiss_btn.font = (\'Menlo\', 16)\n        dismiss_btn.background_color = \'#222222\'\n        dismiss_btn.tint_color = TEXT_DIM\n        dismiss_btn.corner_radius = 6\n        dismiss_btn.action = lambda s: self._input.end_editing()\n        self._dismiss_btn = dismiss_btn\n        self.add_subview(dismiss_btn)\n\n        # Run Clipboard button\n        clip_btn = ui.Button()\n        clip_btn.title = \'⬇ Run Clipboard\'\n        clip_btn.font = (\'Menlo\', 14)\n        clip_btn.background_color = \'#1e3a2e\'\n        clip_btn.tint_color = TEXT_GREEN\n        clip_btn.corner_radius = 8\n        clip_btn.action = lambda s: self._run_clipboard()\n        self._clip_btn = clip_btn\n        self.add_subview(clip_btn)\n\n        # Run Bundle button\n        run_btn = ui.Button()\n        run_btn.title = \'▶ Run Bundle\'\n        run_btn.font = (\'Menlo\', 14)\n        run_btn.background_color = \'#1a2a3a\'\n        run_btn.tint_color = \'#3498db\'\n        run_btn.corner_radius = 8\n        run_btn.action = lambda s: self._run_bundle()\n        self._run_btn = run_btn\n        self.add_subview(run_btn)\n\n        # Header label\n        header = ui.Label()\n        header.text = \'⚡ FORGE2 TERMINAL\'\n        header.font = (\'Menlo\', 12)\n        header.text_color = TEXT_DIM\n        header.alignment = ui.ALIGN_CENTER\n        self._header = header\n        self.add_subview(header)\n\n        self._keyboard_h = 0\n        self._append_output(\'FORGE2 TERMINAL READY\\n\', TEXT_DIM)\n        self._append_output(\'Paste a bundle or type one below.\\n\\n\', TEXT_DIM)\n\n    def layout(self):\n        w = self.width\n        h = self.height\n        p = 10\n        btn_h = 44\n        input_h = 100\n        header_h = 30\n        ac_h = 36\n        dismiss_w = 44\n\n        self._header.frame = (0, 44, w, header_h)\n        top = 44 + header_h + p\n\n        safe_h = h - self._keyboard_h\n        btn_y = safe_h - p - btn_h\n        ac_y = btn_y - p - ac_h\n        input_y = ac_y - p - input_h\n        output_h = input_y - top - p\n\n        self._output.frame = (p, top, w - p * 2, max(output_h, 60))\n        self._input.frame = (p, input_y, w - p * 2 - dismiss_w - p, input_h)\n        self._dismiss_btn.frame = (w - p - dismiss_w, input_y, dismiss_w, input_h)\n        self._ac_bar.frame = (0, ac_y, w, ac_h)\n\n        btn_w = (w - p * 3) / 2\n        self._clip_btn.frame = (p, btn_y, btn_w, btn_h)\n        self._run_btn.frame = (p * 2 + btn_w, btn_y, btn_w, btn_h)\n\n    def keyboard_frame_did_change(self, frame):\n        if frame.height > 100:\n            self._keyboard_h = frame.height\n        else:\n            self._keyboard_h = 0\n        self.layout()\n\n    def textview_did_change(self, tv):\n        text = tv.text or \'\'\n        pos = tv.selected_range[0]\n        line_start = text.rfind(\'\\n\', 0, pos)\n        line_start = 0 if line_start == -1 else line_start + 1\n        current_line = text[line_start:pos]\n        stripped = current_line.strip()\n\n        # Check if we\'re in path mode — op + space + optional path\n        parts = stripped.split(\' \', 1)\n        if len(parts) == 2:\n            from forge2.registry import OP_SPECS\n            op = parts[0].upper()\n            if op in OP_SPECS:\n                self._update_path_complete(parts[1], line_start, op)\n                return\n\n        self._update_autocomplete(stripped)\n\n    def _update_path_complete(self, partial, line_start, op):\n        for sub in list(self._ac_bar.subviews):\n            self._ac_bar.remove_subview(sub)\n        self._ac_pills = []\n\n        root = os.path.expanduser(\'~/Documents\')\n\n        # Determine directory to list\n        if \'/\' in partial:\n            dir_part = partial.rsplit(\'/\', 1)[0] + \'/\'\n            prefix = partial.rsplit(\'/\', 1)[1]\n        else:\n            dir_part = \'\'\n            prefix = partial\n\n        dir_abs = os.path.join(root, dir_part) if dir_part else root\n\n        try:\n            entries = sorted(os.listdir(dir_abs))\n        except Exception:\n            return\n\n        matches = [e for e in entries\n                   if e.startswith(prefix)\n                   and not e.startswith(\'.\')\n                   and e != \'__pycache__\']\n\n        x = 8\n        for name in matches[:12]:\n            full_abs = os.path.join(dir_abs, name)\n            is_dir = os.path.isdir(full_abs)\n            display = name + \'/\' if is_dir else name\n            rel_path = dir_part + name + (\'/\' if is_dir else \'\')\n\n            pill = ui.Button()\n            pill.title = display\n            pill.font = (\'Menlo\', 12)\n            pill.background_color = \'#2a1a3a\' if is_dir else \'#1a2a3a\'\n            pill.tint_color = \'#9b59b6\' if is_dir else \'#3498db\'\n            pill.corner_radius = 6\n            pill.size_to_fit()\n            pill_w = pill.width + 16\n            pill.frame = (x, 4, pill_w, 28)\n            pill.action = lambda s, rp=rel_path, ls=line_start, o=op: self._insert_path(rp, ls, o)\n            self._ac_bar.add_subview(pill)\n            self._ac_pills.append(pill)\n            x += pill_w + 8\n\n        self._ac_bar.content_size = (x, 36)\n\n    def _insert_path(self, rel_path, line_start, op):\n        new_line = op + \' \' + rel_path\n        text = self._input.text or \'\'\n        # Find end of current line\n        pos = self._input.selected_range[0]\n        line_end = text.find(\'\\n\', line_start)\n        if line_end == -1:\n            line_end = len(text)\n        new_text = text[:line_start] + new_line + text[line_end:]\n        self._input.text = new_text\n        new_pos = min(line_start + len(new_line), len(new_text))\n        ui.delay(lambda *a: self._set_cursor(new_pos), 0.05)\n        # Stay in path mode — refresh pills\n        ui.delay(lambda *a: self._update_path_complete(rel_path, line_start, op), 0.1)\n\n    def _update_autocomplete(self, prefix):\n        # Clear by removing all subviews from the bar directly\n        for sub in list(self._ac_bar.subviews):\n            self._ac_bar.remove_subview(sub)\n        self._ac_pills = []\n\n        if not prefix or not prefix[0].isupper():\n            return\n\n        try:\n            from forge2.registry import OP_SPECS\n            matches = [op for op in sorted(OP_SPECS.keys())\n                       if op.startswith(prefix.upper())]\n        except Exception:\n            return\n\n        x = 8\n        for op in matches[:8]:\n            pill = ui.Button()\n            pill.title = op\n            pill.font = (\'Menlo\', 12)\n            pill.background_color = \'#2a2a2a\'\n            pill.tint_color = TEXT_GREEN\n            pill.corner_radius = 6\n            pill.size_to_fit()\n            pill_w = pill.width + 16\n            pill.frame = (x, 4, pill_w, 28)\n            pill.action = lambda s, op=op: self._insert_op(op)\n            self._ac_bar.add_subview(pill)\n            self._ac_pills.append(pill)\n            x += pill_w + 8\n\n        self._ac_bar.content_size = (x, 36)\n\n    def _insert_op(self, op):\n        text = self._input.text or \'\'\n        pos = self._input.selected_range[0]\n        line_start = text.rfind(\'\\n\', 0, pos)\n        line_start = 0 if line_start == -1 else line_start + 1\n        new_text = text[:line_start] + op + \' \' + text[pos:]\n        self._input.text = new_text\n        new_pos = min(line_start + len(op) + 1, len(new_text))\n        ui.delay(lambda *a: self._set_cursor(new_pos), 0.05)\n\n        # Show root path pills immediately\n        ui.delay(lambda *a: self._update_path_complete(\'\', line_start, op), 0.1)\n\n        # Show HELP in console\n        self._run_help(op)\n\n    def _run_help(self, op):\n        to_remove = [k for k in sys.modules if k.startswith(\'forge2\')]\n        for k in to_remove:\n            del sys.modules[k]\n        try:\n            from forge2.op_help import render_op_help\n            help_text = render_op_help(op)\n            if help_text:\n                self._append_output(\'─\' * 40 + \'\\n\', TEXT_DIM)\n                self._append_output(help_text + \'\\n\', TEXT_WHITE)\n        except Exception as e:\n            self._append_output(\'HELP error: %s\\n\' % e, TEXT_RED)\n\n    def _build_snippet(self, op):\n        try:\n            from forge2.registry import OP_SPECS, OP_MODULES\n            spec = OP_SPECS.get(op, {})\n            mod = next((m for m in OP_MODULES\n                        if getattr(m, \'SPEC\', {}).get(\'name\') == op), None)\n\n            required = sorted(spec.get(\'required_directives\', set()))\n            body_mode = spec.get(\'body_mode\', \'forbidden\')\n\n            # Use minimal_example from HELP if available\n            help_meta = getattr(mod, \'HELP\', None) or {}\n            if help_meta.get(\'minimal_example\'):\n                return \'\\n\'.join(help_meta[\'minimal_example\']) + \'\\n\'\n\n            # Build from SPEC\n            lines = [op + \' \']\n            for d in required:\n                lines.append(d + \': \')\n            if body_mode == \'required\':\n                lines.append(\'BEGIN_BODY\')\n                lines.append(\'\')\n                lines.append(\'END_BODY\')\n            return \'\\n\'.join(lines) + \'\\n\'\n        except Exception:\n            return op + \' \'\n\n    def _set_cursor(self, pos):\n        try:\n            n = len(self._input.text or \'\')\n            self._input.selected_range = (min(pos, n), min(pos, n))\n        except Exception:\n            pass\n\n    def keyboard_frame_did_change(self, frame):\n        if frame.height > 100:\n            self._keyboard_h = frame.height\n        else:\n            self._keyboard_h = 0\n        self.layout()\n\n    def _append_output(self, text, color=None):\n        import objc_util\n        tv = self._output\n        current = tv.text or \'\'\n        tv.text = current + text\n        # Scroll to bottom\n        ui.delay(lambda *a: self._scroll_to_bottom(), 0.05)\n\n    def _scroll_to_bottom(self):\n        try:\n            from objc_util import ObjCInstance\n            tv = ObjCInstance(self._output)\n            size = tv.contentSize()\n            offset = (0, max(0, size.height - self._output.height))\n            tv.setContentOffset_animated_(offset, False)\n        except Exception:\n            pass\n\n    def _run_clipboard(self):\n        import clipboard\n        bundle = clipboard.get() or \'\'\n        if not bundle.strip():\n            self._append_output(\'⚠ Clipboard is empty.\\n\\n\', TEXT_YELLOW)\n            return\n        self._append_output(\'⬇ Running from clipboard...\\n\', TEXT_DIM)\n        self._execute(bundle)\n\n    def _run_bundle(self):\n        bundle = self._input.text or \'\'\n        if not bundle.strip():\n            self._append_output(\'⚠ No bundle in editor.\\n\\n\', TEXT_YELLOW)\n            return\n        self._append_output(\'▶ Running bundle...\\n\', TEXT_DIM)\n        self._execute(bundle)\n\n    def _execute(self, bundle_text):\n        # Bust module cache\n        to_remove = [k for k in sys.modules if k.startswith(\'forge2\')]\n        for k in to_remove:\n            del sys.modules[k]\n\n        try:\n            from forge2.entry import run_from_text\n        except Exception as e:\n            self._append_output(\'FAILED TO IMPORT FORGE2: %s\\n\\n\' % e, TEXT_RED)\n            return\n\n        ts = time.strftime(\'%H:%M:%S\')\n        self._append_output(\'─\' * 40 + \'\\n\', TEXT_DIM)\n        self._append_output(\'[%s]\\n\' % ts, TEXT_DIM)\n\n        try:\n            result = run_from_text(\n                bundle_text,\n                project_root=os.path.expanduser(\'~/Documents\')\n            )\n        except Exception as e:\n            self._append_output(\'ERROR: %s\\n\\n\' % e, TEXT_RED)\n            return\n\n        ctx = result.get(\'context\')\n        packet = result.get(\'packet\', \'\')\n\n        # Copy packet to clipboard\n        try:\n            import clipboard\n            clipboard.set(packet)\n        except Exception:\n            pass\n\n        if ctx:\n            for r in ctx.results:\n                status = r.get(\'status\') or \'UNKNOWN\'\n                op     = r.get(\'op\') or \'?\'\n                target = r.get(\'target\') or \'?\'\n                msg    = r.get(\'message\') or \'\'\n                line   = \'%s | %s | %s\' % (status, op, target)\n                if msg:\n                    line += \' :: \' + msg\n\n                if status == \'APPLIED\':\n                    color = TEXT_GREEN\n                elif status.startswith(\'SKIPPED\'):\n                    color = TEXT_YELLOW\n                elif status.startswith(\'FAILED\'):\n                    color = TEXT_RED\n                else:\n                    color = TEXT_WHITE\n\n                self._append_output(line + \'\\n\', color)\n\n            previews = [r for r in ctx.results if r.get(\'preview\')]\n            if previews:\n                self._append_output(\'\\n\', TEXT_DIM)\n                for r in previews:\n                    self._append_output(r.get(\'preview\', \'\') + \'\\n\', TEXT_WHITE)\n        else:\n            self._append_output(packet + \'\\n\', TEXT_WHITE)\n\n        self._append_output(\'\\n── packet copied to clipboard ──\\n\\n\', TEXT_DIM)\n\n\ndef run():\n    try:\n        import crash_guard\n        crash_guard.install()\n    except Exception as e:\n        print(\'crash_guard not available:\', e)\n    app = ForgeTerminal()\n    app.name = \'Forge Terminal\'\n    app.present(\'fullscreen\')\n    try:\n        crash_guard.mark_clean_exit()\n    except Exception:\n        pass\n\n\nif __name__ == \'__main__\':\n    run()\n',
}

def install(root=None):
    if root is None:
        root = os.path.expanduser('~/Documents')

    print('Installing forge2 to: ' + root)
    written = 0

    for rel, content in FILES.items():
        abs_path = os.path.join(root, rel)
        dir_path = os.path.dirname(abs_path)
        if not os.path.isdir(dir_path):
            os.makedirs(dir_path)
        with open(abs_path, 'w', encoding='utf-8') as f:
            f.write(content)
        print('  wrote: ' + rel)
        written += 1

    print('')
    print('Done. ' + str(written) + ' files installed.')
    print('')
    print('=== NEXT STEPS ===')
    print('')
    print('1. Set up your iOS Shortcut:')
    print('   - Open the Shortcuts app')
    print('   - New Shortcut > Add Action > Pythonista > Run Script')
    print('   - Set script to: forge2_shortcut.py')
    print('   - Save it as: Forge')
    print('   - Assign to Action Button, Home Screen, or Lock Screen')
    print('')
    print('2. Or use the terminal UI (no Shortcut needed):')
    print('   - Open forge_terminal.py in Pythonista and run it')
    print('   - Type op bundles directly, results appear inline')
    print('   - Good for exploring before setting up the Shortcut')
    print('')
    print('3. Start your first session with an LLM:')
    print('   - Open forge2/onboarding.md')
    print('   - Copy the entire contents')
    print('   - Paste into Claude (claude.ai) or another capable LLM')
    print('   - The LLM will orient itself and ask what you want to build')
    print('')
    print('Tip: run LIST_FILES . and LIST_OPS as your first bundle')
    print('     to show the LLM exactly what it has to work with.')
    print('in Pythonista to get started immediately.')

if __name__ == '__main__':
    install()
